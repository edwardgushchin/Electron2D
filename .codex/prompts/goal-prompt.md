Выполнить <task-id> по регламенту `.codex/prompts/goal-task-workflow.md`: прочитать каноническую `.taskboard/board.e2tasks` только через `e2d tasks`, затем пройти ready-to-start gate (`open`/ready state, зависимости accepted/closed или saved `ACCEPT`, внешний blocker только с явным user override). Если state `blocked`/`tracking`, зависимости не приняты или override нет — добавить blocker report через `e2d tasks comment add`, при необходимости поставить `Blocked` через `e2d tasks set-status`, обновить дневник и не делать production work, audit packaging или commit. Если gate пройден: довести задачу до сохранённых primary и control `VERDICT: ACCEPT`, закрыть actionable `RISKS_AND_NOTES`, обновить task activity/status только через CLI, дневник и release notes, по явному разрешению архивировать через `e2d tasks archive`, удалить временные audit-артефакты <task-id>, выполнить `e2d tasks verify --project . --format json` и остальные финальные проверки, затем сделать Conventional Commit без push. Цель не считается выполненной без saved verdict reports, `verify audit-followups`, финального `git status` и commit. Если `VERDICT: ACCEPT` получен с первой попытки, Control Accept не нужен. Если нужна финальная человеческая приёмка или явное решение оператора — поставить блокер цели.

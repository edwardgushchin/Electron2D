# Дневник разработки: 22-06-2026

## 00:01 +03:00 - Agent: Codex

- Задача: T-0094 / T-0095 / T-0096 / T-0102 blockers.
- Контекст: пользователь уточнил, что reference games нельзя закрывать, если для них не хватает публичного API или реальных ассетов. Временный reference platformer, собранный на data-only tilemap и synthetic resources, не должен считаться выполнением `T-0094`.
- Действия:
  - 23:57 - удалены незакоммиченные файлы временного `examples/reference-platformer`, verifier и CI-шаг.
  - 23:58 - `T-0094`, `T-0095` и `T-0096` помечены как blocked через `[!]` и получили пояснения по недостающим API/ассетам.
  - 23:58 - `T-0102` помечена как blocked, потому что её критерии требуют метрик по reference games.
  - 00:00 - добавлены новые активные задачи `T-0135` для публичного tilemap runtime API и `T-0136` для реальных ассетов reference games.
  - 00:00 - проверено, что временный пример, verifier и CI-изменения не остались в рабочей копии.
- Изменения: обновлён `TASKS.md`; временные изменения по примеру не сохранены.
- Решения: reference games должны блокироваться до полной готовности публичного API и ассетов; нельзя закрывать их placeholder-ресурсами, синтетическим звуком, data-only workaround-ом или isolated demo.
- Проверки: `git status --short` показывает только `TASKS.md` и дневники; поиск по `Verify-ReferencePlatformer` и `reference-platformer` не показывает временных файлов вне удалённого diff.
- Далее: следующая практическая разблокировка для platformer - выполнить `T-0135`; для обеих reference games также нужна `T-0136`.

## 00:02 +03:00 - Agent: Codex

- Задача: T-0094 / blocker cleanup verification.
- Контекст: после удаления временного примера нужно было проверить, что в рабочей копии остались только task/diary правки.
- Действия:
  - 00:02 - выполнен `git diff --check`.
  - 00:02 - проверено состояние рабочей копии.
  - 00:02 - выполнен поиск временных строк `Verify-ReferencePlatformer`, `examples/reference-platformer` и `reference-platformer` в `.github`, `tools`, `docs`, `examples` и `TASKS.md`.
- Изменения: новых файлов примера, verifier-а и CI-шагов нет.
- Решения: commit и push не выполнять, потому что задача не завершена; зафиксированы только blockers и новые задачи-разблокировки.
- Проверки: `git diff --check` прошёл без whitespace errors; `git status --short --branch` показывает изменения только в `TASKS.md` и дневниках; поиск временных reference-platformer строк не нашёл совпадений.
- Далее: продолжать с `T-0135` или другой незаблокированной задачей по указанию пользователя/очереди `TASKS.md`.

## 00:04 +03:00 - Agent: Codex

- Задача: T-0135 / публичный tilemap runtime API.
- Контекст: пользователь попросил продолжать работу; `T-0094` заблокирована отсутствием публичного tilemap API и ассетов, поэтому следующая практичная разблокировка - `T-0135`.
- Действия:
  - 00:04 - проверены `AGENTS.md`, `TASKS.md`, состояние рабочей копии и новые blocker-правки.
  - 00:04 - выбран `T-0135` как следующая задача: публичный runtime API для tilemap, нужный reference platformer.
- Изменения: добавлена запись дневника перед изменениями кода.
- Решения: реализовывать настоящий публичный API и тесты, а не helper внутри примера; blocker-правки `TASKS.md` остаются частью текущей рабочей ветки.
- Проверки: пока не запускались в рамках `T-0135`.
- Далее: сверить релизные документы, текущие rendering/resource/physics API и добавить спецификацию с тестами перед production-кодом.

## 00:12 +03:00 - Agent: Codex

- Задача: T-0135 / публичный tilemap runtime API.
- Контекст: задача требует добавить реальный tilemap runtime API, а не временный reference example. Дополнительно проверена форма `TileMapLayer`: публичный узел должен оставаться `Node2D`, а physics participation нужно делать внутренним поведением слоя.
- Действия:
  - 00:12 - добавлена спецификация `docs/specifications/rendering/tilemap-layer-runtime-api.md`.
  - 00:12 - добавлена документация реализации `docs/documentation/rendering/tilemap-layer-runtime-api.md`.
  - 00:12 - обновлены индексы `docs/specifications/README.md` и `docs/documentation/README.md`.
  - 00:12 - добавлен `tests/Electron2D.Tests.Integration/TileMapLayerRuntimeTests.cs`.
  - 00:12 - запущен `dotnet test src\Electron2D.sln --no-restore --filter TileMapLayerRuntimeTests`.
- Изменения: зафиксирован контракт для `TileSet`, `TileSetAtlasSource`, `TileData`, `TileMapLayer`, canvas submission и one-way tile collision.
- Решения: `TileMapLayer` не наследуется от `PhysicsBody2D`; collision будет внутренним query/movement surface.
- Проверки: `dotnet test src\Electron2D.sln --no-restore --filter TileMapLayerRuntimeTests` ожидаемо упал до реализации на отсутствующих публичных типах `TileSet`, `TileSetAtlasSource` и `TileMapLayer`.
- Далее: реализовать публичные tilemap типы, расширить canvas submission и physics query path.

## 00:40 +03:00 - Agent: Codex

- Задача: T-0135 / публичный tilemap runtime API.
- Контекст: нужно было завершить runtime API, закрыть сериализацию tile definitions, обновить wiki/API baseline и перенести задачу из `TASKS.md` в месячный архив.
- Действия:
  - 00:16 - добавлены public `TileSet`, `TileSetSource`, `TileSetAtlasSource`, `TileData` и `TileMapLayer`.
  - 00:18 - расширены `CanvasSubmissionContext`, `PhysicsQuery2D`, `PhysicsDirectSpaceState2D` и `PhysicsBody2DMotion` для rendering submission, direct queries и movement against tilemap collision.
  - 00:20 - фокусный `TileMapLayerRuntimeTests` доведён до green; позиция one-way collision сверена с существующим centered AABB solver.
  - 00:24 - обновлены public API baseline, GitHub Wiki API pages и `API-Compatibility.md`.
  - 00:35 - добавлены internal AOT-safe metadata `TileSetResourceMetadata` и integration test на `.e2res` round-trip tile definitions/collision metadata.
  - 00:38 - `T-0135` удалена из active `TASKS.md` и добавлена в `completed-tasks/2026/06 Июнь.md`.
  - 00:40 - повторно выполнены полные тесты и API/wiki/license/domain checks.
- Изменения: обновлены runtime rendering/physics/resource serialization файлы, tests, specs/docs, API wiki clone, `TASKS.md`, completed archive и дневник.
- Решения: `TileMapLayer` остаётся `Node2D`; tile collision хранится как внутренний RID/query surface. `TileSet` serializes tile definitions и collision/render metadata, а полный texture asset restore остаётся задачей resource/import pipeline и blocker-ассетов `T-0136`.
- Проверки: `dotnet test src\Electron2D.sln --no-restore` прошёл: unit `74/74`, integration `235/235`, runtime smoke `5/5`, golden-data `12/12`; `Verify-PublicApiXmlDocs.ps1 -FailOnIssues` прошёл, checked symbols `1389`; `Verify-ApiCompatibility.ps1` прошёл, public types `130`; `Verify-PublicApiDocumentationAudit.ps1 -WikiPath .github\wiki` прошёл, generated pages `147`; `Update-ApiWiki.ps1 -OutputPath .github\wiki -Check` прошёл, generated pages `147`; `Verify-SourceDomainLayout.ps1` прошёл, scanned source files `269`; `Verify-SourceLicenseHeaders.ps1` прошёл для tracked source files.
- Далее: staged/commit/push для `T-0135`; `T-0094` всё ещё заблокирована `T-0136`, потому что реальные ассеты reference games не готовы.

## 00:49 +03:00 - Agent: Codex

- Задача: T-0093 / T-0094 / blocker уточнение по examples.
- Контекст: пользователь уточнил, что цели для примеров тоже должны оставаться в blocker-е, пока нет реальных ассетов: звуков, спрайтов, UI/font resources и других локально проверяемых материалов.
- Действия:
  - 00:49 - проверен блок active tasks вокруг `T-0093`-`T-0096`, `T-0102` и `T-0136`.
  - 00:49 - `T-0093` помечена как `[!]` и получила прямые зависимости от `T-0094`, `T-0095` и `T-0136`.
  - 00:49 - заметка `T-0094` уточнена после закрытия `T-0135`: tilemap API готов, но reference platformer остаётся заблокирован отсутствием утверждённых ассетов и verifier-а из `T-0136`.
- Изменения: обновлён `TASKS.md`; production-код, specs и docs не менялись.
- Решения: platform smoke/soak для reference project не должен проверять временный demo или surrogate с placeholder resources.
- Проверки: `git diff --check` прошёл без whitespace errors; рабочая копия содержит только `TASKS.md` и текущий дневник.
- Далее: зафиксировать blocker-уточнение отдельным содержательным commit и выбрать следующую незаблокированную задачу из `TASKS.md`.

## 00:55 +03:00 - Agent: Codex

- Задача: T-0136 / реальные ассеты для reference games.
- Контекст: после `T-0135` единственная P0-задача без зависимостей - подготовить локальные licensed assets для reference platformer и UI-heavy reference game, чтобы `T-0094`, `T-0095`, `T-0093` и `T-0102` не закрывались на placeholder resources.
- Действия:
  - 00:55 - `T-0136` помечена как in progress в `TASKS.md`.
  - 00:55 - проверены источники Kenney/OpenGameArt для `Pixel Platformer`, `UI Pack` и `RPGsounds_Kenney`: страницы указывают author Kenney, CC0 license и состав архивов.
  - 00:55 - архивы скачаны во временную папку `.temp/asset-sources` только как staging input; build/verifier не будут использовать сетевые источники.
- Изменения: пока изменён только `TASKS.md` и дневник; durable assets ещё не добавлены.
- Решения: в репозиторий войдёт небольшой curated subset с manifest, license metadata и verifier-ом, а не весь внешний архив и не generated placeholder.
- Проверки: пока не запускались для `T-0136`.
- Далее: извлечь subset ассетов, добавить manifest, verifier, спецификацию, документацию и CI step.

## 01:03 +03:00 - Agent: Codex

- Задача: T-0136 / реальные ассеты для reference games.
- Контекст: нужно было завершить asset pack, сделать его проверяемым и перенести задачу из active `TASKS.md` в месячный архив.
- Действия:
  - 00:58 - извлечён curated subset из Kenney/OpenGameArt архивов в `assets/reference-games/`.
  - 00:59 - добавлены localization JSON, card-set JSON, `manifest.json`, `README.md` и `LICENSES.md`.
  - 01:00 - добавлен `tools\Verify-ReferenceGameAssets.ps1` и CI-step `Verify reference game assets`.
  - 01:01 - добавлены `docs/specifications/examples/reference-game-assets.md` и `docs/documentation/examples/reference-game-assets.md`, обновлены документационные индексы.
  - 01:02 - выполнены asset verifier, source license header verifier, user documentation verifier, CI matrix verifier и `git diff --check`.
  - 01:03 - `T-0136` удалена из active `TASKS.md` и перенесена в `completed-tasks/2026/06 Июнь.md`.
- Изменения: добавлены локальные assets, manifest, license metadata, verifier, docs/specs, CI step, archive entry и обновлён task tracker.
- Решения: полный upstream archive не хранить в репозитории; verifier запрещает `.url`, `.sfk`, cache/temp/helper files и файлы вне manifest.
- Проверки: `Verify-ReferenceGameAssets.ps1` прошёл, files `25`, sources `4`; `Verify-SourceLicenseHeaders.ps1` прошёл, tracked source files `392`; `Verify-UserDocumentation.ps1` прошёл; `Verify-CiMatrix.ps1` прошёл; `git diff --check` прошёл без whitespace errors. После архивирования повторно прошли `Verify-ReferenceGameAssets.ps1`, `Verify-SourceLicenseHeaders.ps1`, `Verify-CiMatrix.ps1` и `git diff --check`.
- Далее: commit/push `T-0136`; затем выбрать следующую незаблокированную задачу.

## 01:10 +03:00 - Agent: Codex

- Задача: T-0136 / исправление в том же commit.
- Контекст: пользователь уточнил правило "одна задача - один commit". CI run `27918927043` для commit `3490aff` упал на `Verify-ReferenceGameAssets.ps1`: на Linux/macOS text assets получили LF checkout и не совпали с manifest, подготовленным на Windows bytes.
- Действия:
  - 01:07 - изучен лог GitHub Actions: mismatch `tilemap-example-a.tmx`, expected `4576`, got `4495`.
  - 01:08 - добавлен `.gitattributes` с binary policy для `assets/reference-games/**`, чтобы asset bytes не менялись между ОС.
  - 01:09 - verifier исправлен для `Resolve-Path -Relative` на Windows/Linux/macOS (`.\...` и `./...`).
  - 01:10 - переиндексированы source level files под binary/no-EOL-normalization без создания нового task commit.
- Изменения: `.gitattributes`, три level source files в index как binary blobs, `tools\Verify-ReferenceGameAssets.ps1`.
- Решения: не создавать второй commit по `T-0136`; исправление будет внесено через `git commit --amend` и `push --force-with-lease`.
- Проверки: `Verify-ReferenceGameAssets.ps1` прошёл, files `25`, sources `4`; `Verify-CiMatrix.ps1` прошёл; `git diff --check` и `git diff --cached --check` прошли без whitespace errors.
- Далее: amend commit `T-0136: add reference game asset pack`, force-with-lease push и дождаться нового CI run.

## 01:17 +03:00 - Agent: Codex

- Задача: T-0094 / законченный reference platformer.
- Контекст: после закрытия `T-0135` и `T-0136` у platformer больше нет формальных blocker-ов: публичный tilemap API готов, а реальные локальные ассеты прошли отдельный verifier.
- Действия:
  - 01:17 - проверены `AGENTS.md`, чистое состояние рабочей копии, active `TASKS.md`, текущий дневник и каталог `examples/reference-platformer`.
  - 01:17 - `T-0094` переведена из blocker-а в работу и получила заметку о снятии blocker-а после `T-0136`.
- Изменения: обновлены `TASKS.md` и текущий дневник.
- Решения: пример должен быть проверяемой reference game на публичном API и реальных ассетах из `assets/reference-games/`, без placeholder resources и без второго commit-а по той же задаче.
- Проверки: на старте `git status --short --branch` показал чистую ветку `main...origin/main`; дополнительные проверки ещё не запускались.
- Далее: добавить spec/docs/verifier и tracked project для `examples/reference-platformer`, затем выполнить проверку и закрыть задачу одним commit-ом.

## 01:26 +03:00 - Agent: Codex

- Задача: приоритет reference games / active task queue.
- Контекст: пользователь уточнил, что примеры игр нужно делать в самый последний этап `0.1.0 Preview`.
- Действия:
  - 01:26 - остановлена работа по `T-0094` до изменения кода и без commit-а.
  - 01:26 - `T-0094` возвращена в blocker/отложенный статус; `T-0093`, `T-0095`, `T-0096` и reference-game часть `T-0102` получили заметки о переносе на финальную фазу.
- Изменения: обновлён `TASKS.md` и текущий дневник; production-код, specs, docs и assets не менялись.
- Решения: до reference games сначала закрывать не-example задачи релизного ядра, tooling, editor, export, documentation, verification и production repository.
- Проверки: дополнительные проверки ещё не запускались; это изменение task tracker-а и дневника.
- Далее: выбрать следующую незаблокированную не-example задачу из `TASKS.md`.

## 01:29 +03:00 - Agent: Codex

- Задача: T-0052 / input dispatch и UI focus pipeline.
- Контекст: после переноса reference games в финальный этап очередь задач показала цикл зависимостей между input, UI и editor tasks.
- Действия:
  - 01:29 - пересчитаны active dependencies по заголовкам completed archive, чтобы не считать ID из ссылок и заметок завершёнными задачами.
  - 01:29 - выбрана `T-0052` как следующая не-example runtime-задача.
  - 01:29 - у `T-0052` удалена ошибочная зависимость от `T-0066`, потому что animation timeline в редакторе не должен блокировать базовый input/focus pipeline.
- Изменения: обновлён `TASKS.md`; код пока не менялся.
- Решения: закрывать input pipeline до `Control` layout и editor UI, чтобы разорвать неверный dependency cycle без переноса игровых примеров вперёд.
- Проверки: выполнена ручная проверка зависимостей через active `TASKS.md` и `completed-tasks/2026/06 Июнь.md`.
- Далее: прочитать input/UI спецификации и текущую реализацию перед тестами `T-0052`.

## 01:50 +03:00 - Agent: Codex

- Задача: T-0052 / input dispatch и UI focus pipeline.
- Контекст: нужно было закрыть ранний runtime input/focus baseline после переноса reference games в финальную фазу.
- Действия:
  - 01:32 - добавлена целевая спецификация `docs/specifications/input/input-dispatch-ui-focus.md`.
  - 01:34 - добавлен красный integration test `InputDispatchControlTests`, который упал на отсутствующем `Control._GuiInput`.
  - 01:39 - реализованы `Viewport.SetInputAsHandled()`, `Node.GetViewport()`, handled-state в traversal, `Control._GuiInput()`, `AcceptEvent()`, `MouseFilter`, `FocusMode` и focus helpers.
  - 01:42 - focused tests стали зелёными: `InputDispatchControlTests` прошёл `4/4`.
  - 01:45 - добавлена документация реализации, обновлены public API baseline tests, generated GitHub Wiki API pages и compatibility table.
  - 01:48 - выполнены public API docs, license, wiki, user docs, domain layout и full test checks.
  - 01:50 - `T-0052` перенесена из active `TASKS.md` в `completed-tasks/2026/06 Июнь.md`.
- Изменения: runtime input traversal, `Viewport`, `Node`, `Control`, новые UI enum files, integration/unit tests, specs/docs, API wiki generator, task tracker, completed archive и дневник.
- Решения: `SceneTree.DispatchInput()` остаётся internal entry point; public event injection будет отдельной automation/CLI задачей. `T-0052` больше не зависит от `T-0066`, потому что animation timeline не должен блокировать runtime input/focus pipeline.
- Проверки: focused integration `13/13`, focused unit `8/8`, public API XML docs `1406` symbols/`0` issues, source license headers `393` files, API compatibility public types `132`, API wiki check `149` pages, public API documentation audit `160` Markdown files, user docs, source domain layout `271` files, full tests unit `74/74`, integration `239/239`, runtime smoke `5/5`, golden-data `12/12`, CI matrix, reference game assets and `git diff --check` all passed.
- Далее: повторить Markdown-sensitive checks after archive edit, затем stage/commit/push `T-0052` and wiki changes.

## 01:57 +03:00 - Agent: Codex

- Задача: T-0052 / финализация commit, push и CI.
- Контекст: после локальных проверок нужно было зафиксировать задачу одним commit-ом, синхронизировать GitHub Wiki и дождаться CI без дополнительных служебных commit-ов.
- Действия:
  - 01:51 - staged ровно файлы `T-0052` и проверен `git diff --cached --check`.
  - 01:52 - создан основной commit `11c1572` с сообщением `T-0052: add input dispatch control focus`.
  - 01:53 - создан wiki commit `d4cefc6` с сообщением `T-0052: sync input dispatch API wiki`.
  - 01:54 - `main` и `.github/wiki` успешно отправлены в удалённые репозитории.
  - 01:57 - GitHub Actions run `27920104162` завершился успешно на Windows, macOS и Ubuntu.
- Изменения: после закрывающего commit-а дополнительных изменений к runtime/API не вносилось; эта запись фиксирует финальное состояние задачи.
- Решения: не создавать отдельные `push`/`CI success` commit-ы; итоговая запись дневника будет добавлена через amend к commit-у `T-0052`.
- Проверки: `git diff --cached --check` прошёл; GitHub Actions `CI` run `27920104162` завершился success.
- Далее: amend текущего task commit-а только этой diary entry, `push --force-with-lease`, повторно проверить чистое состояние и CI.

## 02:02 +03:00 - Agent: Codex

- Задача: T-0067 / старт Control layout core.
- Контекст: пользователь попросил продолжать выполнение `TASKS.md`; игровые примеры остаются на самый последний этап, поэтому выбрана следующая незаблокированная не-example задача.
- Действия:
  - 02:00 - проверены чистое состояние `main` и `.github/wiki`.
  - 02:01 - перечитаны локальные `AGENTS.md`, `TASKS.md` и архив завершённых задач.
  - 02:01 - пересчитаны зависимости активных задач; единственная незаблокированная не-example задача - `T-0067`.
  - 02:02 - добавлена стартовая заметка к `T-0067` в `TASKS.md`.
- Изменения: обновлён `TASKS.md` и текущий дневник; production-код пока не менялся.
- Решения: `T-0067` нужно закрыть до контейнеров, базовых UI controls и редактора, потому что она задаёт правила геометрии `Control`, фокуса и layout.
- Проверки: `git status --short --branch` показал чистые `main...origin/main` и `.github/wiki master...origin/master`; ручной расчёт зависимостей подтвердил, что `T-0067` разблокирована.
- Далее: прочитать UI спецификации, текущий `Control`/`Viewport` код и существующие UI/input tests перед добавлением failing tests.

## 02:30 +03:00 - Agent: Codex

- Задача: T-0067 / Control layout core.
- Контекст: нужно было закрыть базовый UI layout baseline, который разблокирует containers, базовые controls и редактор.
- Действия:
  - 02:05 - добавлены `docs/specifications/ui/control-layout-core.md`, `docs/documentation/ui/control-layout-core.md` и красный integration test `ControlLayoutCoreTests`.
  - 02:06 - красная фаза подтвердила отсутствие `_GetMinimumSize()`.
  - 02:12 - добавлен public enum `GrowDirection`, anchors/offsets, computed `Position`/`Size`, minimum-size API и focus path API в `Control`.
  - 02:17 - `Viewport` подключён к `ClipContents` при pointer hit-test и к keyboard focus navigation.
  - 02:20 - добавлен тест на `Tab`, `Shift+Tab` и directional neighbor navigation.
  - 02:24 - обновлены public API baseline tests и GitHub Wiki generator category.
  - 02:27 - выполнены focused tests, public API docs, license, wiki, user docs, domain layout и full test checks.
  - 02:30 - `T-0067` удалена из active `TASKS.md` и добавлена в `completed-tasks/2026/06 Июнь.md`.
- Изменения: `Control`, `Viewport`, новый `GrowDirection`, integration/unit tests, UI spec/docs, documentation indexes, API wiki generator, generated GitHub Wiki, task tracker, completed archive и дневник.
- Решения: контейнеры, готовые widgets, tooltips, theme/high-DPI и renderer scissor не включать в `T-0067`; это отдельные задачи поверх нового layout core.
- Проверки: red phase упала на отсутствующем `_GetMinimumSize()`; затем `ControlLayoutCoreTests` прошёл `5/5`; focused integration прошёл `13/13`; focused unit прошёл `9/9`; public API XML docs `1437` symbols/`0` issues; source license headers `396` files; API compatibility public types `133`; API wiki check `150` pages; public API documentation audit `161` Markdown files; user docs, source domain layout `272` files, full tests unit `74/74`, integration `244/244`, runtime smoke `5/5`, golden-data `12/12`, CI matrix, reference game assets and `git diff --check` all passed.
- Далее: повторить Markdown-sensitive checks after archive edit, затем stage/commit/push `T-0067` and wiki changes.

## 02:35 +03:00 - Agent: Codex

- Задача: T-0067 / commit preparation.
- Контекст: после переноса задачи в архив нужно было зафиксировать основной репозиторий одним task commit-ом и отдельно зафиксировать GitHub Wiki.
- Действия:
  - 02:31 - проверено, что `T-0067` есть только в `completed-tasks/2026/06 Июнь.md`, а не в active `TASKS.md`.
  - 02:32 - повторно прошли `Verify-UserDocumentation.ps1` и `git diff --check`.
  - 02:33 - staged только файлы `T-0067` в основном репозитории и проверен `git diff --cached --check`.
  - 02:34 - staged generated wiki pages в `.github/wiki` и проверен wiki staged diff.
  - 02:35 - создан основной commit `c53f523` с сообщением `T-0067: add control layout core`.
  - 02:35 - создан wiki commit `6ae7d3b` с сообщением `T-0067: sync control layout API wiki`.
- Изменения: новых runtime/API изменений после предыдущей записи не вносилось; эта запись фиксирует commit state перед push.
- Решения: не создавать отдельные служебные commit-ы для push/CI; diary entry будет внесена в тот же `T-0067` commit через amend.
- Проверки: main и wiki staged diff прошли `--check`; user docs и active/completed placement повторно проверены после архивирования.
- Далее: amend основного `T-0067` commit-а только этой diary entry, push main/wiki и дождаться CI.

## 02:36 +03:00 - Agent: Codex

- Задача: ad-hoc / release sequencing после `T-0067`.
- Контекст: после успешного CI по `T-0067` пользователь уточнил, что реализацию редактора нельзя начинать до полного UI public API со статусом `Supported`, а не `Partial`.
- Действия:
  - 02:36 - подтверждён успешный GitHub Actions run `27920813325` для commit-а `T-0067: add control layout core`.
  - 02:36 - добавлено правило release sequencing в `AGENTS.md`.
  - 02:36 - обновлены release-management specification и documentation для UI public API gate перед редактором.
  - 02:36 - добавлена gate-задача `T-0129` и `T-0078` переведена на зависимость от неё.
- Изменения: `AGENTS.md`, `TASKS.md`, `docs/specifications/release-management/api-compatibility.md`, `docs/documentation/release-management/api-compatibility.md` и дневник разработки.
- Решения: не менять текущие wiki-статусы UI API на `Supported` без реализации, тестов и документации; вместо этого зафиксировать явный blocker для editor-задач.
- Проверки: ещё не запускались после этой правки; нужно проверить пользовательскую документацию, diff whitespace и состояние git перед фиксацией.
- Далее: выполнить документационные проверки, зафиксировать изменения отдельным английским commit-ом и синхронизировать удалённый репозиторий без служебных push/CI commit-ов.

## 02:37 +03:00 - Agent: Codex

- Задача: ad-hoc / проверка UI public API gate.
- Контекст: после добавления blocker-а перед редактором нужно было проверить, что документация и task tracker остаются согласованными.
- Действия:
  - 02:37 - выполнена проверка пользовательской документации.
  - 02:37 - выполнена whitespace-проверка diff.
  - 02:37 - проверено, что `T-0129` упоминается в `TASKS.md`, `AGENTS.md` и release-management документах.
- Изменения: новых содержательных изменений после предыдущей записи не вносилось; эта запись фиксирует результаты проверок перед commit-ом.
- Решения: оставить wiki statuses без изменений, потому что UI rows всё ещё должны пройти реальную реализацию до `Supported`.
- Проверки: `Verify-UserDocumentation.ps1` прошёл; `git diff --check` прошёл без ошибок, только с предупреждениями о будущей замене LF на CRLF; wiki working tree чистый.
- Далее: stage related Markdown files, сделать английский commit и отправить `main` в remote.

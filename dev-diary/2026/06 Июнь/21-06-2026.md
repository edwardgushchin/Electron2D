## Запись: Задача AI-friendly workflow и правило понятного языка

### Контекст
- Пользователь потребовал, чтобы в `AGENTS.md` было правило для расшифровки внутренних инженерных терминов нормальным языком.
- Пользователь также задал новое позиционирование Electron2D: AI-friendly движок с редакторской панелью терминала для `codex`, `opencode`, `claude code` и проектными skills.

### Хронология
- 00:00
  - В `TASKS.md` добавлена задача `T-0113` про AI-friendly позиционирование, агентскую панель редактора, создание git-репозитория, проектный `AGENTS.md` и стартовые skills.
  - Уточнено правило в `AGENTS.md`: оно явно распространяется на задачи, архивы задач, спецификации, документацию, release notes, changelog, README и дневниковые записи текущей сессии.
- 00:02
  - Дочищены формулировки в runtime diagnostics документации: полуанглийские выражения заменены понятными русскими описаниями.
  - Обновлены архивные задачи `T-0009` и `T-0015`, где оставались старые непонятные формулировки.

### Изменения
- Добавлена активная задача `T-0113` в `TASKS.md`.
- Обновлены `AGENTS.md`, runtime diagnostics спецификация и справка, связанные object-model документы, changelog, release notes и архивы задач.

### Решения
- `T-0113` оставлена активной будущей задачей, а не начата немедленно: текущий рабочий блок закрывает правило документации.
- Проектный `AGENTS.md` для новых Electron2D-проектов должен быть шаблоном, похожим по назначению на глобальный файл пользователя, но не должен слепо копировать приватные пользовательские правила.

### Проверки
- `rg` по непонятным внутренним шорткатам показал, что совпадение осталось только в `AGENTS.md`, где выражения приведены как запрещённые примеры.
- `git diff --check` - прошло; предупреждения были только о будущей нормализации переносов строк Git под Windows.

### Следующие шаги
- Выполнить `git diff --check`.
- Сделать commit `T-0112: добавить правило понятного языка в документации`.
- Вернуться к `T-0017`.

## Запись: Commit T-0112

### Контекст
- Задача `T-0112` завершена, а пользовательская задача про AI-friendly workflow добавлена в активный backlog как `T-0113`.

### Хронология
- 00:04
  - Создан commit `T-0112: добавить правило понятного языка в документации`.

### Изменения
- В commit вошли правило понятного языка в `AGENTS.md`, исправления в человеко-читаемых документах, архив `T-0112`, новая активная задача `T-0113` и записи дневника.

### Решения
- Реализация `T-0113` не начата в этом commit; задача только заведена для дальнейшей работы.

### Проверки
- Перед commit выполнены `rg` по непонятным внутренним шорткатам и `git diff --check`.

### Следующие шаги
- Проверить рабочую копию.
- Вернуться к `T-0017`.

## Запись: Старт T-0017

### Контекст
- Следующая задача backlog: `T-0017`, 2D math baseline для `Vector2`, `Vector2I`, `Rect2`, `Rect2I`, `Transform2D`, `Color` и `Mathf`.

### Хронология
- 00:11
  - Создана спецификация `docs/specifications/core-types/2d-math-baseline.md`.
  - Добавлены unit-тесты `Math2DBaselineTests`.
  - Red-state подтверждён: `dotnet test tests\Electron2D.Tests.Unit\Electron2D.Tests.Unit.csproj --no-restore` падает на отсутствующих публичных math-типах.

### Изменения
- Пока изменены только спецификация, индекс спецификаций и unit-тесты.

### Решения
- Контракт T-0017 ограничен 2D math baseline, без `Variant`, `RandomNumberGenerator`, `StringName`, `Rid` и без 3D-типов.
- Публичные имена должны оставаться Godot-like и C#-style: PascalCase-члены вместо Unity-like aliases.

### Проверки
- Red-state: `dotnet test tests\Electron2D.Tests.Unit\Electron2D.Tests.Unit.csproj --no-restore` - ожидаемо упал на отсутствующих типах `Vector2`, `Vector2I`, `Rect2`, `Rect2I`, `Transform2D`, `Color`, `Mathf`.

### Следующие шаги
- Добавить минимальную runtime-реализацию math-типов.
- Запустить narrow unit tests.

## Запись: AI-friendly архитектурный контракт 0.1

### Контекст
- Пользователь приложил файл с уточнением: AI-friendly Electron2D должен быть архитектурным требованием, а не встроенным чатом.
- Текущая работа по `T-0017` была незавершённой, поэтому её нужно было не смешивать с новым документационно-планировочным commit.

### Хронология
- 00:17
  - Текущие изменения `T-0017` временно сохранены в git stash `wip T-0017 math baseline before AI-friendly spec`.
  - Проверены внешние источники из файла: Godot command line docs, JSON Schema Draft 2020-12, MCP tools spec, AGENTS.md convention, SARIF 2.1.0, Microsoft `global.json`.
- 00:21
  - Создана спецификация `docs/specifications/architecture/ai-friendly-workflow.md`.
  - Обновлена release spec `docs/specifications/releases/0.1.0-preview.md`: AI-friendly workflow включён в цель `0.1.0 Preview`.
  - `TASKS.md` дополнен задачами `T-0115`-`T-0128` по Tooling, CLI, текстовым форматам, API manifest, MCP, context pack, runtime automation, testing, diagnostics, debug bridge, safety, reproducibility, docs и benchmark.
  - Уточнена `T-0113`: агентская панель редактора должна работать поверх Tooling/CLI, а не быть отдельным встроенным AI-chat.

### Изменения
- Добавлена архитектурная спецификация AI-friendly workflow.
- Обновлены release spec, индекс спецификаций, backlog и архив `completed-tasks/T-0114.md`.

### Решения
- AI-friendly для `0.1.0` означает CLI, MCP, общий tooling layer, текстовые форматы, структурированную диагностику и автоматизированный runtime-запуск, а не встроенную LLM и не API, привязанный к одному поставщику.
- Встроенный AI-chat, генерация игры по одному prompt, генерация ассетов и автоматическая публикация игр остаются вне первой версии.

### Проверки
- Runtime-тесты не запускались: production-код не менялся в этой задаче.
- `rg` по непонятным гибридным формулировкам в новых AI-friendly документах - совпадений не осталось.
- `git diff --check` - прошло; предупреждения были только о будущей нормализации переносов строк Git под Windows.

### Следующие шаги
- Выполнить `git diff --check`.
- Сделать commit `T-0114: зафиксировать AI-friendly контракт 0.1`.
- Вернуть stash `T-0017` и продолжить math baseline.

## Запись: Commit T-0114

### Контекст
- Архитектурный контракт AI-friendly workflow для `0.1.0 Preview` зафиксирован как отдельная завершённая задача.

### Хронология
- 00:25
  - Создан commit `T-0114: зафиксировать AI-friendly контракт 0.1`.

### Изменения
- В commit вошли `docs/specifications/architecture/ai-friendly-workflow.md`, обновление release spec, новые задачи `T-0115`-`T-0128`, уточнение `T-0113`, архив `T-0114` и запись дневника.

### Решения
- Следующим шагом нужно вернуть сохранённый stash с `T-0017` и продолжить 2D math baseline.

### Проверки
- Перед commit выполнены `rg` по непонятным гибридным формулировкам и `git diff --check`.

### Следующие шаги
- Вернуть stash `T-0017`.
- Проверить рабочую копию после merge.
- Продолжить реализацию `T-0017`.

## Запись: Завершение T-0017

### Контекст
- Завершалась задача `T-0017`: 2D math baseline.
- Пользователь попросил разнести исходные файлы runtime по доменным папкам, не меняя namespace.

### Хронология
- 00:39
  - Реализованы `Vector2`, `Vector2I`, `Rect2`, `Rect2I`, `Transform2D`, `Color`, `Mathf`.
  - Runtime-файлы разнесены по папкам `Core/Math`, `Core/ObjectModel`, `Core/SceneTree`; namespace остался `Electron2D`.
  - Обновлены спецификация, справка, Wiki compatibility table, changelog и release notes.
  - `T-0017` перенесена из `TASKS.md` в `completed-tasks/T-0017.md`.

### Изменения
- Public API runtime расширен до 18 типов.
- Добавлены unit-тесты `Math2DBaselineTests`.
- Документация фиксирует текущий 2D math baseline и ограничения будущих задач.

### Решения
- Не добавлять `Variant`, `RandomNumberGenerator`, `StringName`, `Rid` и 3D math-типы в `T-0017`.
- Не добавлять Unity-like aliases для math API.

### Проверки
- Red-state: `dotnet test tests\Electron2D.Tests.Unit\Electron2D.Tests.Unit.csproj --no-restore` - ожидаемо упал на отсутствующих math-типах.
- Narrow green: `dotnet test tests\Electron2D.Tests.Unit\Electron2D.Tests.Unit.csproj --no-restore` - прошло, 12 тестов.
- `powershell -ExecutionPolicy Bypass -File tools\Run-Tests.ps1` - прошло, 51 тест.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ApiCompatibility.ps1` - прошло, public types: 18.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ReleaseMetadata.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ProjectTemplate.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-CiMatrix.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-PerformanceBudgets.ps1` - прошло.
- `git diff --check` - прошло; предупреждения были только о будущей нормализации переносов строк Git под Windows.

### Следующие шаги
- Проверить финальный status.
- Сделать commit `T-0017: реализовать 2D math baseline`.
- Перейти к `T-0018`.

## Запись: Commit T-0017

### Контекст
- Задача `T-0017` завершена и перенесена в `completed-tasks/`.

### Хронология
- 00:40
  - Создан commit `T-0017: реализовать 2D math baseline`.

### Изменения
- В commit вошли 2D math types, unit-тесты, спецификация, справка, Wiki compatibility table, release records, доменное разбиение runtime-файлов, архив `T-0017` и дневник.

### Решения
- Следующей активной задачей остаётся `T-0018`: `RandomNumberGenerator` с документированным seed и воспроизводимостью.

### Проверки
- Перед commit выполнены проверки из записи «Завершение T-0017», включая полный тестовый скрипт, API/release verifiers и `git diff --check`.

### Следующие шаги
- Проверить статус рабочей копии.
- Перейти к `T-0018`.

## 00:45 +03:00 - Agent: Codex

- Задача: T-0129 / формат дневника разработки.
- Контекст: пользователь попросил изменить `AGENTS.md`, чтобы дневники разработки выглядели как в `G:\Projects\2026\portfolio-bot\dev-diary\2026`; текущие правила Electron2D всё ещё требовали старую многораздельную структуру `## Запись`.
- Действия:
  - 00:44 - проверены текущий `AGENTS.md`, состояние рабочей копии и хвост дневника Electron2D.
  - 00:44 - изучены реальные дневники `portfolio-bot` и его правило `Development Diary`: дневной заголовок, блок `## HH:MM +03:00 - Agent: Codex` и поля `Задача`, `Контекст`, `Действия`, `Изменения`, `Решения`, `Проверки`, `Далее`.
  - 00:45 - обновлены правила `Development Diary` в `AGENTS.md`.
  - 00:45 - обновлён локальный skill `.codex/skills/obsidian-markdown/SKILL.md`, чтобы следующий агент не получил старый шаблон дневника.
  - 00:45 - создан архив завершённой задачи `completed-tasks/T-0129.md`.
  - 00:46 - выполнен `git diff --check`; ошибок нет, остались только предупреждения Git о будущей нормализации переносов строк.
  - 00:46 - проверено, что `.codex/skills/obsidian-markdown/SKILL.md` находится в исключённом из Git каталоге `.codex/`, поэтому в commit попадут только отслеживаемые проектные файлы.
- Изменения: изменены `AGENTS.md`, `.codex/skills/obsidian-markdown/SKILL.md`, добавлен `completed-tasks/T-0129.md`, обновлён текущий дневник.
- Решения: старые дневниковые записи не переписывать ради миграции; новый формат применять к новым файлам и новым записям в хвосте существующих файлов.
- Проверки: сверены примеры `portfolio-bot`; `git diff --check` прошёл без ошибок; хвост дневника проверен, эта запись находится последней.
- Далее: создать отдельный commit `T-0129: обновить формат дневника агента`; затем продолжить очередь задач с `T-0018`.

## 00:49 +03:00 - Agent: Codex

- Задача: T-0130 / MIT License и license headers.
- Контекст: пользователь потребовал, чтобы Electron2D распространялся по MIT License, а каждый source-файл имел лицензионный заголовок в стиле SDL.
- Действия:
  - 00:47 - создан commit `T-0129: обновить формат дневника агента`.
  - 00:48 - проверено текущее состояние: `PackageLicenseExpression` уже равен `MIT`, но корневой `LICENSE` содержит zlib-подобный текст, а C# source-файлы не имеют license header.
  - 00:49 - создана активная задача `T-0130` в `TASKS.md` перед правками лицензирования.
  - 00:50 - добавлены спецификация, справка и verifier `tools/Verify-SourceLicenseHeaders.ps1`.
  - 00:51 - первый red-state verifier упал на старом zlib-подобном `LICENSE`.
  - 00:51 - `LICENSE` заменён на MIT License; второй red-state verifier упал на source-файлах без header.
  - 00:52 - единый MIT header добавлен во все отслеживаемые C# и PowerShell source-файлы.
  - 00:53 - green-state verifier прошёл: `43 tracked source files`.
  - 00:54 - полный `tools\Run-Tests.ps1` прошёл: `51` тест.
  - 00:55 - прошли release metadata, project template, CI matrix, performance budgets, API compatibility и `git diff --check`.
  - 00:56 - `T-0130` перенесена из `TASKS.md` в `completed-tasks/T-0130.md`.
  - 00:57 - после staging повторно выполнен license verifier; проверка прошла уже по `44 tracked source files`, включая новый verifier-скрипт.
  - 00:58 - создан commit `T-0130: перейти на MIT License`.
- Изменения: обновлены `LICENSE`, C# и PowerShell source headers, CI workflow, README, changelog, release notes, спецификация, справка, индексы документации, `TASKS.md`, архив задачи и дневник.
- Решения: использовать MIT License по SPDX и закрепить собственный полный header, потому что у MIT License нет единого обязательного standard header.
- Проверки: red-state verifier упал на старом `LICENSE`, затем на отсутствующих headers; после исправления прошли license verifier, полный тестовый скрипт, release/template/CI/performance/API verifiers, `git diff --check`, staged license verifier и staged diff check.
- Далее: продолжить очередь задач с `T-0018`.

## 01:00 +03:00 - Agent: Codex

- Задача: T-0018 / `RandomNumberGenerator`.
- Контекст: продолжена очередь `TASKS.md` после лицензирования; следующая активная задача требует Godot-like `RandomNumberGenerator` с documented seed, deterministic sequences, range APIs и serialization seed tests.
- Действия:
  - 01:00 - сверены release spec, текущая core-types документация, Wiki compatibility table и unit-тесты.
  - 01:01 - добавлена спецификация `docs/specifications/core-types/random-number-generator.md`.
  - 01:02 - добавлены unit-тесты `RandomNumberGeneratorTests` и обновлён baseline public API test.
  - 01:02 - red-state `dotnet test tests\Electron2D.Tests.Unit\Electron2D.Tests.Unit.csproj --no-restore` ожидаемо упал на отсутствующем `Electron2D.RandomNumberGenerator`.
  - 01:03 - добавлена runtime-реализация `src/Electron2D/Core/Random/RandomNumberGenerator.cs`.
  - 01:04 - green-state unit-тестов прошёл: `16` тестов.
  - 01:05 - обновлены справка, README, changelog, release notes, docs indexes и GitHub Wiki source.
  - 01:06 - прошли license verifier, полный тестовый скрипт, release metadata, API compatibility, project template, CI matrix, performance budgets и `git diff --check`.
  - 01:07 - `T-0018` перенесена из `TASKS.md` в `completed-tasks/T-0018.md`.
  - 01:08 - после staging повторно выполнен license verifier; проверка прошла по `46 tracked source files`, включая новые runtime/test файлы.
  - 01:09 - создан commit `T-0018: реализовать RandomNumberGenerator`.
- Изменения: добавлены спецификация, unit-тесты, runtime-тип `RandomNumberGenerator` и документация текущего API.
- Решения: зафиксировать PCG32 sequence policy для `0.1.0 Preview`; не добавлять `RandWeighted()` до Godot collections/Variant слоя.
- Проверки: red-state и green-state unit-тестов выполнены; полный набор verifier-скриптов, `git diff --check`, staged license verifier и staged diff check прошли.
- Далее: перейти к `T-0019`.

## 01:11 +03:00 - Agent: Codex

- Задача: T-0019 / `StringName`, `Rid` и identity semantics.
- Контекст: продолжена очередь `TASKS.md`; следующая задача требует базовые Godot-like identity-типы для строковых имён и server resource identifiers.
- Действия:
  - 01:10 - проверены текущие задачи, source files, `Resource`, baseline public API test и упоминания `StringName`/`Rid` в документации.
  - 01:11 - сверены официальные Godot docs: `StringName` как immutable interned name и `RID` как opaque handle с invalid ID `0`.
  - 01:12 - добавлены спецификация `docs/specifications/core-types/stringname-rid.md`, unit-тесты `StringNameRidTests`, integration-тесты `RidAllocatorTests` и обновлён baseline public API test.
  - 01:13 - red-state focused tests ожидаемо упали: unit project не видел `StringName`/`Rid`, integration project не видел `RidAllocator`.
  - 01:14 - добавлены runtime-типы `StringName`, `Rid` и internal `RidAllocator` в `src/Electron2D/Core/Identity/`.
  - 01:15 - focused tests прошли: unit `19` тестов, integration `37` тестов.
  - 01:17 - обновлены справка, README, changelog, release notes, docs indexes, GitHub Wiki source и устаревшие ограничения в object-model документах.
  - 01:18 - прошли полный `tools\Run-Tests.ps1`, release metadata, API compatibility, project template, CI matrix, performance budgets и `git diff --check`.
  - 01:19 - `T-0019` перенесена из `TASKS.md` в `completed-tasks/T-0019.md`.
  - 01:20 - после staging выполнен license verifier; проверка прошла по `51 tracked source file`.
  - 01:21 - создан commit `T-0019: реализовать StringName и Rid`.
- Изменения: добавлены спецификация, unit/integration tests, runtime identity-типы и документация текущего API.
- Решения: публично добавить только Godot-like `StringName` и `Rid`; allocation/lifetime для будущих серверов оставить internal и покрыть integration-тестами через `InternalsVisibleTo`.
- Проверки: red-state и green-state focused tests выполнены; полный набор verifier-скриптов, `git diff --check`, staged license verifier и staged diff check прошли.
- Далее: перейти к `T-0020`.

## 01:23 +03:00 - Agent: Codex

- Задача: T-0020 / закрытый список `Variant` для `0.1.0`.
- Контекст: продолжена очередь `TASKS.md`; следующая задача должна ввести Godot-like `Variant` как закрытый value carrier для `0.1.0 Preview` без legacy compatibility layer.
- Действия:
  - 01:22 - проверены чистота рабочей копии, хвост дневника и наличие commit `T-0019`.
  - 01:23 - сверены `TASKS.md`, релизная спецификация, текущие core-types документы, source layout и официальная Godot C# Variant documentation.
  - 01:23 - задача `T-0020` помечена как `in progress`; в заметках зафиксирована граница scope между закрытым списком `Variant` и отдельной serialization-задачей `T-0021`.
  - 01:24 - добавлена спецификация `docs/specifications/core-types/variant.md`.
  - 01:25 - добавлены unit-тесты `VariantTests` и обновлён baseline public API test.
  - 01:26 - red-state unit-тестов ожидаемо упал на отсутствующих `Electron2D.Variant` и `Electron2D.Collections`.
  - 01:29 - добавлены runtime-типы `Variant`, `Electron2D.Collections.Array` и `Electron2D.Collections.Dictionary`.
  - 01:30 - green-state unit-тестов прошёл: `25` тестов.
  - 01:34 - обновлены справка, README, changelog, release notes, docs indexes, GitHub Wiki source и устаревшие object-model ограничения про отсутствующий `Variant`.
  - 01:36 - полный `tools\Run-Tests.ps1` прошёл: `65` тестов.
  - 01:37 - прошли release metadata, API compatibility, project template, CI matrix и performance budgets verifiers.
  - 01:38 - `T-0020` перенесена из `TASKS.md` в `completed-tasks/T-0020.md`.
  - 01:39 - после staging выполнены source license verifier по `55 tracked source files` и `git diff --cached --check`.
  - 01:40 - создан commit `T-0020: реализовать Variant baseline`.
- Изменения: добавлены спецификация, unit-тесты, runtime `Variant`, Godot-like коллекции, справка и обновления compatibility/release документов.
- Решения: не смешивать `T-0020` с `T-0021`; сейчас реализован закрытый список поддерживаемых Variant-значений, а stable round-trip serialization остаётся следующей задачей.
- Проверки: red-state и green-state unit-тестов выполнены; полный тестовый скрипт, release/API/template/CI/performance verifiers, staged source license verifier и staged diff check прошли.
- Далее: перейти к `T-0021`.

## 01:42 +03:00 - Agent: Codex

- Задача: T-0021 / stable serialization round-trip для `Variant`.
- Контекст: продолжена очередь `TASKS.md`; после закрытого списка `Variant` нужно зафиксировать стабильный формат round-trip для сериализуемых базовых значений.
- Действия:
  - 01:41 - проверена чистая рабочая копия после commit `T-0020: реализовать Variant baseline`.
  - 01:42 - сверены `TASKS.md`, релизная спецификация, текущая `Variant` спецификация/справка и ограничения `Rid`/`Object` как runtime-only значений.
  - 01:42 - задача `T-0021` помечена как `in progress`; зафиксировано решение делать internal runtime serializer без нового публичного не-Godot API.
  - 01:43 - добавлена спецификация `docs/specifications/core-types/variant-serialization.md`.
  - 01:44 - добавлены integration tests `VariantSerializationTests`.
  - 01:45 - red-state integration tests ожидаемо упали на отсутствующем `Electron2D.VariantTextSerializer`.
  - 01:46 - добавлен internal serializer `src/Electron2D/Core/Variant/VariantTextSerializer.cs`.
  - 01:47 - green-state integration tests прошли: `42` теста.
  - 01:48 - обновлены справка, README, changelog, release notes, docs indexes и связанные core/object-model ограничения.
  - 01:50 - полный `tools\Run-Tests.ps1` прошёл: `70` тестов.
  - 01:51 - прошли release metadata, API compatibility, project template, CI matrix и performance budgets verifiers.
  - 01:52 - `T-0021` перенесена из `TASKS.md` в `completed-tasks/T-0021.md`.
  - 01:53 - после staging выполнены source license verifier по `57 tracked source files` и `git diff --cached --check`.
  - 01:54 - создан commit `T-0021: реализовать Variant serialization`.
- Изменения: добавлены спецификация формата, integration tests, internal serializer, справка и обновления release/core документации.
- Решения: сериализуемый subset включает stable data values; `Object`, `Callable` и `Rid` дают понятную ошибку, потому что не являются переносимыми данными в `0.1.0 Preview`.
- Проверки: red-state и green-state integration tests выполнены; полный тестовый скрипт, release/API/template/CI/performance verifiers, staged source license verifier и staged diff check прошли.
- Далее: перейти к `T-0022`.

## 01:56 +03:00 - Agent: Codex

- Задача: T-0022 / `RenderingServer` и backend abstraction.
- Контекст: продолжена очередь `TASKS.md`; следующая задача должна ввести серверную границу рендеринга, профили `Standard`/`Compatibility` и feature flags без возвращения legacy renderer API.
- Действия:
  - 01:55 - проверена чистая рабочая копия после commit `T-0021: реализовать Variant serialization`.
  - 01:56 - сверены `TASKS.md`, релизная спецификация, архитектурная спецификация и официальная Godot RenderingServer documentation.
  - 01:56 - задача `T-0022` помечена как `in progress`; зафиксирован минимальный scope без SDL device creation.
  - 01:57 - добавлена спецификация `docs/specifications/rendering/rendering-server.md`.
  - 01:58 - добавлены unit/integration tests для public API, backend leaks и internal backend switching.
  - 01:59 - red-state unit/integration tests ожидаемо упали на отсутствующем `RenderingServer` и backend types.
  - 02:00 - добавлены runtime-типы `RenderingServer`, `IRenderingBackend`, `RenderingBackend`, `CompatibilityRenderingBackend`, `StandardRenderingBackend`.
  - 02:01 - green-state focused tests прошли: unit `28`, integration `43`.
  - 02:02 - обновлены справка, README, changelog, release notes, docs indexes, GitHub Wiki source и compatibility baseline.
  - 02:04 - полный `tools\Run-Tests.ps1` прошёл: `74` теста.
  - 02:06 - прошли release metadata, API compatibility, project template, CI matrix и performance budgets verifiers.
  - 02:07 - `T-0022` перенесена из `TASKS.md` в `completed-tasks/T-0022.md`.
  - 02:08 - после staging выполнены source license verifier по `64 tracked source files` и `git diff --cached --check`.
  - 02:09 - создан commit `T-0022: ввести RenderingServer baseline`.
- Изменения: добавлены rendering specification, runtime server/backend boundary, unit/integration tests, справка и release/compatibility документы.
- Решения: публично добавить только Godot-like `RenderingServer` facade с nested profile/feature enums; concrete backend abstractions оставить internal.
- Проверки: red-state и green-state focused tests выполнены; полный тестовый скрипт, release/API/template/CI/performance verifiers, staged source license verifier и staged diff check прошли.
- Далее: перейти к `T-0023`.

## 02:13 +03:00 - Agent: Codex

- Задача: T-0023 / SDL_GPU device, swapchain, command submission и frame lifecycle.
- Контекст: продолжена очередь `TASKS.md`; после `RenderingServer` boundary нужно ввести release-baseline lifecycle для SDL_GPU backend.
- Действия:
  - 02:10 - проверена чистая рабочая копия после commit `T-0022: ввести RenderingServer baseline`.
  - 02:11 - сверены `TASKS.md`, релизная спецификация, архитектурная спецификация, `Electron2D.csproj` и отсутствие SDL3-CS dependency.
  - 02:12 - сверены upstream SDL3-CS release line `3.4.10.x` и SDL3 GPU API для device creation, window claim, command buffer acquire/submit.
  - 02:13 - задача `T-0023` помечена как `in progress`; зафиксирован CI-safe scope через internal lifecycle adapter и smoke tests без обязательного реального окна.
- Изменения: обновлены `TASKS.md` и текущий дневник.
- Решения: не делать фальшивый публичный GPU API; начать с controlled dependency, internal adapter, state machine и логирования ошибок lifecycle.
- Проверки: на старте подтверждена чистая рабочая копия; production-код ещё не менялся.
- Далее: добавить спецификацию SDL_GPU lifecycle, failing smoke tests и затем реализацию.

## 02:23 +03:00 - Agent: Codex

- Задача: T-0023 / SDL_GPU lifecycle baseline.
- Контекст: продолжена реализация после red-state smoke tests; требование пользователя про MIT source headers учтено для всех новых C# файлов.
- Действия:
  - 02:14 - добавлена спецификация `docs/specifications/rendering/sdl-gpu-lifecycle.md`.
  - 02:15 - добавлены integration tests `SdlGpuLifecycleTests`, которые ожидаемо упали на отсутствующих internal SDL_GPU lifecycle типах.
  - 02:16 - добавлена dependency `SDL3-CS` версии `3.4.10.3` в runtime project.
  - 02:18 - сверены binding names SDL3-CS для `CreateGPUDevice`, `ClaimWindowForGPUDevice`, `AcquireGPUCommandBuffer`, `SubmitGPUCommandBuffer` и `DestroyGPUDevice`.
  - 02:20 - добавлены internal handles, window info, lifecycle state/events, `ISdlGpuApi`, `SdlGpuApi` и `SdlGpuRenderingBackend`.
  - 02:21 - focused integration tests прошли: `49` тестов; затем исправлены analyzer warnings в новых тестах.
  - 02:22 - повторный focused integration test прошёл: `49` тестов без предупреждений.
  - 02:23 - обновлены rendering documentation, docs indexes, README, changelog и release notes под фактическое internal lifecycle поведение.
- Изменения: SDL_GPU lifecycle реализован как internal runtime surface без расширения public Godot-like API.
- Решения: production adapter требует настоящий `SDL_Window` handle, а CI проверяет state machine через deterministic fake adapter.
- Проверки: `dotnet test tests\Electron2D.Tests.Integration\Electron2D.Tests.Integration.csproj --no-restore` прошёл.
- Далее: запустить полный набор проверок, перенести `T-0023` в `completed-tasks/` и сделать commit.

## 02:27 +03:00 - Agent: Codex

- Задача: T-0023 / завершение и подготовка commit.
- Контекст: implementation/docs уже готовы; осталось закрыть release-gate проверки и оформить архив задачи.
- Действия:
  - 02:24 - полный `tools\Run-Tests.ps1` прошёл: unit `28`, integration `49`, runtime smoke `2`, golden data `1`.
  - 02:25 - прошли release metadata, API compatibility, project template, CI matrix и performance budgets verifiers.
  - 02:26 - `T-0023` перенесена из `TASKS.md` в `completed-tasks/T-0023.md`.
  - 02:27 - после staging выполнен source license verifier по `75 tracked source files`.
  - 02:28 - создан commit `088695b` (`T-0023: реализовать SDL_GPU lifecycle baseline`).
- Изменения: активный backlog теперь начинается с `T-0024`; архив `completed-tasks/T-0023.md` содержит результат, документацию и проверки.
- Решения: не публиковать release и не запускать real-window GPU smoke без отдельной задачи/команды.
- Проверки: полный тестовый скрипт, release/API/template/CI/performance verifiers, `git diff --check` и staged source license verifier прошли.
- Далее: перейти к `T-0024`.

## 02:30 +03:00 - Agent: Codex

- Задача: T-0024 / CanvasItem sorting, visibility, modulate и batching.
- Контекст: продолжена очередь `TASKS.md` после commit `b8c9d89`; рабочая копия чистая.
- Действия:
  - 02:28 - проверен clean status после T-0023 и хвост дневника.
  - 02:29 - сверены T-0024, релизная спецификация, architecture stack, текущий rendering code и API compatibility wiki.
  - 02:30 - сверена официальная Godot stable documentation по `CanvasItem` и `RenderingServer`: tree order, visibility propagation, modulate/self_modulate, `z_index`, `y_sort_enabled` и canvas-item backend API.
  - 02:30 - задача `T-0024` помечена как `in progress`.
- Изменения: обновлены `TASKS.md` и текущий дневник.
- Решения: не добавлять public `CanvasItem`/`Node2D` в T-0024, потому что они запланированы в T-0026; сейчас нужен internal render queue baseline для будущих node submissions.
- Проверки: на старте подтверждена чистая рабочая копия; production-код ещё не менялся.
- Далее: добавить спецификацию internal canvas item render queue, failing tests и затем реализацию.

## 02:35 +03:00 - Agent: Codex

- Задача: T-0024 / internal CanvasItem render queue baseline.
- Контекст: после спецификации добавлены TDD tests и минимальная реализация internal render queue без расширения public API.
- Действия:
  - 02:31 - добавлена спецификация `docs/specifications/rendering/canvas-item-render-queue.md`.
  - 02:32 - добавлены integration tests `CanvasItemRenderQueueTests`.
  - 02:33 - red-state integration tests ожидаемо упали на отсутствующих `CanvasItemRenderQueue`, `CanvasItemRenderCommand` и `CanvasItemBatchKey`.
  - 02:34 - добавлены internal `CanvasItemBlendMode`, `CanvasItemBatchKey`, `CanvasItemRenderCommand`, `CanvasItemRenderBatch`, `CanvasItemRenderPlan` и `CanvasItemRenderQueue`.
  - 02:34 - green-state integration tests прошли: `54` теста.
  - 02:35 - добавлены дополнительные проверки empty/clear/invalid command; повторный green-state integration test прошёл: `57` тестов.
  - 02:35 - обновлены implementation docs, docs indexes, README, changelog, release notes и rendering-server spec/doc ограничения.
- Изменения: internal queue фильтрует invisible commands, сортирует по layer/z/y/tree/insertion order и строит contiguous batches с `DrawCallCount`.
- Решения: batching не переупорядочивает commands, чтобы draw order оставался первичным правилом.
- Проверки: `dotnet test tests\Electron2D.Tests.Integration\Electron2D.Tests.Integration.csproj --no-restore` прошёл.
- Далее: запустить полный набор проверок, перенести `T-0024` в `completed-tasks/` и сделать commit.

## 02:39 +03:00 - Agent: Codex

- Задача: T-0024 / завершение и подготовка commit.
- Контекст: implementation/docs уже готовы; выполнены release-gate проверки и архивирование задачи.
- Действия:
  - 02:36 - полный `tools\Run-Tests.ps1` прошёл: unit `28`, integration `57`, runtime smoke `2`, golden data `1`.
  - 02:37 - прошли release metadata, API compatibility, project template, CI matrix и performance budgets verifiers.
  - 02:38 - `T-0024` перенесена из `TASKS.md` в `completed-tasks/T-0024.md`.
  - 02:39 - после staging выполнен source license verifier по `82 tracked source files`.
  - 02:40 - создан commit `f5fc66f` (`T-0024: реализовать CanvasItem render queue baseline`).
- Изменения: активный backlog теперь начинается с `T-0025`; архив `completed-tasks/T-0024.md` содержит результат, документацию и проверки.
- Решения: source headers сохранены во всех новых C# файлах; public API compatibility не изменился.
- Проверки: полный тестовый скрипт, release/API/template/CI/performance verifiers, `git diff --check` и staged source license verifier прошли.
- Далее: перейти к `T-0025`.

## 02:43 +03:00 - Agent: Codex

- Задача: T-0025 / `Texture2D`, atlas regions, filter/repeat descriptors, transparency и resource lifetime.
- Контекст: продолжена очередь `TASKS.md` после commit `d1b3dc3`; рабочая копия чистая.
- Действия:
  - 02:40 - проверен clean status после T-0024 и хвост дневника.
  - 02:41 - сверены T-0025, релизная спецификация, architecture stack, API compatibility wiki и текущий rendering/resource code.
  - 02:42 - сверена официальная Godot stable documentation по `Texture2D`, `AtlasTexture`, `CanvasItem.TextureFilter`/`TextureRepeat` и `RenderingServer` RID/resource model.
  - 02:43 - задача `T-0025` помечена как `in progress`.
- Изменения: обновлены `TASKS.md` и текущий дневник.
- Решения: добавить public Godot-like `Texture2D`/`AtlasTexture`, но оставить filter/repeat как internal sampling descriptor до public `CanvasItem` в T-0026; PNG/JPEG import не входит в T-0025.
- Проверки: на старте подтверждена чистая рабочая копия; production-код ещё не менялся.
- Далее: добавить спецификацию texture resource baseline, failing unit/integration/runtime-smoke tests и затем реализацию.

## 02:50 +03:00 - Agent: Codex

- Задача: T-0025 / texture resource baseline.
- Контекст: после спецификации добавлены public API tests, internal registry tests и runtime smoke leak tracking.
- Действия:
  - 02:44 - добавлена спецификация `docs/specifications/rendering/texture-resource-baseline.md`.
  - 02:45 - добавлены unit tests для `Texture2D`/`AtlasTexture`, integration tests для `TextureResourceRegistry` и runtime smoke leak test.
  - 02:46 - red-state unit tests ожидаемо упали на отсутствующем `Electron2D.Texture2D`.
  - 02:48 - добавлены public `Texture2D`/`AtlasTexture`, internal `RuntimeTexture2D`, sampling options, texture descriptor, registry events, `ITextureGpuApi` и `TextureResourceRegistry`; runtime smoke assembly получил internal access.
  - 02:49 - unit tests сначала упали на устаревшем public baseline, затем expected public surface обновлён для `AtlasTexture` и `Texture2D`.
  - 02:49 - focused unit tests прошли: `33` теста.
  - 02:50 - focused integration tests прошли: `61` тест; runtime smoke прошёл: `3` теста.
  - 02:50 - обновлены implementation docs, docs indexes, API compatibility Wiki, README, changelog, release notes и rendering docs.
- Изменения: добавлен public texture baseline и internal upload/reload/release registry с leak tracking.
- Решения: public filter/repeat enums не добавлять в T-0025, потому что в Godot они принадлежат `CanvasItem`; сейчас они представлены internal sampling descriptor.
- Проверки: focused unit/integration/runtime smoke tests прошли.
- Далее: запустить полный набор проверок, перенести `T-0025` в `completed-tasks/` и сделать commit.

## 02:55 +03:00 - Agent: Codex

- Задача: T-0025 / завершение и подготовка commit.
- Контекст: public texture baseline и internal registry готовы; выполнены release-gate проверки и архивирование задачи.
- Действия:
  - 02:51 - полный `tools\Run-Tests.ps1` прошёл: unit `33`, integration `61`, runtime smoke `3`, golden data `1`.
  - 02:52 - прошли release metadata, API compatibility, project template, CI matrix и performance budgets verifiers.
  - 02:54 - `T-0025` перенесена из `TASKS.md` в `completed-tasks/T-0025.md`.
  - 02:55 - после staging выполнен source license verifier по `97 tracked source files`.
  - 02:56 - создан commit `753a743` (`T-0025: реализовать Texture2D resource baseline`).
- Изменения: активный backlog теперь начинается с `T-0026`; архив `completed-tasks/T-0025.md` содержит result, public API notes, документацию и проверки.
- Решения: API compatibility Wiki обновлена, public types count теперь `30`.
- Проверки: полный тестовый скрипт, release/API/template/CI/performance verifiers, `git diff --check` и staged source license verifier прошли.
- Далее: перейти к `T-0026`.

## 03:00 +03:00 - Agent: Codex

- Задача: T-0131 / закрепить MIT License правило в `AGENTS.md`.
- Контекст: пользователь повторно уточнил, что движок должен быть под MIT License, а каждый source-файл должен иметь лицензионный header по аналогии с SDL.
- Действия:
  - 02:59 - проверены `AGENTS.md`, `TASKS.md`, существующая license policy, архив `T-0130`, текущий source header и состояние рабочей копии.
  - 02:59 - запущен `tools\Verify-SourceLicenseHeaders.ps1`; проверка прошла по `97 tracked source files`.
  - 03:00 - в `AGENTS.md` добавлено явное правило: Electron2D использует MIT License, SDL-like означает per-file header, а не zlib license text SDL.
  - 03:00 - добавлен архив `completed-tasks/T-0131.md` с результатом уточнения.
  - 03:01 - staged source license verifier прошёл по `97 tracked source files`.
  - 03:01 - создан commit `T-0131: закрепить MIT License правило`.
- Изменения: обновлены локальные агентские правила и архив завершённой задачи.
- Решения: не менять исходники, потому что MIT header уже стоит и verifier проходит; закрепить требование в `AGENTS.md`, чтобы новые source-файлы создавались правильно.
- Проверки: `powershell -ExecutionPolicy Bypass -File tools\Verify-SourceLicenseHeaders.ps1` прошёл до изменения правил и после staging; `git diff --cached --check` прошёл.
- Далее: продолжить с `T-0026`.

## 03:03 +03:00 - Agent: Codex

- Задача: T-0026 / `Sprite2D`, `CanvasItem`, `Node2D`, `CanvasLayer` и базовая submission-модель.
- Контекст: начата следующая задача `0.1.0 Preview`; рабочая копия чистая после commit `d877961`.
- Действия:
  - 03:02 - сверены текущие `TASKS.md`, `AGENTS.md`, public API Wiki, существующие `Node`, `Texture2D`, `AtlasTexture`, render queue и math types.
  - 03:02 - открыта официальная Godot stable documentation по `CanvasItem`, `Node2D`, `Sprite2D` и `CanvasLayer`.
  - 03:03 - `TASKS.md` дополнен заметкой агента о взятии `T-0026` в работу.
  - 03:03 - для `T-0026` явно проставлен статус `in progress`.
  - 03:06 - добавлена спецификация `docs/specifications/rendering/canvas-node-submission-baseline.md` и ссылка в индексе спецификаций.
  - 03:08 - добавлены unit tests `CanvasNodePublicApiTests`, обновлён public API baseline и добавлены integration tests `CanvasNodeSubmissionTests`.
  - 03:09 - red-state unit tests ожидаемо упали на отсутствующих `CanvasItem`, `Node2D`, `Sprite2D` и `CanvasLayer`; integration tests ожидаемо упали на отсутствующем `Sprite2D`.
  - 03:14 - добавлены public `CanvasItem`, `Node2D`, `Sprite2D`, `CanvasLayer`, internal `CanvasSubmissionContext`; `Node.Reparent()` научился сохранять `Node2D.GlobalTransform`.
  - 03:16 - focused unit tests прошли: `40` тестов; focused integration tests прошли: `65` тестов.
  - 03:19 - обновлены implementation docs, docs indexes, API compatibility Wiki, README, changelog и release notes.
  - 03:20 - `tools\Verify-ApiCompatibility.ps1` прошёл, public types: `34`.
  - 03:21 - полный `tools\Run-Tests.ps1` прошёл: unit `40`, integration `65`, runtime smoke `3`, golden data `1`.
  - 03:22 - прошли release metadata, project template, CI matrix и performance budgets verifiers; `git diff --check` прошёл с предупреждениями только о CRLF-нормализации.
  - 03:23 - повторно прошли API compatibility verifier (`34` public types) и source license verifier до staging.
  - 03:24 - `T-0026` перенесена из `TASKS.md` в `completed-tasks/T-0026.md`.
  - 03:25 - staged source license verifier прошёл по `104 tracked source files`; `git diff --cached --check` прошёл.
  - 03:26 - создан commit `T-0026: реализовать canvas node submission baseline`.
- Изменения: обновлены `TASKS.md`, спецификация, tests и runtime код для canvas node baseline.
- Решения: реализовывать только Godot-like публичные типы и не вводить legacy/compatibility API; submission-модель держать внутренней, пока она не является публичным Godot API.
- Проверки: red-state и green-state focused unit/integration tests выполнены; полный test runner и release-gate verifiers прошли.
- Далее: перейти к `T-0027`.

## 03:31 +03:00 - Agent: Codex

- Задача: T-0027 / `Camera2D`, viewport scaling, presentation modes и pixel snapping.
- Контекст: начата следующая задача `0.1.0 Preview`; рабочая копия чистая после commit `faed59f`.
- Действия:
  - 03:28 - сверены `TASKS.md`, релизная спецификация, architecture stack и текущие `SceneTree`, `CanvasSubmissionContext`, `Node2D`, `CanvasLayer`.
  - 03:29 - открыта официальная Godot stable documentation по `Camera2D`, `Viewport` и multiple resolutions.
  - 03:31 - `T-0027` помечена как `in progress`.
  - 03:34 - добавлена спецификация `docs/specifications/rendering/camera-viewport-presentation-baseline.md` и ссылка в индексе спецификаций.
- Изменения: обновлены `TASKS.md` и дневник разработки.
- Решения: public API добавлять только для Godot-like типов `Camera2D`/`Viewport`; presentation/stretch расчёт держать internal, пока отдельный Godot-like project/window settings API не введён.
- Проверки: на старте подтверждена чистая рабочая копия; production-код ещё не менялся.
- Далее: добавить failing tests и минимальную реализацию.

## 03:39 +03:00 - Agent: Codex

- Задача: T-0027 / завершение Camera2D, Viewport и presentation baseline.
- Контекст: спецификация и RED-тесты были добавлены после старта задачи; реализация доведена до green-state и синхронизирована с документацией.
- Действия:
  - 03:33 - добавлены unit tests для public `Camera2D`/`Viewport` API и integration tests для camera transform, pixel snapping и presentation plan.
  - 03:34 - red-state unit tests ожидаемо упали на отсутствующих `Camera2D` и `Viewport`; red-state integration tests ожидаемо упали на отсутствующих camera/viewport/presentation types.
  - 03:35 - добавлены public `Camera2D`, `Viewport`, internal presentation model, `SceneTree.Root` как `Viewport` instance и camera/pixel snapping support в `CanvasSubmissionContext`.
  - 03:36 - исправлена internal проверка current camera и нормализация `Node2D` rotation decomposition для стабильного `PI`.
  - 03:36 - focused unit tests прошли: `45` тестов; focused integration tests прошли: `69` тестов.
  - 03:37 - обновлены implementation docs, docs indexes, API compatibility Wiki, README, changelog, release notes и связанные object-model/canvas docs.
  - 03:38 - `tools\Verify-ApiCompatibility.ps1` прошёл, public types: `36`.
  - 03:38 - полный `tools\Run-Tests.ps1` прошёл: unit `45`, integration `69`, runtime smoke `3`, golden data `1`.
  - 03:39 - прошли release metadata, project template, CI matrix и performance budgets verifiers.
  - 03:39 - `git diff --check` прошёл с предупреждениями только о CRLF-нормализации; pre-stage source license verifier прошёл по `104 tracked source files`.
  - 03:39 - `T-0027` перенесена из `TASKS.md` в `completed-tasks/T-0027.md`.
  - 03:40 - staged source license verifier прошёл по `113 tracked source files`; `git diff --cached --check` прошёл.
  - 03:41 - создан commit `T-0027: реализовать Camera2D viewport baseline`.
- Изменения: добавлены public `Camera2D`/`Viewport`, internal viewport presentation model, тесты, спецификация, документация и архив задачи.
- Решения: public `Window`/display settings не вводить в T-0027; stretch/presentation API держать internal до отдельной Godot-like задачи.
- Проверки: focused RED/GREEN unit/integration tests, полный test runner, API compatibility и release-gate verifiers прошли.
- Далее: перейти к `T-0028`.

## 03:41 +03:00 - Agent: Codex

- Задача: T-0028 / immediate drawing API `CanvasItem`.
- Контекст: начата следующая задача `0.1.0 Preview` после commit `61b5d2f`; рабочая копия чистая.
- Действия:
  - 03:41 - `T-0028` помечена как `in progress`, добавлена заметка агента в `TASKS.md`.
  - 03:43 - сверена официальная Godot stable documentation по `CanvasItem`, custom drawing in 2D и `Font`.
  - 03:45 - добавлена спецификация `docs/specifications/rendering/immediate-drawing-baseline.md` и ссылка в индексе спецификаций.
  - 03:48 - добавлены RED unit, integration и golden-data tests для `_Draw()`, `QueueRedraw()` и immediate drawing commands.
  - 03:49 - RED unit tests ожидаемо упали на отсутствующем `_Draw()`; RED integration tests - на отсутствующих `Font`/`_Draw()`; RED golden-data tests - на отсутствующем `_Draw()`.
  - 03:55 - добавлены public `Font`, `HorizontalAlignment`, `_Draw()`, `QueueRedraw()`, `DrawLine()`, `DrawRect()`, `DrawCircle()`, `DrawPolygon()`, `DrawTexture()`, `DrawString()` и internal cached draw command submission.
  - 03:57 - focused unit tests прошли: `47` тестов; focused integration tests прошли: `72` теста; golden-data tests прошли: `2` теста.
  - 03:59 - обновлены implementation docs, docs indexes, API compatibility Wiki, README, changelog, release notes и связанные canvas/texture docs.
  - 03:59 - `tools\Verify-ApiCompatibility.ps1` прошёл, public types: `38`.
  - 04:00 - полный `tools\Run-Tests.ps1` прошёл: unit `47`, integration `72`, runtime smoke `3`, golden data `2`.
  - 04:01 - прошли release metadata, project template, CI matrix и performance budgets verifiers.
  - 04:01 - `git diff --check` прошёл с предупреждениями только о CRLF-нормализации; pre-stage source license verifier прошёл по `113 tracked source files`.
  - 04:01 - `T-0028` перенесена из `TASKS.md` в `completed-tasks/T-0028.md`.
  - 04:02 - staged source license verifier прошёл по `120 tracked source files`; `git diff --cached --check` прошёл.
  - 04:03 - создан commit `T-0028: реализовать immediate drawing baseline`.
- Изменения: обновлены `TASKS.md`, спецификация, тесты, runtime code, документация и архив T-0028.
- Решения: начинать T-0028 со спецификации и RED-тестов; public API сверять с Godot-like `CanvasItem` drawing surface.
- Проверки: RED/green focused tests, полный test runner, API compatibility и release-gate verifiers прошли.
- Далее: перейти к `T-0029`.

## 04:05 +03:00 - Agent: Codex

- Задача: T-0035 / resource format, stable UID, external/internal references.
- Контекст: после commit `de33e03` следующая активная T-0029 заблокирована зависимостью T-0035; рабочая копия чистая.
- Действия:
  - 04:05 - `T-0029` помечена как blocked до закрытия `T-0035`.
  - 04:05 - `T-0035` помечена как `in progress`, добавлена заметка агента в `TASKS.md`.
- Изменения: обновлены `TASKS.md` и дневник разработки.
- Решения: не начинать text backend до resource format baseline, потому что T-0029 явно зависит от T-0035.
- Проверки: перед стартом подтверждена чистая рабочая копия после T-0028.
- Далее: изучить текущие `Resource`, Variant serialization и спецификации resource pipeline.

## 04:09 +03:00 - Agent: Codex

- Задача: T-0035 / resource format, stable UID, external/internal references.
- Контекст: при повторной проверке `TASKS.md` обнаружено, что отметка `in progress` была ошибочно записана в T-0033, а не в T-0035.
- Действия:
  - 04:09 - удалена ошибочная отметка `in progress` и заметка агента из `T-0033`.
  - 04:09 - `T-0035` помечена как `in progress`, добавлена корректная заметка агента.
- Изменения: исправлен `TASKS.md`.
- Решения: не оставлять две активные задачи и не переносить ошибочную отметку в будущий коммит.
- Проверки: проверен `git diff` по `TASKS.md` и дневнику перед началом реализации.
- Далее: добавить спецификацию resource file baseline и RED-тесты.

## 04:10 +03:00 - Agent: Codex

- Задача: T-0035 / resource format, stable UID, external/internal references.
- Контекст: перед тестами оформлен целевой контракт ресурсного формата.
- Действия:
  - 04:10 - добавлена спецификация `docs/specifications/resources/resource-file-baseline.md`.
  - 04:10 - обновлён индекс `docs/specifications/README.md`.
- Изменения: создан раздел спецификаций ресурсов.
- Решения: публично вводить только Godot-like `ResourceUid`; `.e2res` serializer оставить внутренней частью движка до отдельного публичного `ResourceLoader`/`ResourceSaver` контракта.
- Проверки: production-код ещё не менялся; следующий шаг - RED-тесты.
- Далее: добавить unit, integration и golden-data тесты для UID и `.e2res`.

## 04:17 +03:00 - Agent: Codex

- Задача: T-0035 / resource format, stable UID, external/internal references.
- Контекст: спецификация готова, добавлены проверки перед реализацией.
- Действия:
  - 04:11 - добавлены unit tests для public `ResourceUid`.
  - 04:12 - добавлены integration tests для `.e2res` round-trip с external/internal references.
  - 04:13 - добавлен golden-data test с exact JSON output.
  - 04:14 - RED-запуск unit/integration/golden tests показал ожидаемые ошибки отсутствующих `ResourceUid`, `ResourceFileDocument`, `ResourceFileExternalReference`, `ResourceFileInternalResource` и `ResourceFileTextSerializer`; параллельный первый запуск дополнительно дал lock conflict, поэтому проверки повторены последовательно.
  - 04:15 - добавлены `ResourceUid`, internal resource file document model и serializer в `src/Electron2D/Core/Resources/`.
  - 04:16 - исправлен internal constructor shape для `ResourceFileExternalReference`.
  - 04:17 - serializer нормализует переносы строк в LF для стабильного diff-friendly формата.
  - 04:17 - фокусные проверки прошли: unit `3`, integration `2`, golden-data `1`.
- Изменения: добавлены ресурсные source files и тесты.
- Решения: `ResourceUid` сделать публичной Godot-like поверхностью; `.e2res` model и serializer оставить internal, то есть внутренними типами движка, доступными тестам, но не пользовательскому API.
- Проверки: `dotnet test` для `ResourceUidTests`, `ResourceFileSerializationTests`, `ResourceFileGoldenTests` прошёл последовательно.
- Далее: обновить документацию реализации, API compatibility и релизные материалы.

## 04:24 +03:00 - Agent: Codex

- Задача: T-0035 / resource format, stable UID, external/internal references.
- Контекст: реализация и документация T-0035 синхронизированы, выполнены полные проверки.
- Действия:
  - 04:18 - добавлена документация `docs/documentation/resources/resource-file-baseline.md` и обновлены индексы.
  - 04:19 - обновлены API compatibility Wiki, release-management docs, README, CHANGELOG и RELEASE-NOTES.
  - 04:20 - уточнены object-model и Variant serialization docs, чтобы ссылки на ресурсы описывались через `.e2res`, а не через `Variant.Object`.
  - 04:21 - compatibility-focused checks прошли: `CleanRuntimeBaselineTests` и `tools\Verify-ApiCompatibility.ps1`, public types: `39`.
  - 04:22 - полный `tools\Run-Tests.ps1` прошёл: unit `50`, integration `74`, runtime smoke `3`, golden data `3`.
  - 04:23 - прошли release metadata, project template, CI matrix и performance budgets verifiers.
  - 04:23 - `T-0035` перенесена из `TASKS.md` в `completed-tasks/T-0035.md`.
  - 04:24 - с `T-0029` снята блокировка, потому что зависимость `T-0035` завершена.
- Изменения: обновлены документация, release notes, changelog, Wiki, `TASKS.md` и архив задачи.
- Решения: не добавлять public `ResourceLoader`/`ResourceSaver` в T-0035; это отдельные будущие контракты, а текущая задача закрывает UID и text resource file baseline.
- Проверки: полный test runner и release-gate verifiers прошли.
- Далее: выполнить pre-stage checks, staged checks и создать commit T-0035.

## 04:25 +03:00 - Agent: Codex

- Задача: T-0035 / resource format, stable UID, external/internal references.
- Контекст: перед коммитом выполнены финальные локальные проверки staged state.
- Действия:
  - 04:25 - `git diff --check` прошёл; предупреждения были только о будущей CRLF-нормализации Git под Windows.
  - 04:25 - pre-stage source license verifier прошёл по `120 tracked source files`.
  - 04:25 - staged source license verifier прошёл по `126 tracked source files`.
  - 04:25 - `git diff --cached --check` прошёл.
- Изменения: staged полный набор файлов T-0035.
- Решения: новые source-файлы имеют MIT header и входят в staged license check.
- Проверки: whitespace и source license checks прошли.
- Далее: создать commit `T-0035: реализовать resource UID и e2res baseline`.

## 04:26 +03:00 - Agent: Codex

- Задача: T-0035 / resource format, stable UID, external/internal references.
- Контекст: T-0035 завершена и зафиксирована в Git.
- Действия:
  - 04:26 - создан commit `T-0035: реализовать resource UID и e2res baseline`.
- Изменения: дневник дополнен записью о commit.
- Решения: commit hash в дневнике не фиксируется, потому что запись будет добавлена через amend.
- Проверки: commit создан успешно.
- Далее: перейти к следующей доступной задаче `T-0029`.

## 04:27 +03:00 - Agent: Codex

- Задача: T-0029 / SDL_ttf text backend, `Font`, glyph layout, fallback и cache.
- Контекст: зависимость T-0035 закрыта, рабочая копия чистая после commit T-0035.
- Действия:
  - 04:27 - `T-0029` помечена как `in progress` в `TASKS.md`.
- Изменения: обновлены `TASKS.md` и дневник разработки.
- Решения: начать T-0029 с изучения текущего `Font`, `DrawString`, rendering command model и официальных API SDL_ttf/Godot для текстового поведения.
- Проверки: перед стартом подтверждена чистая рабочая копия после T-0035.
- Далее: изучить код и спецификации перед RED-тестами.

## 04:31 +03:00 - Agent: Codex

- Задача: T-0029 / уточнение требования MIT License для новых source-файлов.
- Контекст: пользователь уточнил, что Electron2D должен распространяться по MIT License, а каждый source-файл должен иметь license header в стиле SDL.
- Действия:
  - 04:31 - проверены `AGENTS.md`, `LICENSE`, документация политики лицензирования и архивы задач `T-0130`/`T-0131`.
  - 04:31 - выполнена проверка `tools\Verify-SourceLicenseHeaders.ps1`; она подтвердила MIT headers для `126 tracked source files`.
- Изменения: изменён только дневник разработки.
- Решения: не создавать новую задачу, потому что требование уже закрыто `T-0130` и закреплено правилами `T-0131`; все новые C# и PowerShell файлы в T-0029 должны создаваться с полным MIT header.
- Проверки: `powershell -ExecutionPolicy Bypass -File tools\Verify-SourceLicenseHeaders.ps1` - прошло, `126 tracked source files`.
- Далее: продолжить T-0029 со спецификации и RED-тестов.

## 04:34 +03:00 - Agent: Codex

- Задача: T-0029 / SDL_ttf text backend, `Font`, glyph layout, fallback и cache.
- Контекст: перед RED-тестами нужно закрепить целевой контракт текстового baseline.
- Действия:
  - 04:32 - изучены текущие `Font`, `CanvasItem.DrawString()`, internal drawing/render commands, render profiles и API compatibility Wiki.
  - 04:33 - сверены официальные страницы Godot `Font`, `Control`, `Label` и SDL_ttf QuickReference.
  - 04:34 - добавлена спецификация `docs/specifications/rendering/text-backend-baseline.md`.
  - 04:34 - обновлён индекс `docs/specifications/README.md`.
- Изменения: создана спецификация T-0029 и обновлён индекс спецификаций.
- Решения: публично добавить только Godot-like `Font` methods, `Control`, `Label` и `VerticalAlignment`; SDL_ttf handles, glyph layout records и cache оставить внутренней частью движка.
- Проверки: production-код ещё не менялся; следующий шаг - RED-тесты.
- Далее: добавить unit, integration и golden-data tests, которые должны упасть на текущем baseline.

## 04:37 +03:00 - Agent: Codex

- Задача: T-0029 / SDL_ttf text backend, `Font`, glyph layout, fallback и cache.
- Контекст: спецификация готова, добавлены проверки до изменения production-кода.
- Действия:
  - 04:35 - добавлен unit test `TextPublicApiTests` для `Font`, `Control`, `Label` и `VerticalAlignment`.
  - 04:35 - добавлен integration test `TextLayoutSubmissionTests` для `DrawString`, Unicode, fallback, RTL, cache и профилей renderer.
  - 04:36 - добавлен golden-data test `TextLayoutGoldenTests` для стабильной layout summary.
  - 04:36 - RED-запуск unit tests упал ожидаемо: отсутствуют методы `Font.GetStringSize()`, `GetHeight()`, `GetAscent()`, `GetDescent()`, `HasChar()` и типы `Control`, `Label`, `VerticalAlignment`.
  - 04:37 - RED-запуск integration/golden tests упал ожидаемо: отсутствуют internal virtual hooks `HasGlyph`, `GetGlyphAdvance`, `GetFallbackFonts` и layout API.
- Изменения: добавлены RED-тесты в unit, integration и golden-data projects.
- Решения: проверять текстовый baseline на уровне публичного API и internal render command данных, чтобы `DrawString()` не остался строковой заглушкой.
- Проверки: `dotnet test` для `TextPublicApiTests`, `TextLayoutSubmissionTests`, `TextLayoutGoldenTests` дал ожидаемые compile failures из-за отсутствующего поведения.
- Далее: реализовать минимальный Godot-like text backend baseline.

## 04:43 +03:00 - Agent: Codex

- Задача: T-0029 / SDL_ttf text backend, `Font`, glyph layout, fallback и cache.
- Контекст: RED-тесты готовы, реализован минимальный production baseline.
- Действия:
  - 04:38 - расширен `Font`: public measurement API, `HasChar()`, internal layout cache, glyph advance, fallback font hooks и basic RTL detection.
  - 04:39 - добавлены internal `TextLayout`, `TextGlyph`, cache stats/key/direction и SDL_ttf boundary `ISdlTtfApi`/`SdlTtfApi`.
  - 04:40 - `CanvasItem.DrawString()` начал создавать text layout и передавать его в drawing/render commands.
  - 04:41 - добавлены public `Control`, `Label` и `VerticalAlignment` с Godot-like XML docs.
  - 04:41 - добавлена зависимость `SDL3-CS.Native.TTF`, чтобы SDL_ttf native libraries входили в runtime dependency graph.
  - 04:42 - focused unit, integration и golden-data tests по тексту прошли.
  - 04:43 - соседние immediate drawing tests прошли; public surface baseline обновлён под новые Godot-like типы.
- Изменения: обновлены runtime source files, добавлены UI/text source files, добавлены tests и обновлён public surface test.
- Решения: `HorizontalAlignment.Fill` для 0.1 ведёт себя как left alignment; сложный text shaping и real draw calls остаются будущими задачами, но command data уже содержит layout для backend.
- Проверки: `TextPublicApiTests` `2/2`, `TextLayoutSubmissionTests` `3/3`, `TextLayoutGoldenTests` `1/1`, immediate drawing integration/golden tests прошли; clean runtime baseline после обновления прошёл.
- Далее: обновить implementation docs, API Wiki, release materials и выполнить полные проверки.

## 04:46 +03:00 - Agent: Codex

- Задача: T-0029 / SDL_ttf text backend, `Font`, glyph layout, fallback и cache.
- Контекст: runtime baseline реализован, нужно синхронизировать документацию и release-gates.
- Действия:
  - 04:44 - добавлена документация `docs/documentation/rendering/text-backend-baseline.md`.
  - 04:44 - обновлены rendering docs, documentation/specification indexes и снято устаревшее утверждение, что `DrawString()` не выполняет glyph layout.
  - 04:45 - обновлены GitHub Wiki API compatibility source, release-management API docs, README, CHANGELOG и RELEASE-NOTES.
  - 04:45 - dependency `SDL3-CS.Native.TTF` заменена с локального preview build на публичную NuGet-версию `3.3.0`.
  - 04:46 - `tools\Verify-ApiCompatibility.ps1` прошёл, public types: `42`.
  - 04:46 - `tools\Verify-ReleaseMetadata.ps1` прошёл.
- Изменения: обновлены documentation/release материалы и package dependency для SDL_ttf native libraries.
- Решения: не закреплять локальный preview NuGet package в репозитории; использовать публичный `SDL3-CS.Native.TTF` `3.3.0`.
- Проверки: API compatibility и release metadata verifiers прошли.
- Далее: выполнить полный test runner и остальные release-gate checks.

## 04:50 +03:00 - Agent: Codex

- Задача: T-0029 / SDL_ttf text backend, `Font`, glyph layout, fallback и cache.
- Контекст: после release-gate проверок задача готова к архивированию.
- Действия:
  - 04:47 - первый полный `tools\Run-Tests.ps1` нашёл race в новом integration test: тест менял singleton `RenderingServer` параллельно с существующим тестом backend switching.
  - 04:48 - integration test исправлен: `Label` проверяет `Compatibility` и `Standard` backend instances без изменения global renderer state.
  - 04:48 - полный integration project прошёл: `77` тестов.
  - 04:49 - полный `tools\Run-Tests.ps1` прошёл: unit `52`, integration `77`, runtime smoke `3`, golden data `4`.
  - 04:49 - прошли `Verify-ProjectTemplate`, `Verify-CiMatrix`, `Verify-PerformanceBudgets`, `Verify-ApiCompatibility`, `Verify-ReleaseMetadata` и pre-stage `Verify-SourceLicenseHeaders`.
  - 04:50 - `T-0029` перенесена из `TASKS.md` в `completed-tasks/T-0029.md`.
- Изменения: исправлен integration test isolation, создан архив задачи T-0029, обновлён `TASKS.md`.
- Решения: не менять singleton renderer state в новом text integration test, потому что xUnit выполняет integration tests параллельно.
- Проверки: полный test runner и release-gate verifiers прошли; pre-stage source license verifier прошёл по tracked files.
- Далее: выполнить whitespace check, staging, staged license check и commit T-0029.

## 04:51 +03:00 - Agent: Codex

- Задача: T-0029 / SDL_ttf text backend, `Font`, glyph layout, fallback и cache.
- Контекст: перед commit выполнены финальные staged checks.
- Действия:
  - 04:50 - `git diff --check` прошёл; предупреждения были только о будущей CRLF-нормализации Git под Windows.
  - 04:50 - staged файлы T-0029 добавлены в индекс.
  - 04:51 - staged `tools\Verify-SourceLicenseHeaders.ps1` прошёл по `140 tracked source files`.
  - 04:51 - `git diff --cached --check` прошёл.
- Изменения: staged полный набор файлов T-0029.
- Решения: новые source-файлы с MIT header входят в staged license check.
- Проверки: staged source license и whitespace checks прошли.
- Далее: создать commit `T-0029: реализовать SDL_ttf text backend baseline`.

## 04:51 +03:00 - Agent: Codex

- Задача: T-0029 / SDL_ttf text backend, `Font`, glyph layout, fallback и cache.
- Контекст: T-0029 завершена и зафиксирована в Git.
- Действия:
  - 04:51 - создан commit `T-0029: реализовать SDL_ttf text backend baseline`.
- Изменения: дневник дополнен записью о commit.
- Решения: commit hash в дневнике не фиксируется, потому что запись будет добавлена через amend.
- Проверки: commit создан успешно.
- Далее: проверить рабочую копию и перейти к следующей задаче.

## 04:52 +03:00 - Agent: Codex

- Задача: T-0030 / offscreen render targets и восстановление GPU resources после потери устройства.
- Контекст: рабочая копия чистая после commit T-0029; следующая активная задача в `TASKS.md` - T-0030.
- Действия:
  - 04:52 - `T-0030` помечена как `in progress` в `TASKS.md`.
  - 04:52 - добавлена заметка агента о старте после commit T-0029.
- Изменения: обновлены `TASKS.md` и дневник разработки.
- Решения: начать T-0030 со спецификации и RED-тестов, потому что задача меняет runtime/rendering behavior.
- Проверки: перед стартом подтверждена чистая рабочая копия после T-0029.
- Далее: изучить текущие texture registry, SDL_GPU lifecycle и rendering docs перед спецификацией.

## 04:54 +03:00 - Agent: Codex

- Задача: T-0030 / offscreen render targets и восстановление GPU resources после потери устройства.
- Контекст: изучены текущие `Viewport`, `TextureResourceRegistry`, SDL_GPU lifecycle и texture resource docs.
- Действия:
  - 04:53 - сверены официальные Godot docs по `Viewport.get_texture()` и `ViewportTexture`, а также SDL docs по `SDL_CreateGPUTexture`.
  - 04:54 - добавлена спецификация `docs/specifications/rendering/offscreen-render-target-recovery-baseline.md`.
  - 04:54 - обновлён индекс `docs/specifications/README.md`.
- Изменения: создана спецификация T-0030 и обновлён индекс спецификаций.
- Решения: публичная Godot-like часть T-0030 - `Viewport.GetTexture()` и `ViewportTexture`; render target handles и восстановление device остаются внутренним механизмом движка.
- Проверки: production-код ещё не менялся; следующий шаг - RED-тесты.
- Далее: добавить unit, integration и runtime smoke tests для render-to-texture и recovery path.

## 04:57 +03:00 - Agent: Codex

- Задача: T-0030 / offscreen render targets и восстановление GPU resources после потери устройства.
- Контекст: спецификация и RED-тесты готовы; нужно подтвердить, что тесты падают на отсутствующем контракте.
- Действия:
  - 04:55 - `ViewportTexturePublicApiTests` упал на отсутствующих `Viewport.GetTexture()` и `ViewportTexture`.
  - 04:56 - `RenderTargetRecoveryTests` упал на отсутствующих `CreateRenderTarget()`, `TextureResourceUsage`, `TextureUploadDescriptor.Usage`, `RenderTargetCreated`, `Restored` и `RestoreAfterDeviceLoss()`.
  - 04:56 - `RenderTargetRecoverySmokeTests` упал на отсутствующих `CreateRenderTarget()` и `RestoreAfterDeviceLoss()`.
  - 04:57 - перед production-правками проверено, что `Verify-SourceLicenseHeaders.ps1` проходит для `140 tracked source files`.
- Изменения: production-код ещё не менялся; зафиксирована RED-фаза TDD.
- Решения: реализовать только Godot-like public API (`Viewport.GetTexture()` и `ViewportTexture`), а render target lifecycle оставить внутренним механизмом движка.
- Проверки: три focused RED-команды завершились ожидаемыми compile errors; license header verifier прошёл.
- Далее: добавить минимальную реализацию `ViewportTexture`, render target descriptors и восстановление texture registry.

## 05:00 +03:00 - Agent: Codex

- Задача: T-0030 / offscreen render targets и восстановление GPU resources после потери устройства.
- Контекст: RED-фаза подтверждена, внесена минимальная runtime-реализация.
- Действия:
  - 04:58 - добавлены `Viewport.GetTexture()` и public `ViewportTexture`.
  - 04:58 - добавлены internal `TextureResourceUsage`, render target descriptors, `RenderTargetCreated` и `Restored` события.
  - 04:59 - `TextureResourceRegistry` получил создание render target и восстановление active textures/render targets через новый `ITextureGpuApi`.
  - 05:00 - focused unit tests по `ViewportTexture` и public runtime baseline прошли: `2` теста.
  - 05:00 - focused integration tests по render target lifecycle и restore прошли: `2` теста.
  - 05:00 - focused runtime smoke test по restore/release без leaks прошёл: `1` тест.
- Изменения: изменены runtime rendering files и обновлён baseline public API test.
- Решения: `ViewportTexture` имеет internal constructor и создаётся только через `Viewport.GetTexture()`, чтобы не добавлять лишний public factory API.
- Проверки: новые focused GREEN-команды прошли.
- Далее: обновить документацию, API Wiki source, release materials и затем выполнить полный набор проверок.

## 05:07 +03:00 - Agent: Codex

- Задача: T-0030 / offscreen render targets и восстановление GPU resources после потери устройства.
- Контекст: реализация прошла focused GREEN, нужно закрыть документацию, release-gates и архив задачи.
- Действия:
  - 05:01 - добавлена справка `docs/documentation/rendering/offscreen-render-target-recovery-baseline.md`.
  - 05:02 - обновлены rendering docs, documentation index, API Wiki source, release-management API docs, README, CHANGELOG и RELEASE-NOTES.
  - 05:03 - `Verify-ApiCompatibility.ps1` прошёл, public types: `43`.
  - 05:04 - полный `Run-Tests.ps1` прошёл: unit `53`, integration `79`, runtime smoke `4`, golden data `4`.
  - 05:05 - прошли `Verify-ProjectTemplate`, `Verify-CiMatrix`, `Verify-PerformanceBudgets`, `Verify-ReleaseMetadata`, повторный `Verify-ApiCompatibility` и `Verify-SourceLicenseHeaders`.
  - 05:07 - `T-0030` перенесена из `TASKS.md` в `completed-tasks/T-0030.md`; ошибочная стартовая заметка T-0030 удалена из секции T-0033.
- Изменения: обновлены docs/release материалы, task archive и active task tracker.
- Решения: T-0030 закрывает только baseline: `ViewportTexture`, internal render target descriptors и restore path без публичных SDL_GPU handles.
- Проверки: полный test runner и release-gate verifiers прошли; pre-stage source license verifier прошёл для tracked source files.
- Далее: выполнить whitespace check, staging, staged license check и commit T-0030.

## 05:08 +03:00 - Agent: Codex

- Задача: T-0030 / offscreen render targets и восстановление GPU resources после потери устройства.
- Контекст: задача заархивирована, перед commit нужны staged checks.
- Действия:
  - 05:08 - `git diff --check` прошёл; предупреждения были только о будущей CRLF-нормализации Git под Windows.
  - 05:08 - изменения T-0030 добавлены в индекс.
  - 05:08 - staged `Verify-SourceLicenseHeaders.ps1` прошёл по `145 tracked source files`.
  - 05:08 - `git diff --cached --check` прошёл.
- Изменения: staged полный набор файлов T-0030.
- Решения: новые source-файлы `ViewportTexture`, `TextureResourceUsage` и тесты входят в staged MIT license check.
- Проверки: staged source license и whitespace checks прошли.
- Далее: создать commit `T-0030: реализовать offscreen render target recovery baseline`.

## 05:08 +03:00 - Agent: Codex

- Задача: T-0030 / offscreen render targets и восстановление GPU resources после потери устройства.
- Контекст: T-0030 завершена и зафиксирована в Git.
- Действия:
  - 05:08 - создан commit `T-0030: реализовать offscreen render target recovery baseline`.
- Изменения: дневник дополнен записью о commit.
- Решения: commit hash в дневнике не фиксируется, потому что запись будет добавлена через amend.
- Проверки: commit создан успешно.
- Далее: проверить рабочую копию и продолжить выполнение следующей задачи.

## 05:09 +03:00 - Agent: Codex

- Задача: T-0031 / canvas shaders: import, SDL_shadercross compile, vertex/fragment stages и диагностика файл/строка.
- Контекст: рабочая копия чистая после commit T-0030; следующая активная задача в `TASKS.md` - T-0031.
- Действия:
  - 05:09 - `T-0031` помечена как `in progress` в `TASKS.md`.
  - 05:09 - добавлена заметка агента о старте после commit T-0030.
- Изменения: обновлены `TASKS.md` и дневник разработки.
- Решения: начать T-0031 со спецификации и RED-тестов, потому что задача меняет runtime/rendering behavior.
- Проверки: перед стартом подтверждена чистая рабочая копия после T-0030.
- Далее: изучить текущие rendering specs, shader-related gaps и dependency graph перед спецификацией.

## 05:12 +03:00 - Agent: Codex

- Задача: T-0031 / canvas shaders: import, SDL_shadercross compile, vertex/fragment stages и диагностика файл/строка.
- Контекст: изучены release spec, rendering server boundary, SDL_GPU lifecycle и доступные SDL_shadercross bindings.
- Действия:
  - 05:10 - подтверждено, что T-0031 не должна добавлять `ShaderMaterial`: это отдельная T-0032.
  - 05:11 - проверено, что SDL3-CS `3.4.10.3` содержит managed `SDL3.ShaderCross`, а NuGet видит `SDL3-CS.Native.Shadercross` `3.0.0`.
  - 05:12 - добавлена спецификация `docs/specifications/rendering/canvas-shader-import-baseline.md`.
  - 05:12 - обновлён индекс `docs/specifications/README.md`.
- Изменения: создана спецификация T-0031 и обновлён индекс спецификаций.
- Решения: public часть T-0031 - только Godot-like `Shader`; import pipeline, compiler boundary и compiled bytecode остаются внутренним механизмом движка и tooling.
- Проверки: production-код ещё не менялся; следующий шаг - RED-тесты.
- Далее: добавить unit, integration и runtime smoke tests для `Shader`, import diagnostics и iOS no-runtime-compile contract.

## 05:14 +03:00 - Agent: Codex

- Задача: T-0031 / canvas shaders: import, SDL_shadercross compile, vertex/fragment stages и диагностика файл/строка.
- Контекст: RED-тесты готовы; нужно подтвердить, что они падают на отсутствующем shader contract.
- Действия:
  - 05:13 - `ShaderPublicApiTests` упал на отсутствующем public `Shader`.
  - 05:13 - `CanvasShaderImportTests` упал на отсутствующих `ICanvasShaderCompiler`, `CanvasShaderCompileRequest` и `CanvasShaderCompileResult`.
  - 05:14 - `CanvasShaderImportSmokeTests` упал на отсутствующих internal shader import types.
- Изменения: production-код ещё не менялся; зафиксирована RED-фаза TDD.
- Решения: реализовать минимальный `Shader` и import pipeline без `ShaderMaterial`, uniforms и public compiled bytecode.
- Проверки: три focused RED-команды завершились ожидаемыми compile errors.
- Далее: добавить production-реализацию `Shader`, parser, import result/diagnostics и SDL_shadercross compiler boundary.

## 05:19 +03:00 - Agent: Codex

- Задача: T-0031 / canvas shaders: import, SDL_shadercross compile, vertex/fragment stages и диагностика файл/строка.
- Контекст: RED-фаза подтверждена, внесена минимальная production-реализация.
- Действия:
  - 05:15 - добавлен public `Shader` с `Code`, `GetMode()` и nested `Mode.CanvasItem`.
  - 05:16 - добавлены internal canvas shader import request/result, diagnostics, compiled stages, stage/platform enums и compiler interface.
  - 05:17 - добавлен `CanvasShaderImportPipeline` с парсингом `shader_type canvas_item;`, `vertex_entry`, `fragment_entry` и mapping compiler diagnostics.
  - 05:18 - добавлен `SdlShaderCrossCompiler` и package dependency `SDL3-CS.Native.Shadercross` `3.0.0`.
  - 05:18 - focused unit tests по `Shader` и public runtime baseline прошли: `3` теста.
  - 05:18 - focused integration tests по shader import и diagnostics прошли: `2` теста.
  - 05:19 - focused runtime smoke test по iOS no-runtime-compile artifact прошёл: `1` тест.
- Изменения: добавлены shader runtime source files, зависимость shadercross и новые unit/integration/runtime smoke tests.
- Решения: `ShaderMaterial`, uniforms, samplers и public compiled bytecode не добавлять в T-0031; это scope T-0032 и будущего resource pipeline.
- Проверки: новые focused GREEN-команды прошли без предупреждений после исправления SDL3-CS `out`/`in` signatures.
- Далее: обновить implementation docs, API Wiki source, release materials и выполнить полный набор проверок.

## 05:22 +03:00 - Agent: Codex

- Задача: T-0031 / canvas shaders: import, SDL_shadercross compile, vertex/fragment stages и диагностика файл/строка.
- Контекст: focused GREEN пройден, нужно синхронизировать документы и release-gates.
- Действия:
  - 05:20 - добавлена справка `docs/documentation/rendering/canvas-shader-import-baseline.md`.
  - 05:20 - обновлены rendering docs, SDL_GPU lifecycle docs, documentation index, API Wiki source, release-management API docs, README, CHANGELOG и RELEASE-NOTES.
  - 05:20 - `Verify-ApiCompatibility.ps1` прошёл, public types: `45`.
  - 05:21 - полный `Run-Tests.ps1` прошёл: unit `55`, integration `81`, runtime smoke `5`, golden data `4`.
  - 05:22 - прошли `Verify-ProjectTemplate`, `Verify-CiMatrix`, `Verify-PerformanceBudgets`, `Verify-ReleaseMetadata`, повторный `Verify-ApiCompatibility` и `Verify-SourceLicenseHeaders`.
- Изменения: обновлены implementation docs, API/release материалы и release-gate dependency notes.
- Решения: native `SDL3-CS.Native.Shadercross` добавлен как dependency для import/export host, а platform-specific packaging остаётся будущими export задачами.
- Проверки: полный test runner и release-gate verifiers прошли; pre-stage source license verifier прошёл для tracked source files.
- Далее: перенести T-0031 в `completed-tasks`, выполнить staged checks и commit.

## 05:23 +03:00 - Agent: Codex

- Задача: T-0031 / canvas shaders: import, SDL_shadercross compile, vertex/fragment stages и диагностика файл/строка.
- Контекст: release-gate проверки прошли, задача готова к архивированию.
- Действия:
  - 05:23 - `T-0031` перенесена из `TASKS.md` в `completed-tasks/T-0031.md`.
- Изменения: создан архив задачи T-0031, обновлён `TASKS.md`.
- Решения: T-0032 остаётся следующей активной задачей для `ShaderMaterial`, uniforms и samplers.
- Проверки: дополнительных проверок после архивирования пока не запускалось.
- Далее: выполнить whitespace check, staging, staged license check и commit T-0031.

## 05:24 +03:00 - Agent: Codex

- Задача: T-0031 / canvas shaders: import, SDL_shadercross compile, vertex/fragment stages и диагностика файл/строка.
- Контекст: задача заархивирована, перед commit нужны staged checks.
- Действия:
  - 05:23 - `git diff --check` прошёл; предупреждения были только о будущей CRLF-нормализации Git под Windows.
  - 05:24 - изменения T-0031 добавлены в индекс.
  - 05:24 - staged `Verify-SourceLicenseHeaders.ps1` прошёл по `161 tracked source files`.
  - 05:24 - `git diff --cached --check` прошёл.
- Изменения: staged полный набор файлов T-0031.
- Решения: новые source-файлы `Shader`, shader import pipeline и tests входят в staged MIT license check.
- Проверки: staged source license и whitespace checks прошли.
- Далее: создать commit `T-0031: реализовать canvas shader import baseline`.

## 05:24 +03:00 - Agent: Codex

- Задача: T-0031 / canvas shaders: import, SDL_shadercross compile, vertex/fragment stages и диагностика файл/строка.
- Контекст: T-0031 завершена и зафиксирована в Git.
- Действия:
  - 05:24 - создан commit `T-0031: реализовать canvas shader import baseline`.
- Изменения: дневник дополнен записью о commit.
- Решения: commit hash в дневнике не фиксируется, потому что запись будет добавлена через amend.
- Проверки: commit создан успешно.
- Далее: проверить рабочую копию и продолжить выполнение следующей задачи.

## 05:25 +03:00 - Agent: Codex

- Задача: T-0032 / `ShaderMaterial`, uniforms, texture samplers и engine-provided built-ins.
- Контекст: рабочая копия чистая после commit T-0031; следующая активная задача в `TASKS.md` - T-0032.
- Действия:
  - 05:25 - `T-0032` помечена как `in progress` в `TASKS.md`.
  - 05:25 - добавлена заметка агента о старте после commit T-0031.
- Изменения: обновлены `TASKS.md` и дневник разработки.
- Решения: начать T-0032 со спецификации и RED-тестов, потому что задача меняет public API, serialization behavior и rendering resource model.
- Проверки: перед стартом подтверждена чистая рабочая копия после T-0031.
- Далее: сверить Godot `ShaderMaterial`, текущие `Variant`/`Texture2D` capabilities и границы T-0032 перед спецификацией.

## 05:27 +03:00 - Agent: Codex

- Задача: T-0032 / уточнение пользователя по MIT License.
- Контекст: пользователь напомнил, что Electron2D должен быть под MIT License, а каждый source-файл должен содержать полный license header в стиле SDL.
- Действия:
  - 05:26 - проверены `AGENTS.md`, `LICENSE`, `docs/specifications/repository/license-policy.md`, `docs/documentation/repository/license-policy.md` и архивы `T-0130`/`T-0131`.
  - 05:26 - подтверждено, что локальное правило уже требует MIT License header для C# и PowerShell source-файлов.
  - 05:27 - запущена проверка `tools/Verify-SourceLicenseHeaders.ps1`.
- Изменения: новых файлов и задач не добавлено, потому что политика уже реализована и заархивирована в `completed-tasks`.
- Решения: не создавать дублирующую задачу; в рамках T-0032 все новые source-файлы должны начинаться с действующего MIT License header.
- Проверки: `powershell -ExecutionPolicy Bypass -File tools/Verify-SourceLicenseHeaders.ps1` прошёл, проверено `161 tracked source files`.
- Далее: продолжить T-0032 со сверки Godot `ShaderMaterial` и подготовки спецификации.

## 05:41 +03:00 - Agent: Codex

- Задача: T-0032 / `ShaderMaterial`, uniforms, texture samplers и engine-provided built-ins.
- Контекст: T-0032 меняет public API, сериализуемую model layer для материалов и release documentation.
- Действия:
  - 05:28 - сверены Godot `ShaderMaterial`, `Material` и canvas shader built-ins по официальной документации.
  - 05:30 - добавлена спецификация `docs/specifications/rendering/shader-material-baseline.md`.
  - 05:32 - добавлены RED unit/integration/golden-data tests для `Material`, `ShaderMaterial`, uniforms, `Texture2D` samplers, reserved built-ins и stable JSON snapshot.
  - 05:33 - RED-проверки подтвердили отсутствующие public/internal типы; параллельный запуск тестов дал блокировку build output, поэтому дальнейшие heavy checks выполнялись последовательно.
  - 05:36 - добавлена реализация `Material`, `ShaderMaterial`, validators, snapshot/serializer и `CanvasShaderBuiltInRegistry`.
  - 05:37 - focused GREEN unit/integration/golden-data tests прошли: `5`, `4` и `1` тест.
  - 05:39 - обновлены implementation docs, API Wiki source, README, changelog и release notes.
  - 05:40 - прошли `Verify-ApiCompatibility`, полный `Run-Tests.ps1`, project template, CI matrix, performance budgets, release metadata и source license verifier.
  - 05:41 - T-0032 перенесена из `TASKS.md` в `completed-tasks/T-0032.md`.
- Изменения: добавлены public `Material`/`ShaderMaterial`, internal shader material parameter snapshot/serializer, tests, спецификация, документация, Wiki/API и release materials.
- Решения: `CanvasItem.Material` не добавлять в T-0032; эта задача закрывает resource/model layer, а material-aware draw pipeline остаётся отдельным renderer scope.
- Проверки: full runner прошёл: unit `59`, integration `85`, runtime smoke `5`, golden data `5`; `Verify-ApiCompatibility.ps1` прошёл, public types `47`; source license verifier прошёл для tracked source files до staging.
- Далее: выполнить whitespace check, staging, staged source license/API checks и commit `T-0032: реализовать ShaderMaterial baseline`.

## 05:42 +03:00 - Agent: Codex

- Задача: T-0032 / staging и pre-commit проверки.
- Контекст: T-0032 заархивирована, изменения добавлены в Git index.
- Действия:
  - 05:41 - `git diff --check` прошёл без whitespace errors; были только стандартные Windows предупреждения о будущей CRLF-нормализации.
  - 05:41 - файлы T-0032 добавлены в индекс.
  - 05:41 - `git diff --cached --check` прошёл.
  - 05:42 - staged `Verify-SourceLicenseHeaders.ps1` прошёл по `172 tracked source files`.
  - 05:42 - staged `Verify-ApiCompatibility.ps1` прошёл, public types `47`.
- Изменения: staged весь набор T-0032, включая новые source-файлы с MIT header и архив `completed-tasks/T-0032.md`.
- Решения: commit делать после добавления этой записи дневника через повторный staging diary-файла.
- Проверки: staged whitespace, MIT source headers и API compatibility прошли.
- Далее: создать commit `T-0032: реализовать ShaderMaterial baseline`.

## 05:43 +03:00 - Agent: Codex

- Задача: T-0032 / `ShaderMaterial`, uniforms, texture samplers и engine-provided built-ins.
- Контекст: T-0032 завершена и зафиксирована в Git.
- Действия:
  - 05:43 - создан commit `T-0032: реализовать ShaderMaterial baseline`.
- Изменения: дневник дополнен записью о commit.
- Решения: commit hash в дневнике не фиксируется, потому что запись будет добавлена через amend.
- Проверки: commit создан успешно.
- Далее: проверить рабочую копию и перейти к следующей активной задаче.

## 05:44 +03:00 - Agent: Codex

- Задача: T-0033 / SDL_Renderer `Compatibility` backend для спрайтов, UI, текста, TileMap и primitives.
- Контекст: T-0032 завершена; следующая активная задача в `TASKS.md` касается renderer fallback profile, а не совместимости со старым публичным API.
- Действия:
  - 05:44 - подтверждена чистая рабочая копия после commit T-0032.
  - 05:44 - T-0033 помечена как `in progress` в `TASKS.md`.
- Изменения: обновлены `TASKS.md` и дневник разработки.
- Решения: трактовать `Compatibility` как SDL_Renderer backend внутри текущей Godot-like архитектуры; legacy/API compatibility layer по-прежнему не возвращается.
- Проверки: перед стартом T-0033 рабочая копия чистая, branch `main` ahead `39`.
- Далее: изучить действующие rendering specs/docs/source и подготовить спецификацию/RED-тесты T-0033.

## 05:47 +03:00 - Agent: Codex

- Задача: T-0033 / уточнение пользователя по MIT License.
- Контекст: пользователь повторно закрепил требование: Electron2D должен распространяться по MIT License, а каждый source-файл должен содержать полный license header в стиле SDL.
- Действия:
  - 05:46 - проверены `AGENTS.md`, архивы `completed-tasks/T-0130.md` и `completed-tasks/T-0131.md`, а также справка `docs/documentation/repository/license-policy.md`.
  - 05:46 - подтверждено, что правило уже реализовано: MIT License остаётся проектной лицензией, а "в стиле SDL" означает полный per-file header, а не SDL zlib license.
  - 05:46 - запущена актуальная проверка source headers.
- Изменения: новых исходников и документов не добавлено; дневник фиксирует результат проверки перед продолжением T-0033.
- Решения: не создавать дублирующую задачу, потому что `T-0130` и `T-0131` уже завершены; для новых файлов T-0033 продолжает действовать обязательный MIT header.
- Проверки: `powershell -ExecutionPolicy Bypass -File tools\Verify-SourceLicenseHeaders.ps1` прошёл, проверено `172 tracked source files`.
- Далее: продолжить T-0033 со спецификацией и RED-тестами.

## 05:58 +03:00 - Agent: Codex

- Задача: T-0033 / SDL_Renderer `Compatibility` backend для спрайтов, UI, текста, TileMap и primitives.
- Контекст: задача должна дать проверяемый fallback renderer baseline без возврата legacy public API.
- Действия:
  - 05:49 - добавлена спецификация `docs/specifications/rendering/sdl-renderer-compatibility-backend.md` и ссылка в индексе спецификаций.
  - 05:51 - добавлены RED integration/golden-data tests для `SdlRendererFramePlan`, command mapping, limitations и reference scene snapshot.
  - 05:52 - RED-проверки подтвердили отсутствующие `CreateFramePlan`, `Limitations`, `SdlRendererDrawCommandKind` и serializer.
  - 05:54 - реализованы internal `SdlRendererDrawCommandKind`, `SdlRendererDrawCommand`, `SdlRendererFramePlan`, `SdlRendererFramePlanTextSerializer` и `CompatibilityRenderingBackend.CreateFramePlan()`.
  - 05:55 - focused GREEN integration/golden-data tests прошли: 2 и 1 тест; serializer исправлен на LF-only output.
  - 05:56 - добавлена справка реализации и обновлены rendering docs, README, changelog и release notes.
  - 05:57 - прошли full test runner, API compatibility, source license verifier и whitespace check.
  - 05:58 - T-0033 перенесена из `TASKS.md` в `completed-tasks/T-0033.md`.
- Изменения: добавлен internal SDL_Renderer compatibility command-plan backend, tests, спецификация, документация и release-facing материалы.
- Решения: `Compatibility` трактуется как SDL_Renderer fallback profile; real SDL window presentation, actual SDL3-CS renderer calls и public `TileMapLayer` остаются отдельными задачами.
- Проверки: `tools\Run-Tests.ps1` прошёл: unit `59`, integration `87`, runtime smoke `5`, golden data `6`; `Verify-ApiCompatibility.ps1` прошёл, public types `47`; `Verify-SourceLicenseHeaders.ps1` прошёл для `172 tracked source files`; `git diff --check` прошёл с обычными Windows CRLF предупреждениями.
- Далее: выполнить staged проверки и создать commit `T-0033: реализовать SDL_Renderer compatibility baseline`.

## 06:00 +03:00 - Agent: Codex

- Задача: T-0033 / staging и pre-commit проверки.
- Контекст: T-0033 заархивирована, изменения добавлены в Git index.
- Действия:
  - 05:59 - файлы T-0033 добавлены в индекс.
  - 05:59 - `git diff --cached --check` прошёл без ошибок.
  - 06:00 - staged `Verify-ApiCompatibility.ps1` прошёл, public types `47`.
  - 06:00 - staged `Verify-SourceLicenseHeaders.ps1` прошёл по `178 tracked source files`.
- Изменения: staged весь набор T-0033, включая новые internal source-файлы с MIT header и архив `completed-tasks/T-0033.md`.
- Решения: commit делать после добавления этой записи дневника через повторный staging diary-файла.
- Проверки: staged whitespace, API compatibility и MIT source headers прошли.
- Далее: создать commit `T-0033: реализовать SDL_Renderer compatibility baseline`.

## 06:00 +03:00 - Agent: Codex

- Задача: T-0033 / SDL_Renderer `Compatibility` backend для спрайтов, UI, текста, TileMap и primitives.
- Контекст: T-0033 завершена и зафиксирована в Git.
- Действия:
  - 06:00 - создан commit `T-0033: реализовать SDL_Renderer compatibility baseline`.
- Изменения: дневник дополнен записью о commit.
- Решения: commit hash в дневнике не фиксируется, потому что запись будет добавлена через amend.
- Проверки: commit создан успешно.
- Далее: проверить рабочую копию и перейти к следующей активной задаче.

## 06:01 +03:00 - Agent: Codex

- Задача: T-0034 / Android mobile profile, GPU smoke test и automatic/fail fallback policy.
- Контекст: после commit T-0033 следующая активная задача должна закрыть internal startup/fallback policy для SDL_GPU mobile profile и `Compatibility` fallback.
- Действия:
  - 06:01 - подтверждена чистая рабочая копия после commit T-0033.
  - 06:01 - T-0034 помечена как `in progress` в `TASKS.md`.
- Изменения: обновлены `TASKS.md` и дневник разработки.
- Решения: трактовать задачу как internal renderer startup policy: mobile SDL_GPU smoke test с reasons и fallback на `Compatibility`, без возврата legacy compatibility layer.
- Проверки: перед стартом T-0034 рабочая копия чистая, branch `main` ahead `40`.
- Далее: изучить SDL_GPU lifecycle baseline, Android fallback требования и подготовить спецификацию/RED-тесты.

## 06:13 +03:00 - Agent: Codex

- Задача: T-0034 / Android mobile profile, GPU smoke test и automatic/fail fallback policy.
- Контекст: задача меняет internal renderer startup behavior и должна сохранить public Godot-like API без новых public backend types.
- Действия:
  - 06:03 - сверены `SdlGpuRenderingBackend`, `ISdlGpuApi`, SDL3-CS signatures и действующие SDL_GPU lifecycle docs/tests.
  - 06:05 - добавлена спецификация `docs/specifications/rendering/android-mobile-gpu-fallback-policy.md`.
  - 06:06 - добавлены RED integration tests для Android mobile create profile, smoke steps, `Automatic` fallback, `FailIfUnavailable` и startup log.
  - 06:07 - RED-проверка подтвердила отсутствующие startup policy/create info/smoke result типы и старую сигнатуру `ISdlGpuApi.CreateDevice`.
  - 06:09 - реализованы internal `SdlGpuDeviceCreateInfo`, `SdlGpuDeviceInfo`, smoke result/model, `SdlGpuMobileSmokeTest`, `SdlGpuStartupPolicy` и `SdlGpuStartupException`.
  - 06:10 - `SdlGpuApi` переведён на `SDL_CreateGPUDeviceWithProperties`, device info и smoke adapter methods; `SdlGpuRenderingBackend` получил create info/device info.
  - 06:11 - focused GREEN tests прошли: `SdlGpuStartupPolicyTests` 3 теста и `SdlGpuLifecycleTests` 6 тестов.
  - 06:12 - добавлена справка реализации и обновлены rendering docs, README, changelog и release notes.
  - 06:13 - прошли full test runner, API compatibility, source license verifier и whitespace check.
  - 06:13 - T-0034 перенесена из `TASKS.md` в `completed-tasks/T-0034.md`.
- Изменения: добавлен internal Android mobile GPU startup/fallback baseline, tests, спецификация, документация и release-facing материалы.
- Решения: mobile profile остаётся internal SDL_GPU create policy; public `RenderingProfile.Mobile` не добавлялся. Pipeline smoke в production adapter пока проверяет shader format availability, а полноценный compiled shader pipeline smoke остаётся будущей задачей.
- Проверки: `tools\Run-Tests.ps1` прошёл: unit `59`, integration `90`, runtime smoke `5`, golden data `6`; `Verify-ApiCompatibility.ps1` прошёл, public types `47`; `Verify-SourceLicenseHeaders.ps1` прошёл для `178 tracked source files`; `git diff --check` прошёл с обычными Windows CRLF предупреждениями.
- Далее: выполнить staged проверки и создать commit `T-0034: реализовать Android GPU fallback policy`.

## 06:15 +03:00 - Agent: Codex

- Задача: T-0034 / staging и pre-commit проверки.
- Контекст: T-0034 заархивирована, изменения добавлены в Git index.
- Действия:
  - 06:14 - файлы T-0034 добавлены в индекс.
  - 06:14 - `git diff --cached --check` прошёл без ошибок.
  - 06:15 - staged `Verify-ApiCompatibility.ps1` прошёл, public types `47`.
  - 06:15 - staged `Verify-SourceLicenseHeaders.ps1` прошёл по `183 tracked source files`.
- Изменения: staged весь набор T-0034, включая новые internal source-файлы с MIT header и архив `completed-tasks/T-0034.md`.
- Решения: commit делать после добавления этой записи дневника через повторный staging diary-файла.
- Проверки: staged whitespace, API compatibility и MIT source headers прошли.
- Далее: создать commit `T-0034: реализовать Android GPU fallback policy`.

## 06:15 +03:00 - Agent: Codex

- Задача: T-0034 / Android mobile profile, GPU smoke test и automatic/fail fallback policy.
- Контекст: T-0034 завершена и зафиксирована в Git.
- Действия:
  - 06:15 - создан commit `T-0034: реализовать Android GPU fallback policy`.
- Изменения: дневник дополнен записью о commit.
- Решения: commit hash в дневнике не фиксируется, потому что запись будет добавлена через amend.
- Проверки: commit создан успешно.
- Далее: проверить рабочую копию и перейти к следующей активной задаче.

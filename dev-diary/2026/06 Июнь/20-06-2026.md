## Запись: Подготовка backlog Electron2D 0.1.0

### Контекст
- Пользователь попросил изучить `GOAL.md` и `GOAL-0.1.0.md`, оформить их как документацию в доменных разделах и подготовить `TASKS.md` со списком атомарных задач для версии `0.1.0`.
- В репозитории уже были незакоммиченные изменения `.gitignore`, `LICENSE` и новый `AGENTS.md`; они не относятся к текущей работе и не изменялись.

### Хронология
- 16:13
  - Проверены локальные инструкции, структура репозитория, `README.md`, `.sln`, проекты примеров и наличие `TASKS.md`, `docs/`, `dev-diary/`.
- 16:16
  - `GOAL.md` перенесён в `docs/specifications/architecture/engine-platform-stack.md`.
  - `GOAL-0.1.0.md` перенесён в `docs/specifications/releases/0.1.0-preview.md`.
  - Добавлен индекс `docs/specifications/README.md`.

### Изменения
- Создана структура спецификаций для архитектурного стека и релиза `0.1.0 Preview`.

### Решения
- Goal-документы размещены в `docs/specifications/`, потому что они описывают целевое состояние, а не уже реализованное поведение.
- `TASKS.md` будет ссылаться на новые спецификации, а не на корневые goal-файлы.

### Проверки
- Проверено, что до изменений в репозитории отсутствовали `TASKS.md`, `docs/` и `dev-diary/`.

### Следующие шаги
- Сформировать атомарный backlog `TASKS.md`.
- Проверить итоговое состояние Markdown-файлов и рабочей копии.

## Запись: Итог подготовки TASKS.md

### Контекст
- Требовалось подготовить задачи для достижения `Electron2D 0.1.0 Preview` и ссылаться на оформленные goal-документы как на спецификации.

### Хронология
- 16:18
  - Создан `TASKS.md` с релизным backlog от `T-0001` до `T-0105`.
- 16:20
  - Проверено, что строки задач идут непрерывно от `T-0001` до `T-0105`.
  - Проверено, что только `T-0001` имеет статус `in progress`.
- 16:21
  - Проверено наличие целевых документов в `docs/specifications/`.

### Изменения
- `TASKS.md` описывает 105 атомарных задач по runtime, editor, export pipeline, примерам, документации, производительности и релизному gate.
- В `TASKS.md` добавлены общие `DoD-Code` и `DoD-Docs`.

### Решения
- `T-0001` оставлен в статусе `in progress`, потому что пользователь ещё не принял структуру backlog.
- Остальные задачи имеют статус `open`.

### Проверки
- Проверены ID задач, единственный статус `in progress`, существование ссылочных путей `docs/specifications/README.md`, `docs/specifications/architecture/engine-platform-stack.md`, `docs/specifications/releases/0.1.0-preview.md`, `Electron2D/Core`, `Electron2D/Runtime`, `Examples`.
- Тесты кода не запускались, потому что изменения затрагивали только Markdown и структуру документов.

### Следующие шаги
- Дождаться принятия или корректировок backlog.
- После принятия закрыть или архивировать `T-0001` только по явному указанию пользователя.

## Запись: Переформатирование TASKS.md в карточки задач

### Контекст
- Пользователь уточнил обязательный формат задач в `TASKS.md`: каждая задача должна быть отдельной секцией `## T-XXXX [ ] P*: Заголовок задачи` с метаданными, ссылками, самодостаточным описанием, критериями приёмки, подзадачами и пустыми заметками агента.

### Хронология
- 16:24
  - Перечитаны локальные инструкции, `TASKS.md` и состояние рабочей копии.
- 16:29
  - Табличный backlog преобразован в секционный формат для задач `T-0001`-`T-0105`.
- 16:33
  - Проверено количество заголовков, обязательных блоков и стандартных критериев.
- 16:34
  - Убраны лишние точки в заголовках и служебные `DoD-Code`/`DoD-Docs` из описательных предложений.

### Изменения
- `TASKS.md` больше не содержит таблицы задач.
- Каждая из 105 задач оформлена отдельной карточкой с полями `Создана`, `Приоритет`, `Зависимости`, `Ссылки`, разделами `Самодостаточное описание`, `Критерии приёмки`, `Подзадачи`, `Заметки агента`.

### Решения
- Чекбоксы задач оставлены пустыми `[ ]`, потому что задачи ещё не приняты пользователем.
- Для `Upstream commit` указано `нет`, так как backlog не привязан к внешним upstream-коммитам.
- В ссылках указаны существующие релизная и архитектурная спецификации, а также целевые доменные каталоги `docs/specifications/<domain>/` и `docs/documentation/<domain>/`.

### Проверки
- Подтверждено 105 заголовков формата `## T-XXXX [ ] P*:`.
- Подтверждено 105 повторов каждого обязательного блока: метаданные, ссылки, самодостаточное описание, критерии приёмки, подзадачи, заметки агента.
- Подтверждено отсутствие старых строк таблицы `| T-XXXX |`.
- Подтверждено 105 повторов стандартных критериев про спецификацию, тесты, документацию и команду проверки.

### Следующие шаги
- Перед финальным ответом проверить хвост дневника и состояние рабочей копии.

## Запись: Финальная проверка формата TASKS.md

### Контекст
- Перед финальным ответом нужно было подтвердить, что `TASKS.md` соответствует заданному пользователем формату.

### Хронология
- 16:35
  - Повторно проверены структурные счётчики `TASKS.md`.
  - Проверено состояние рабочей копии.

### Изменения
- Дополнительных изменений в `TASKS.md` после структурной проверки не потребовалось.

### Решения
- Не трогать неожиданные изменения вне Markdown-задачи: `git status` показывает крупные изменения в исходном дереве (`Electron2D/`, проекты `Examples/`, новый `src/`), которые не относятся к текущему переформатированию.

### Проверки
- В `TASKS.md` найдено 105 заголовков задач формата `## T-XXXX [ ] P*:`.
- Старые строки таблицы `| T-XXXX |` отсутствуют.
- Разделы `Самодостаточное описание`, `Критерии приёмки`, `Подзадачи`, `Заметки агента` присутствуют по 105 раз.

### Следующие шаги
- Сообщить пользователю, что формат задач приведён к требуемому виду, и отдельно предупредить о несвязанных изменениях рабочей копии.

## Запись: Фиксация начальной структуры репозитория

### Контекст
- Пользователь попросил зафиксировать новую структуру репозитория, обновить `.gitignore`, переименовать ветку `master` в `main` и сделать commit начального состояния перед крупной работой.

### Хронология
- 16:37
  - Проверены текущая ветка, `git status`, корневая структура и `.gitignore`.
- 16:39
  - Проверена новая раскладка `src/`, `examples/`, `tests/`, `docs/`.
  - Обнаружено, что solution лежал в `src/`, а пути упаковки в `src/Electron2D/Electron2D.csproj` ссылались на неверный уровень.
- 16:41
  - Solution возвращён в корень как `Electron2D.sln`.
  - Исправлены пути в `Electron2D.sln`, `src/Electron2D/Electron2D.csproj` и ссылках `TASKS.md`.
  - `.gitignore` расширен под .NET, IDE, тестовые, coverage, log и временные артефакты.
- 16:43
  - Выполнена проверочная сборка `dotnet build Electron2D.sln --no-restore`.

### Изменения
- Основная библиотека фиксируется в `src/Electron2D/`.
- Примеры фиксируются в `examples/` с явным регистром в Git.
- Для будущих проверок добавлен `tests/README.md`.
- Для документации фактической реализации добавлен `docs/documentation/README.md`.
- Корневой `.gitignore` обновлён для стартовой .NET-структуры.

### Решения
- Оставить solution в корне репозитория, чтобы команда сборки была очевидной: `dotnet build Electron2D.sln`.
- Сохранять `.codex/` вне Git как локальную служебную директорию.
- Не добавлять generated `bin/` и `obj/` после сборки.

### Проверки
- `dotnet build Electron2D.sln` прошёл успешно: 0 ошибок, 5 существующих предупреждений nullable/unused.
- После выравнивания solution повторно выполнен `dotnet build Electron2D.sln --no-restore`: 0 ошибок, 0 предупреждений.

### Следующие шаги
- Переименовать ветку `master` в `main`.
- Проиндексировать целевое состояние и создать commit начального состояния репозитория.

## Запись: Commit начального состояния

### Контекст
- Нужно было завершить фиксацию baseline перед крупной работой.

### Хронология
- 16:45
  - Ветка переименована в `main`.
  - Изменения проиндексированы и проверены перед commit.
  - Создан commit `chore: зафиксировать начальную структуру репозитория`.

### Изменения
- В commit вошли новая структура `src/`, `examples/`, `tests/`, `docs/`, `TASKS.md`, проектные инструкции и обновлённый `.gitignore`.

### Решения
- Использовать один baseline commit без отдельного второго commit под дневник; финальная запись дневника добавляется через amend.

### Проверки
- Перед commit выполнены `dotnet build Electron2D.sln` и `dotnet build Electron2D.sln --no-restore`.
- `git diff --cached --check` прошёл без замечаний после удаления лишней пустой строки в `TASKS.md`.
- В staged файлах не было `.codex/`, `bin/` или `obj/`.

### Следующие шаги
- Сообщить пользователю итоговый hash commit, ветку и результат проверок.

## Запись: Перезапуск 0.1.0 Preview с нуля

### Контекст
- Пользователь отменил продолжение legacy-реализации, потребовал сохранить всю работу после `4007f36bf6857b33d6fc8cf614732f92e839287d` в отдельной приватной ветке без префикса `codex`, вернуть `main` к этому коммиту, удалить `src/Electron2D/` и заново выполнять `TASKS.md`.

### Хронология
- 21:05
  - Создана локальная ветка `private/pre-rewrite-backup-2026-06-20`.
  - В backup-ветку добавлен commit `15d968f` с незакоммиченными правками.
  - Проверено, что backup-ветка содержит всю цепочку задач после `4007f36bf6857b33d6fc8cf614732f92e839287d`.
  - `main` возвращён к `4007f36bf6857b33d6fc8cf614732f92e839287d`.
  - Полностью удалён каталог `src/Electron2D/`.
  - `T-0001` перенесена из `TASKS.md` в `completed-tasks/T-0001.md`.

### Изменения
- Релизная спецификация фиксирует новый baseline: `0.1.0 Preview` пишется заново, без Unity-like component history, без compatibility layer и без публичных API вне Godot-like 2D-поднабора.
- Документация реализации фиксирует отсутствие старого runtime baseline.

### Решения
- Не переносить старые типы `IComponent`, `SpriteRenderer`, legacy physics components и другие элементы старого API в новую реализацию.
- Wiki-материалы релиза готовить как GitHub Wiki source, а не как локальный сайт.

### Проверки
- Подтверждена backup-ветка `private/pre-rewrite-backup-2026-06-20`.
- Подтверждён reset `main` к `4007f36bf6857b33d6fc8cf614732f92e839287d`.
- После удаления `src/Electron2D/` сборка ожидаемо ещё не запускается до восстановления проекта в следующих задачах.

### Следующие шаги
- Закоммитить завершение `T-0001`.
- Перейти к `T-0002`: тестовая инфраструктура с нуля.

## Запись: Commit T-0001

### Контекст
- Нужно было зафиксировать завершение `T-0001` отдельным commit после переноса задачи в `completed-tasks/`.

### Хронология
- 21:05
  - Создан commit `T-0001: перезапустить 0.1.0 с чистого baseline`.
  - В архивной записи задачи заменена временная пометка о commit на сообщение commit.

### Изменения
- В commit вошли удаление старого `src/Electron2D/`, обновление релизной спецификации, документации реализации, `TASKS.md`, архив `completed-tasks/T-0001.md` и дневник.

### Решения
- Не хранить hash commit внутри самого архивного файла, потому что amend меняет итоговый hash.

### Проверки
- Перед commit проверено, что `src/Electron2D/` отсутствует, активный список начинается с `T-0002`, а `git diff --check` не сообщает ошибок.

### Следующие шаги
- Перейти к `T-0002`: тестовая инфраструктура с нуля.

## Запись: T-0002 тестовая инфраструктура после clean reset

### Контекст
- После удаления старого `src/Electron2D/` нужно создать тестовую инфраструктуру заново, без восстановления legacy runtime и без Unity-like component API.

### Хронология
- 21:10
  - Создан новый пустой runtime-проект `src/Electron2D/Electron2D.csproj`.
  - Добавлены проекты `Electron2D.Tests.Unit`, `Electron2D.Tests.Integration`, `Electron2D.Tests.RuntimeSmoke` и `Electron2D.Tests.GoldenData`.
  - Старые examples исключены из основного solution до отдельной задачи миграции.
  - Добавлен `tools/Run-Tests.ps1` для обычного green-check и явного baseline-run.
  - Созданы спецификация и документация тестовой инфраструктуры.

### Изменения
- Unit baseline проверяет отсутствие публичных legacy-типов.
- Runtime smoke содержит отдельный `Category=Baseline` тест, который должен падать, пока не реализован `Electron2D.Node`.

### Решения
- Основной тестовый запуск исключает `Category=Baseline`, чтобы инфраструктурная задача оставалась проверяемой до реализации объектной модели.
- Baseline-тест запускается только явно через `-IncludeBaseline` и служит TDD-якорем для следующих задач.

### Проверки
- `powershell -ExecutionPolicy Bypass -File tools/Run-Tests.ps1` прошёл успешно: unit, integration, runtime smoke и golden-data проекты собираются и проходят без `Category=Baseline`.
- `powershell -ExecutionPolicy Bypass -File tools/Run-Tests.ps1 -IncludeBaseline` завершился ожидаемым падением `SceneTreeBaselineFailsUntilNodeExists`: `Electron2D.Node` ещё отсутствует.
- `dotnet sln Electron2D.sln list` показывает новый runtime-проект и четыре тестовых проекта.

### Следующие шаги
- Перенести `T-0002` в `completed-tasks/` и сделать commit.

## Запись: Commit T-0002

### Контекст
- Нужно было зафиксировать тестовую инфраструктуру отдельным commit и продолжить выполнение `TASKS.md`.

### Хронология
- 21:16
  - Создан commit `T-0002: создать тестовую инфраструктуру`.

### Изменения
- В commit вошли новый runtime-проект, четыре тестовых проекта, test runner, спецификация, документация, обновлённый solution, перенос `T-0002` в `completed-tasks/` и дневник.

### Решения
- `Category=Baseline` остаётся отдельным red-run и не входит в обычный green-check.

### Проверки
- Перед commit выполнены `tools/Run-Tests.ps1`, `tools/Run-Tests.ps1 -IncludeBaseline`, `dotnet sln Electron2D.sln list` и `git diff --check`.

### Следующие шаги
- Перейти к `T-0003`: CI-матрица для тестовой инфраструктуры.

## Запись: T-0003 CI-матрица

### Контекст
- После создания тестовой инфраструктуры нужно добавить CI для desktop-матрицы и явно отметить, что mobile/export checks пока не являются активным gate.

### Хронология
- 21:18
  - Созданы спецификация и документация CI-матрицы.
  - Добавлен локальный verifier `tools/Verify-CiMatrix.ps1`.

### Изменения
- Verifier проверяет наличие `.github/workflows/ci.yml`, desktop runner matrix, .NET SDK `10.0.x`, запуск `tools/Run-Tests.ps1` и явный mobile/export gap.

### Решения
- CI не должен запускать `-IncludeBaseline` по умолчанию, потому что baseline-тест намеренно красный до реализации `Electron2D.Node`.

### Проверки
- Первый запуск `powershell -ExecutionPolicy Bypass -File tools/Verify-CiMatrix.ps1` упал ожидаемо: `.github/workflows/ci.yml` отсутствовал.
- После добавления `.github/workflows/ci.yml` verifier прошёл: `CI matrix verification passed.`
- `powershell -ExecutionPolicy Bypass -File tools/Run-Tests.ps1` повторно прошёл успешно.

### Следующие шаги
- Перенести `T-0003` в `completed-tasks/` и сделать commit.

## Запись: Commit T-0003

### Контекст
- Нужно было зафиксировать CI-матрицу отдельным commit перед переходом к compatibility table.

### Хронология
- 21:23
  - Создан commit `T-0003: настроить CI-матрицу`.

### Изменения
- В commit вошли `.github/workflows/ci.yml`, verifier CI, спецификация, документация, перенос `T-0003` в `completed-tasks/` и дневник.

### Решения
- Desktop CI запускает обычный green-check; mobile/export остаётся явным status gap до задач экспорта.

### Проверки
- Перед commit повторно выполнен `tools/Verify-CiMatrix.ps1`; `git diff --check` не показал ошибок.

### Следующие шаги
- Перейти к `T-0004`: compatibility table для Godot-like public API.

## Запись: T-0004 compatibility table

### Контекст
- Нужно сформировать таблицу совместимости публичного API с Godot-like моделью и не создавать локальный сайт вместо GitHub Wiki.

### Хронология
- 21:25
  - Создана спецификация `docs/specifications/release-management/api-compatibility.md`.
  - Добавлен verifier `tools/Verify-ApiCompatibility.ps1`.

### Изменения
- Verifier собирает runtime assembly, читает exported public types и сверяет их с GitHub Wiki source.
- Запрещённые legacy/component типы явно перечислены в спецификации и verifier.

### Решения
- Canonical source compatibility table для текущего репозитория: `.github/wiki/API-Compatibility.md`.
- Не добавлять local site generator или локальный сайт для Wiki.

### Проверки
- Первый запуск `powershell -ExecutionPolicy Bypass -File tools/Verify-ApiCompatibility.ps1` упал ожидаемо: `.github/wiki/API-Compatibility.md` отсутствовал.
- После добавления GitHub Wiki source verifier прошёл: `API compatibility verification passed. Public types: 0.`
- `powershell -ExecutionPolicy Bypass -File tools/Run-Tests.ps1` прошёл успешно.

### Следующие шаги
- Перенести `T-0004` в `completed-tasks/` и сделать commit.

## Запись: Commit T-0004

### Контекст
- Нужно было зафиксировать GitHub Wiki compatibility table и verifier отдельным commit.

### Хронология
- 21:28
  - Создан commit `T-0004: сформировать compatibility table`.

### Изменения
- В commit вошли `.github/wiki/API-Compatibility.md`, API verifier, спецификация, документация, перенос `T-0004` в `completed-tasks/` и дневник.

### Решения
- Compatibility table хранится как GitHub Wiki source, без локального сайта.

### Проверки
- Перед commit повторно выполнен `tools/Verify-ApiCompatibility.ps1`; `git diff --check` не показал ошибок.

### Следующие шаги
- Перейти к `T-0005`: версия, package metadata, changelog и release notes.

## Запись: T-0005 release metadata

### Контекст
- После clean reset нужно заново зафиксировать версию preview package, changelog и release notes.

### Хронология
- 21:31
  - Созданы спецификация и документация release metadata.
  - Добавлен verifier `tools/Verify-ReleaseMetadata.ps1`.

### Изменения
- Verifier сверяет package metadata в `src/Electron2D/Electron2D.csproj`, наличие `CHANGELOG.md`, `RELEASE-NOTES.md` и упоминание `0.1.0 Preview` в README.

### Решения
- Использовать NuGet-compatible prerelease version `0.1.0-preview`.

### Проверки
- Первый запуск `powershell -ExecutionPolicy Bypass -File tools/Verify-ReleaseMetadata.ps1` упал ожидаемо: package metadata ещё не была заполнена.
- После заполнения metadata verifier прошёл: `Release metadata verification passed.`
- `powershell -ExecutionPolicy Bypass -File tools/Run-Tests.ps1` прошёл успешно.
- `dotnet pack src\Electron2D\Electron2D.csproj --no-restore -o .temp\package-check` успешно создал package `Electron2D.0.1.0-preview.nupkg`.

### Следующие шаги
- Перенести `T-0005` в `completed-tasks/` и сделать commit.

## Запись: Commit T-0005

### Контекст
- Нужно было зафиксировать release metadata отдельным commit перед переходом к шаблону проекта.

### Хронология
- 21:36
  - Создан commit `T-0005: упорядочить release metadata`.

### Изменения
- В commit вошли package metadata, README, changelog, release notes, verifier, спецификация, документация, перенос `T-0005` в `completed-tasks/` и дневник.

### Решения
- NuGet package включает корневой README через `PackageReadmeFile`.

### Проверки
- Перед commit выполнены `Verify-ReleaseMetadata.ps1`, `Run-Tests.ps1`, `dotnet pack` и `git diff --check`.

### Следующие шаги
- Перейти к `T-0006`: формат проекта и минимальный шаблон.

## Запись: T-0006 project template

### Контекст
- Нужно зафиксировать минимальный формат проекта и шаблон нового проекта Electron2D после clean reset.

### Хронология
- 21:42
  - Созданы спецификация и документация project template.
  - Добавлен verifier `tools/Verify-ProjectTemplate.ps1`.

### Изменения
- Verifier должен собрать локальный package, создать временный проект из шаблона, собрать его и запустить пустую сцену-манифест.

### Решения
- Шаблон пока не использует runtime API, потому что объектная модель ещё не реализована.
- Пустая сцена фиксируется manifest-файлом без legacy component fields.

### Проверки
- Первый запуск `powershell -ExecutionPolicy Bypass -File tools/Verify-ProjectTemplate.ps1` упал ожидаемо: `templates/electron2d-empty` отсутствовал.
- После добавления шаблона `tools/Verify-ProjectTemplate.ps1` прошёл: временный проект восстановлен, собран и запущен.
- `powershell -ExecutionPolicy Bypass -File tools/Verify-CiMatrix.ps1` прошёл успешно после добавления template step в workflow.
- `powershell -ExecutionPolicy Bypass -File tools/Run-Tests.ps1` прошёл успешно.

### Следующие шаги
- Перенести `T-0006` в `completed-tasks/` и сделать commit.

## Запись: Commit T-0006

### Контекст
- Нужно было зафиксировать минимальный шаблон проекта отдельным commit.

### Хронология
- 21:46
  - Создан commit `T-0006: добавить минимальный шаблон проекта`.

### Изменения
- В commit вошли template source, verifier, CI step, спецификация, документация, перенос `T-0006` в `completed-tasks/` и дневник.

### Решения
- Шаблон проверяет manifest-проект и пустую сцену без обращения к ещё не реализованному runtime API.

### Проверки
- Перед commit выполнены `Verify-ProjectTemplate.ps1`, `Verify-CiMatrix.ps1`, `Run-Tests.ps1` и `git diff --check`.

### Следующие шаги
- Перейти к `T-0007`: performance budgets и soak-критерии.

## Запись: T-0007 performance budgets

### Контекст
- Нужно зафиксировать матрицу эталонных устройств, performance budgets, 30-минутные soak checks и mobile background/foreground циклы.

### Хронология
- 21:49
  - Добавлен verifier `tools/Verify-PerformanceBudgets.ps1`.

### Изменения
- Verifier требует документы performance budgets в specifications и documentation, а также ключевые фрагменты про desktop/mobile платформы, 60 FPS, frame budget, memory budget, soak и background/foreground.

### Решения
- Даже документационную release-gate задачу закрывать проверяемым script, чтобы CI мог ловить удаление матрицы.

### Проверки
- Первый запуск `powershell -ExecutionPolicy Bypass -File tools/Verify-PerformanceBudgets.ps1` упал ожидаемо: документы performance budgets отсутствовали.
- После добавления матрицы verifier прошёл: `Performance budget documentation verification passed.`
- `powershell -ExecutionPolicy Bypass -File tools/Verify-CiMatrix.ps1` прошёл успешно после добавления workflow step.
- `powershell -ExecutionPolicy Bypass -File tools/Run-Tests.ps1` прошёл успешно.

### Следующие шаги
- Перенести `T-0007` в `completed-tasks/` и сделать commit.

## Запись: Commit T-0007

### Контекст
- Нужно было зафиксировать performance budgets отдельным commit перед началом runtime object model.

### Хронология
- 21:51
  - Создан commit `T-0007: зафиксировать performance budgets`.

### Изменения
- В commit вошли performance budget spec/doc, verifier, CI step, перенос `T-0007` в `completed-tasks/` и дневник.

### Решения
- Performance budgets пока являются документированным release gate; будущие runtime milestones должны добавлять измеримые проверки поверх этой матрицы.

### Проверки
- Перед commit выполнены `Verify-PerformanceBudgets.ps1`, `Verify-CiMatrix.ps1`, `Run-Tests.ps1` и `git diff --check`.

### Следующие шаги
- Перейти к `T-0008`: `Object`, `RefCounted`, `Resource` и lifetime.

## Запись: T-0008 Object/RefCounted/Resource

### Контекст
- Начинается первая runtime-задача после clean reset: нужно добавить Godot-like базовые типы без legacy component API.

### Хронология
- 21:54
  - Создана спецификация `docs/specifications/object-model/base-object-lifetime.md`.
  - Добавлены unit-тесты для `Object`, `RefCounted`, `Resource`.
  - Обновлён baseline-тест public API surface.

### Изменения
- Тесты ожидают только `Electron2D.Object`, `Electron2D.RefCounted`, `Electron2D.Resource` в public surface и продолжают запрещать legacy component types.

### Решения
- Не добавлять `IDisposable` в public API, чтобы не расширять Godot-like surface .NET-specific методом `Dispose()`.

### Проверки
- Первый запуск `dotnet test tests\Electron2D.Tests.Unit\Electron2D.Tests.Unit.csproj --no-restore` упал ожидаемо: `Electron2D.Object`, `RefCounted`, `Resource` отсутствовали.
- После реализации `dotnet test tests\Electron2D.Tests.Unit\Electron2D.Tests.Unit.csproj --no-restore` прошёл: 5 passing tests.
- `powershell -ExecutionPolicy Bypass -File tools/Run-Tests.ps1` прошёл успешно.
- `powershell -ExecutionPolicy Bypass -File tools/Verify-ApiCompatibility.ps1` прошёл после перевода verifier на временный `net10.0` inspector: `Public types: 3`.
- `powershell -ExecutionPolicy Bypass -File tools/Verify-ReleaseMetadata.ps1`, `tools/Verify-ProjectTemplate.ps1`, `tools/Verify-CiMatrix.ps1` и `tools/Verify-PerformanceBudgets.ps1` прошли успешно.

### Следующие шаги
- Перенести `T-0008` в `completed-tasks/` и сделать commit.

## Запись: Новые требования к документации API и coverage

### Контекст
- Пользователь добавил требования: весь public API должен иметь SDL3-style C# XML documentation, Wiki должна автоматически генерировать public API reference в GitHub Wiki, код движка должен быть покрыт тестами на 100%.

### Хронология
- 22:02
  - Добавлена задача `T-0106` про SDL3-style XML documentation для public API.
  - Добавлена задача `T-0107` про автоматическую GitHub Wiki API reference.
  - Добавлена задача `T-0108` про 100% test coverage кода движка.

### Изменения
- `TASKS.md` дополнен тремя P0-задачами качества документации и coverage.

### Решения
- Новые требования не смешивать с текущим `T-0008`; они станут отдельными задачами release quality gate.

### Проверки
- Проверка структуры задач будет выполнена перед commit `T-0008`.

### Следующие шаги
- Закоммитить завершённую `T-0008` вместе с добавленными backlog-требованиями.

## Запись: Commit T-0008

### Контекст
- Нужно было зафиксировать первую runtime-задачу после clean reset и не смешивать её с пользовательским удалением legacy examples.

### Хронология
- 22:04
  - Создан commit `T-0008: реализовать базовые runtime-типы`.

### Изменения
- В commit вошли `Object`, `RefCounted`, `Resource`, unit-тесты, спецификация, документация, compatibility table, release notes/changelog/README, перенос `T-0008` в `completed-tasks/` и новые backlog-задачи `T-0106`-`T-0108`.

### Решения
- Удалённые пользователем legacy examples не индексировались в commit `T-0008`.

### Проверки
- Перед commit выполнены `Run-Tests.ps1`, `Verify-ApiCompatibility.ps1`, `Verify-ReleaseMetadata.ps1`, `Verify-ProjectTemplate.ps1`, `Verify-CiMatrix.ps1`, `Verify-PerformanceBudgets.ps1` и `git diff --check`.

### Следующие шаги
- Проверить статус рабочей копии и перейти к `T-0009`.

## Запись: Удаление legacy examples

### Контекст
- После commit `T-0008` рабочая копия показывала удаление старых examples. Пользователь подтвердил, что удалил их намеренно, потому что это legacy и они больше не нужны.

### Хронология
- 22:07
  - Добавлен архив завершённой задачи `completed-tasks/T-0109.md`.

### Изменения
- Удаление старых `examples/FlappyBird`, `examples/PhysicsUnits`, `examples/SpriteAnimation`, `examples/StartGame` будет зафиксировано отдельным commit.

### Решения
- Не восстанавливать legacy examples.
- Новые examples добавлять позже только под новый Godot-like API.

### Проверки
- Основной solution уже не зависит от старых examples.

### Следующие шаги
- Сделать commit `T-0109: удалить legacy examples`.

## Запись: Commit T-0109

### Контекст
- Нужно было зафиксировать пользовательское удаление legacy examples отдельным commit, чтобы не смешивать его с runtime-задачами.

### Хронология
- 22:07
  - Создан commit `T-0109: удалить legacy examples`.

### Изменения
- В commit вошли удаление старых examples, архив `completed-tasks/T-0109.md` и дневник.

### Решения
- Старые examples не восстанавливать; новые examples будут создаваться отдельно под новый Godot-like API.

### Проверки
- `dotnet sln Electron2D.sln list` подтвердил, что solution не зависит от удалённых examples.

### Следующие шаги
- Вернуться к активному backlog и начать `T-0009`.

## Запись: T-0009 Node/SceneTree lifecycle

### Контекст
- Нужно реализовать первый Godot-like lifecycle baseline: `Node`, `SceneTree`, `_EnterTree`, `_Ready`, `_Process`, `_PhysicsProcess`, `_Input`, `_ExitTree`.

### Хронология
- 22:10
  - Создана спецификация `docs/specifications/object-model/node-scene-tree-lifecycle.md`.
  - Добавлены integration-тесты lifecycle.
  - Runtime smoke baseline на `Electron2D.Node` переведён в обычный passing-test будущей реализации.

### Изменения
- Тесты фиксируют порядок enter/ready/process/physics/input/exit и очистку tree state при `RemoveChild()`.

### Решения
- `SceneTree.Root` в этой задаче временно является `Node`; `Viewport`/`Window` остаются будущими задачами.
- Полная hierarchy/ownership semantics остаётся в `T-0010`.

### Проверки
- Следующий запуск integration/runtime smoke тестов должен упасть до реализации `Node`, `SceneTree`, `InputEvent`.

### Следующие шаги
- Запустить тесты и подтвердить red-state.
- Реализовать минимальный lifecycle.

## Запись: Завершение T-0009

### Контекст
- Продолжена задача `T-0009`: первый Godot-like lifecycle baseline для `Node`, `SceneTree` и `InputEvent`.

### Хронология
- 22:23
  - Уточнён integration-тест `_Input`, чтобы он проверял передачу конкретного `InputEvent`.
  - Выполнен полный набор тестов и verifier'ов.
  - `T-0009` перенесена из `TASKS.md` в `completed-tasks/T-0009.md`.

### Изменения
- Добавлены `src/Electron2D/Core/Node.cs`, `src/Electron2D/Core/SceneTree.cs`, `src/Electron2D/Core/InputEvent.cs`.
- Добавлена internal diagnostics запись исключений lifecycle callbacks.
- Обновлены integration/runtime/unit тесты, спецификация, документация, GitHub Wiki compatibility table, README, changelog и release notes.
- Добавлена задача `T-0111` про production-ready repository и GitHub Release pipeline.

### Решения
- Публичный API оставлен Godot-like: lifecycle callbacks только с подчёркиванием.
- `SceneTree.Root` временно является `Node`; `Viewport`/`Window` остаются будущими задачами.
- Public diagnostics API не добавлялся, потому что он относится к отдельной `T-0016`.

### Проверки
- `dotnet test tests\Electron2D.Tests.Integration\Electron2D.Tests.Integration.csproj --no-restore` - прошло, 6 тестов.
- `powershell -ExecutionPolicy Bypass -File tools\Run-Tests.ps1` - прошло, 14 тестов.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ApiCompatibility.ps1` - прошло, public types: 6.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ReleaseMetadata.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ProjectTemplate.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-CiMatrix.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-PerformanceBudgets.ps1` - прошло.
- `git diff --check` - прошло без whitespace-ошибок.

### Следующие шаги
- Сделать commit `T-0009: реализовать Node и SceneTree lifecycle`.
- Перейти к следующей активной задаче `T-0010`.

## Запись: Commit T-0009

### Контекст
- Задача `T-0009` завершена, перенесена в `completed-tasks/` и готова к фиксации.

### Хронология
- 22:24
  - Создан commit `T-0009: реализовать Node и SceneTree lifecycle`.

### Изменения
- В commit вошли runtime-типы `Node`, `SceneTree`, `InputEvent`, integration/runtime/unit тесты, спецификация, документация, GitHub Wiki compatibility table, release records, архив `T-0009`, обновлённый backlog и дневник.

### Решения
- Следующей активной задачей остаётся `T-0010`: hierarchy, ownership, reparent и безопасное удаление.

### Проверки
- Перед commit выполнены все проверки, перечисленные в записи «Завершение T-0009».

### Следующие шаги
- Проверить статус рабочей копии.
- Продолжить выполнение backlog с `T-0010`.

## Запись: Старт T-0010

### Контекст
- Следующая задача backlog: `T-0010`, иерархия `Node`, ownership, reparent/move и безопасное удаление.

### Хронология
- 22:31
  - Сверен текущий public API `Node` после `T-0009`.
  - Сверен официальный Godot Node contract для lifecycle, `owner`, `remove_child()`, `reparent()` и `queue_free()`.
  - Создана целевая спецификация `docs/specifications/object-model/node-hierarchy-ownership.md`.

### Изменения
- Спецификация фиксирует удаление public `Parent` property в пользу Godot-like `GetParent()` и отдельного `Owner`.
- Сформирован контракт red-тестов для sibling order, ownership, reparent и queue-free.

### Решения
- `keepGlobalTransform` добавляется в сигнатуру `Reparent()` только как Godot-like параметр; до `Node2D` он не меняет поведение.
- `QueueFree()` должен откладывать удаление до конца traversal, чтобы пользовательский код не ломал текущий loop.

### Проверки
- Production-код для `T-0010` ещё не менялся; следующий шаг - добавить red-тесты.

### Следующие шаги
- Добавить red-тесты `T-0010`.
- Подтвердить падение тестов до реализации.

## Запись: Завершение T-0010

### Контекст
- Завершалась задача `T-0010`: hierarchy, ownership, reparent/move и безопасное удаление для Godot-like `Node`.

### Хронология
- 22:35
  - Red-state подтверждён integration-тестами: отсутствовали `GetParent`, `GetChild`, `Owner`, `Reparent`, `QueueFree`, `IsQueuedForDeletion`.
  - Реализован runtime baseline для parent-child инвариантов, `Owner`, `Reparent()` и queued deletion.
  - Обновлены спецификация, справка, Wiki compatibility, README, changelog и release notes.
  - `T-0010` перенесена из `TASKS.md` в `completed-tasks/T-0010.md`.

### Изменения
- `Node` больше не экспортирует public `Parent`; parent читается через `GetParent()`.
- `Owner` отделён от parent и валидируется как ancestor.
- `SceneTree` получил internal queue-free flush после traversal.
- Тесты покрывают порядок children, owner cleanup, reparent, cycles и safe deletion.

### Решения
- `keepGlobalTransform` в `Reparent()` пока no-op, потому что transform/`Node2D` ещё не реализованы.
- `RemoveChild()` не удаляет node; удаление происходит через `QueueFree()`/`Free()`.

### Проверки
- `dotnet test tests\Electron2D.Tests.Integration\Electron2D.Tests.Integration.csproj --no-restore` - сначала упал ожидаемо, затем прошёл, 12 тестов.
- `powershell -ExecutionPolicy Bypass -File tools\Run-Tests.ps1` - прошло, 20 тестов.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ApiCompatibility.ps1` - прошло, public types: 6.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ReleaseMetadata.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ProjectTemplate.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-CiMatrix.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-PerformanceBudgets.ps1` - прошло.

### Следующие шаги
- Выполнить `git diff --check`.
- Сделать commit `T-0010: реализовать hierarchy ownership и QueueFree`.
- Перейти к `T-0011`.

## Запись: Commit T-0010

### Контекст
- Задача `T-0010` завершена и перенесена в `completed-tasks/`.

### Хронология
- 22:36
  - Создан commit `T-0010: реализовать hierarchy ownership и QueueFree`.

### Изменения
- В commit вошли hierarchy/ownership/queue-free runtime changes, red/green integration tests, спецификация, справка, compatibility/wiki updates, release records, архив `T-0010`, обновлённый backlog и дневник.

### Решения
- Следующей активной задачей остаётся `T-0011`: `NodePath`, `GetNode`, `GetNodeOrNull`.

### Проверки
- Перед commit выполнены все проверки из записи «Завершение T-0010», включая `git diff --check`.

### Следующие шаги
- Проверить статус рабочей копии.
- Перейти к `T-0011`.

## Запись: Старт T-0011

### Контекст
- Следующая задача backlog: `T-0011`, `NodePath`, `GetNode`, `GetNodeOrNull` и стабильное разрешение путей.

### Хронология
- 22:42
  - Сверен Godot contract для `NodePath`, relative/absolute paths, `GetNode()` и `GetNodeOrNull()`.
  - Создана спецификация `docs/specifications/object-model/node-path-resolution.md`.

### Изменения
- Спецификация фиксирует минимальный public surface `NodePath` и path resolution для `0.1.0 Preview`.

### Решения
- До появления `StringName` методы `NodePath.GetName()` и `GetSubname()` временно возвращают `string`.
- `GetNode()` будет бросать `InvalidOperationException` при missing path, а `GetNodeOrNull()` возвращать `null`.

### Проверки
- Production-код для `T-0011` ещё не менялся; следующий шаг - добавить red-тесты.

### Следующие шаги
- Добавить integration/unit red-тесты для `NodePath` и path resolution.

## Запись: Завершение T-0011

### Контекст
- Завершалась задача `T-0011`: `NodePath`, `GetNode`, `GetNodeOrNull` и path resolution.

### Хронология
- 22:45
  - Red-state подтверждён integration-тестами: отсутствовали `NodePath`, `GetNode`, `GetNodeOrNull`.
  - Реализованы `NodePath`, relative/absolute lookup и missing-path semantics.
  - Обновлены спецификация, справка, Wiki compatibility, README, changelog и release notes.
  - `T-0011` перенесена из `TASKS.md` в `completed-tasks/T-0011.md`.

### Изменения
- Public runtime surface вырос до 7 типов за счёт `Electron2D.NodePath`.
- `SceneTree.Root` теперь называется `root`, чтобы absolute paths `/root/...` работали как Godot-like convention.
- `Node.GetNode()` бросает `InvalidOperationException` для missing path, `GetNodeOrNull()` возвращает `null`.

### Решения
- До появления `StringName` `NodePath.GetName()` и `GetSubname()` возвращают `string`.
- Subnames парсятся, но node lookup пока использует только node-name часть path.

### Проверки
- `dotnet test tests\Electron2D.Tests.Integration\Electron2D.Tests.Integration.csproj --no-restore` - сначала упал ожидаемо, затем прошёл, 17 тестов.
- `dotnet test tests\Electron2D.Tests.Unit\Electron2D.Tests.Unit.csproj --no-restore` - прошло, 5 тестов.
- `powershell -ExecutionPolicy Bypass -File tools\Run-Tests.ps1` - прошло, 25 тестов.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ApiCompatibility.ps1` - прошло, public types: 7.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ReleaseMetadata.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-ProjectTemplate.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-CiMatrix.ps1` - прошло.
- `powershell -ExecutionPolicy Bypass -File tools\Verify-PerformanceBudgets.ps1` - прошло.

### Следующие шаги
- Выполнить `git diff --check`.
- Сделать commit `T-0011: реализовать NodePath и GetNode`.
- Перейти к `T-0012`.

## Запись: Commit T-0011

### Контекст
- Задача `T-0011` завершена и перенесена в `completed-tasks/`.

### Хронология
- 22:46
  - Создан commit `T-0011: реализовать NodePath и GetNode`.

### Изменения
- В commit вошли `NodePath`, `GetNode()`/`GetNodeOrNull()`, red/green tests, спецификация, справка, compatibility/wiki updates, release records, архив `T-0011`, обновлённый backlog и дневник.

### Решения
- Следующей активной задачей остаётся `T-0012`: группы и group calls.

### Проверки
- Перед commit выполнены все проверки из записи «Завершение T-0011», включая `git diff --check`.

### Следующие шаги
- Проверить статус рабочей копии.
- Перейти к `T-0012`.

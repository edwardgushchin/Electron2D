VERDICT: NEEDS_FIXES

TASK_ASSESSMENT:
- Проверены `AUDIT-MANIFEST.md`, `metadata/audit-package.input.json`, `repo-file-hashes.json`, `T-0001.patch`, raw evidence из `evidence/`, изменения в коде, документации, skill-инструкциях и доступные previous verdict files по `metadata.previousVerdictChain`.
- Содержимое пакета в целом согласовано: manifest, metadata и inventory совпадают по задаче `T-0001`, итерации `r08`, baseline и списку изменённых путей; raw checks `audit-submit-focused-tests`, `verify-docs`, `verify-source-license-headers` и `git-diff-check` завершились успешно; для новых файлов, присутствующих в patch целиком, восстановленный текст с финальным LF совпадает по SHA-256 с `repo-file-hashes.json`.
- Previous verdict chain прочитан: `docs/verdicts/release-management/t-0001-audit-r02.md` и `docs/verdicts/release-management/t-0001-audit-r03.md` присутствуют в изменении, перечислены в manifest и `repo-file-hashes.json`, а внутри текущего пакета добавлены целиком, без частичных переписывающих hunk-ов. Их r02/r03 blocker-ы и `metadata.blockerClosureList` были отдельно сверены с текущей реализацией.
- Существенная часть hardening действительно реализована: удалён второй browser path, отклоняются неподдерживаемые backend-ы, убран старый auth subcommand, используется один основной ZIP, есть ping/pong handler, независимая browser-session identity без `CODEX_THREAD_ID`, ограниченные CDP retry/detach/reattach и запрет закрывать пользовательские вкладки.
- Однако изменение нельзя принять, потому что ключевой контракт текущей итерации про чтение только открытой карточки `Deep research report` / `Углубленный исследовательский отчет` фактически не соблюдён: реализация по-прежнему принимает источники вне самой карточки и не отбрасывает неоднозначные candidate report texts. Тесты это не доказывают и не ловят регрессию.

BLOCKERS:
- B1
  - File/symbol: `eng/Electron2D.Build/AuditSubmitCodexChromeCommand.cs`, `CapturePollingDecisionAsync` (строки 378-411 в восстановленном новом файле), `ReadReportCandidateMessagesAsync` (455-465), `ReadAssistantMessagesAsync` (437-443), `TryOpenDeepResearchReportAsync` (468-480), `DeepResearchReportTextsExpression` (780-832); `eng/Electron2D.Build/AuditSubmitCommand.cs`, `AuditSubmitReportExtractor.Extract` (302-345); `docs/release-management/audit-package.md`, разделы про `audit submit` и «Извлечение verdict-а - жёсткое правило»; `.codex/skills/submit-external-audit/SKILL.md`, основной procedural paragraph и правило про недопустимость whole-page extraction.
  - Criterion: текущий контракт задачи и документации требует читать итог только из полного DOM-текста открытой отчётной карточки, запрещает брать результат из `main.textContent`, всего `article`, history/sidebar, user/tool messages, обычного превью карточки и из нескольких candidate report texts сразу. В `metadata.blockerClosureList` отдельно заявлено закрытие r07 именно через открытие report card и чтение полного verdict text из DOM этой card.
  - Evidence: текущий код не ограничивает источник карточкой. `ReadReportCandidateMessagesAsync` объединяет обычные assistant messages и `DeepResearchReportTextsExpression`; затем `CapturePollingDecisionAsync` сначала читает эти candidate messages, и только если среди них нет полного отчёта, пытается открыть карточку через `TryOpenDeepResearchReportAsync`. Следовательно, любое другое complete-looking candidate source уже блокирует обязательное открытие карточки. Дополнительно `DeepResearchReportTextsExpression` сканирует слишком широкие поверхности: `[role="dialog"]`, `[aria-modal="true"]`, но также общий `article`, `section`, `main` и `div`; внутри него `reportFromText` вырезает текст от первого встретившегося decision marker до конца элемента, а не ограничивает чтение локальным контейнером report card. После этого `AuditSubmitReportExtractor.Extract` просто берёт последний элемент массива candidate texts (`assistantMessages[^1]`) и не проверяет, что candidate ровно один и что он получен именно из открытой карточки.
  - Impact: текущая реализация может сохранить в `--out` текст не из самой отчётной карточки, а из другой DOM-поверхности либо из неоднозначного набора поверхностей. Это прямо нарушает заявленный контракт итерации, не доказывает закрытие closure-item про r07 report-card extraction и оставляет риск ложного вердикта из preview/page/article/history/assistant surfaces вместо реального final report card.
  - Fix: сделать единственным источником итогового отчёта DOM открытой карточки report card после успешного явного открытия. Убрать assistant messages и широкое сканирование `main`/`div`/общего `article` из источника финального verdict-а; ограничить extraction карточным контейнером/диалогом; если найдено больше одного candidate report text или источник не привязан к открытой карточке, возвращать ожидание/ошибку, а не принимать отчёт.
  - Verification: добавить исполняемые regression tests, которые моделируют: один полный отчёт в открытой карточке; полный текст в preview/assistant/article/main; несколько candidate report texts одновременно. Ожидаемое поведение: acceptance только для единственного card-scoped source и отказ для всех остальных. Повторить `dotnet test tests/Electron2D.Tests.Integration/Electron2D.Tests.Integration.csproj --filter FullyQualifiedName~AuditSubmit|FullyQualifiedName~AuditPackageDocumentationRequiresMessageDeepResearchAndAttachedPackage|FullyQualifiedName~AuditPackageSanitizesRepositoryRootInCheckOutput` и приложить новый TRX.

- B2
  - File/symbol: `tests/Electron2D.Tests.Integration/RepositoryBuildToolTests.cs`, методы `AuditSubmitReportExtractorUsesOnlyCompleteLastAssistantMessage`, `AuditSubmitPollingPolicyWaitsWhileGenerationIsActive`, `AuditSubmitReportStabilityTrackerRequiresRepeatedCompleteReport`, `AuditSubmitCodexChromeClicksDeepResearchTool`, `AuditSubmitCommandKeepsOnlyCodexChromeBackend`; artifacts `evidence/T-0001-r08/checks/audit-submit-focused-tests/stdout.txt` и `.../trx/test-result-001.trx`.
  - Criterion: обязательный `test coverage review` и `previous blockers closure` требуют покрыть важные ветки поведения именно там, где контракт стал жёстче: card-only extraction, запрет whole-page/preview/history sources, обработка нескольких candidate report texts и доказательство того, что acceptance без открытия карточки невозможен.
  - Evidence: supplied tests этого не делают. `AuditSubmitReportExtractorUsesOnlyCompleteLastAssistantMessage` проверяет только список строк и headings, но не origin/source surface. `AuditSubmitPollingPolicyWaitsWhileGenerationIsActive` проверяет только булевый флаг `isGenerating`. `AuditSubmitCodexChromeClicksDeepResearchTool` и `AuditSubmitCommandKeepsOnlyCodexChromeBackend` — это source-level string/regex assertions по тексту файла, а не поведенческие проверки. В TRX и stdout есть 18 passed tests, но среди них нет сценария с несколькими candidate report texts, нет сценария, где полный текст найден в `main`/`article`/assistant message/preview, и нет проверки, что код обязан открыть карточку до acceptance.
  - Impact: текущий пакет не предоставляет доказуемого regression coverage именно для самого рискованного места изменения. Из-за этого supplied evidence не подтверждает заявленное closure из `metadata.blockerClosureList`, а реальный дефект из B1 беспрепятственно проходит через существующий test suite.
  - Fix: вынести card-extraction logic в тестируемый seam или добавить исполняемые integration/unit scenarios с synthetic DOM fixtures для: preview-only, page-level contamination, assistant-message contamination, multiple candidates, successful card-only read after explicit open. Тесты должны проверять не наличие строк в source, а фактическое решение polling/extraction.
  - Verification: обновить focused suite так, чтобы TRX явно содержал новые сценарии card-only extraction и ambiguity rejection, затем повторно приложить `stdout.txt` и `trx/test-result-001.trx` из `audit-submit-focused-tests`.

EVIDENCE_REVIEW:
- Прочитаны и сопоставлены:
  - `AUDIT-MANIFEST.md` — task/iteration/baseline/domain, diff name-status, inventory, repo file inventory, checks.
  - `metadata/audit-package.input.json` — checks, repo file globs/allowlist, `metadata.previousVerdictChain`, `metadata.blockerClosureList`.
  - `repo-file-hashes.json` — список repo-owned файлов и их ожидаемые SHA-256.
  - `T-0001.patch` — все изменённые области по `.codex/skills/submit-external-audit/*`, `AGENTS.md`, `data/documentation/electron2d-local-docs-index.json`, `docs/release-management/AUDIT-REQUEST.md`, `docs/release-management/audit-package.md`, previous verdict files, `eng/Electron2D.Build/*` и `tests/Electron2D.Tests.Integration/RepositoryBuildToolTests.cs`.
- Для файлов, добавленных целиком и доступных через patch, проверено восстановление текста и SHA-256 against `repo-file-hashes.json`: `.codex/skills/submit-external-audit/SKILL.md`, `.codex/skills/submit-external-audit/agents/openai.yaml`, `docs/verdicts/release-management/t-0001-audit-r02.md`, `docs/verdicts/release-management/t-0001-audit-r03.md`, `eng/Electron2D.Build/AuditSubmitCodexChromeCommand.cs`, `eng/Electron2D.Build/AuditSubmitCommand.cs`.
- Previous verdict chain:
  - `docs/verdicts/release-management/t-0001-audit-r02.md` прочитан; blocker-ы r02 B1 и r02 B2 выписаны.
  - `docs/verdicts/release-management/t-0001-audit-r03.md` прочитан; blocker r03 B1 выписан.
  - Внутри текущего пакета эти previous verdict files присутствуют как полные новые repo-owned files, перечислены в manifest и `repo-file-hashes.json`; явных признаков сокращения или переоформления внутри самого пакета не найдено.
- `metadata.blockerClosureList` прочитан полностью. Структурно подтверждаются closure items про codex-only backend, removal of auth command, one-ZIP submission, ping/pong, session-id hardening, CDP retry/reattach и отказ от stale-tab cleanup пользовательских вкладок. Не подтверждается closure item про чтение full verdict text именно из report card DOM и rejection of full-page scans; см. B1.
- Raw evidence проверено:
  - `evidence/T-0001-r08/checks/audit-submit-focused-tests/*` — expected/actual exit code 0, duration 35916.951 ms, stdout с 18 passed tests, TRX присутствует.
  - `evidence/T-0001-r08/checks/verify-docs/*` — expected/actual exit code 0, локальный documentation index verified.
  - `evidence/T-0001-r08/checks/verify-source-license-headers/*` — expected/actual exit code 0.
  - `evidence/T-0001-r08/checks/git-diff-check/*` — expected/actual exit code 0, stdout/stderr пустые.
- Documentation review:
  - `docs/release-management/AUDIT-REQUEST.md` обновлён под single main audit ZIP и content-focused external audit.
  - `docs/release-management/audit-package.md`, `AGENTS.md` и SKILL описывают один supported path через `audit submit --browser-backend codex-chrome`, реальный `Глубокое исследование`, один основной ZIP, отсутствие отдельного auth command, запрет manual browser path и запрет whole-page extraction.
  - Именно это описание расходится с B1 в части card-only extraction.
- Secret scanning:
  - Реальных секретов, приватных ключей, bearer/API tokens, паролей и локальных абсолютных путей диска не найдено.
  - Найдены только ожидаемые placeholders `<repo-root>`, `<clean-repo-path>`, фиксированный named-pipe literal `\\.\pipe\...` в тестовом контексте и публичный email автора в MIT license header.
- Scope scanning:
  - Основной объём изменения относится к release-management / audit-submit scope.
  - В `data/documentation/electron2d-local-docs-index.json` есть дополнительная generated запись для `docs/verdicts/release-management/t-0230-audit-r04.md`; это отмечено как collateral/generated spillover, но само по себе не стало blocker-ом при отсутствии доказательства вредного влияния и при успешном `verify-docs`.

RISKS_AND_NOTES:
- Остаточный риск после исправления blocker-ов всё равно останется операционным: зависимость от текущего ChatGPT DOM и extension protocol. Но этот риск допустим только после того, как final-report extraction действительно будет жёстко привязан к открытой report card.
- Отсутствие sidecar/operator-workflow ZIP в текущем r08 не считалось blocker-ом: текущий `AUDIT-REQUEST.md` и `metadata/audit-package.input.json` прямо переводят внешний аудит на content review по одному основному архиву.
- Отсутствие отдельных копий модифицированных файлов вне patch тоже не считалось blocker-ом: запрос явно разрешает анализ изменения по `<task-id>.patch`, а для новых файлов их полный текст удалось восстановить и сверить по hash model.
- `data/documentation/electron2d-local-docs-index.json` содержит unrelated generated entry `t-0230-audit-r04.md`; это замечание по scope, но не доказуемый blocker в пределах текущей задачи.

CLOSURE_DECISION:
- Задача остаётся открытой до исправлений. Пакет доказывает значительную переработку `audit submit`, но не доказывает главный контракт текущей итерации: acceptance только из открытой карточки отчёта и отказ от whole-page/ambiguous sources. Пока B1-B2 не устранены и не подтверждены поведенческими regression tests, закрывать задачу нельзя.
# Аудит T-0207 r01: внутренний C# repository tool

- Задача: `T-0207`
- Домен: `release-management`
- Актуально на: 2026-06-26T03:40:16+03:00
- Область проверки: `T-0207-audit-r01.zip`, внутренний C# repository tool `eng/Electron2D.Build`, доменные документы release-management, focused tests, evidence, manifest и patch.
- Статус вывода: `VERDICT: NEEDS_FIXES`
- Предыдущий аудит: нет.
- Следующий аудит: `docs/verdicts/release-management/t-0207-audit-r02.md` после исправлений.

## Полный ответ аудитора

```text
VERDICT: NEEDS_FIXES Blocker: SHA256SUMS.txt / archive reproducibility Критерий: archive consistency; manifest/checksum verification must be reproducible from the extracted audit archive. Evidence: SHA256SUMS.txt:3 contains dev-diary/2026/06 ????/26-06-2026.md, while the archive entry is the real diary path under dev-diary/2026/06 Июнь/26-06-2026.md. Running sha256sum -c SHA256SUMS.txt fails with No such file or directory for that line. This makes the audit package internally non-verifiable. Fix: regenerate SHA256SUMS.txt with stable UTF-8 paths, or use ASCII-only artifact paths for audit packages. Do not let Windows codepage replacement turn Cyrillic path segments into ????. Проверка: after extraction on Linux and Windows, sha256sum -c SHA256SUMS.txt must pass for every listed file. Blocker: T-0207.patch / incomplete patch model Критерий: patch must restore the complete task-owned acceptance state from baseline. Evidence: T-0207.patch contains diffs only for 7 files: data/documentation/electron2d-local-docs-index.json, two release-management docs, docs/releases/0.1.0-preview.md, eng/Electron2D.Build/*, and RepositoryBuildToolTests.cs. The archive also contains changed TASKS.md and the dev diary, but neither is present in the patch. Therefore the archive cannot be reproduced from baseline using the patch. Fix: regenerate the patch after all workflow files are updated, including TASKS.md and the diary, or explicitly split patch/evidence artifacts with a documented restore procedure that recreates the exact archive state. Проверка: on a clean checkout of baseline 5f428e33de733afdd6da9ed5d04636faef5c31e5, apply the patch, then compare file list and SHA-256 of every task-owned file against the archive. Blocker: TDD evidence is asserted, not evidenced Критерий: red/green focused evidence must be inspectable, not only described in the manifest. Evidence: AUDIT-MANIFEST.md claims a red run, green run, build, license, docs, tasks, and whitespace checks, but the archive contains no red log, green log, TRX, command transcript, restore log, or state snapshot. The only evidence is a prose statement in the manifest. For strict external acceptance, that is insufficient to prove the TDD sequence. Fix: add evidence artifacts, for example .temp/t-0207-evidence/red/, .temp/t-0207-evidence/green/, .temp/t-0207-evidence/checks/, with command, exit code, stdout/stderr, and preferably TRX where applicable. Include those files in SHA256SUMS.txt and AUDIT-MANIFEST.md. Проверка: auditor can open the archive and verify the red failure, green pass, build/verifier exit codes, and exact commands without trusting diary prose. Blocker: tests/Electron2D.Tests.Integration/RepositoryBuildToolTests.cs / release verify route is not covered Критерий: T-0207 requires command skeleton support for release verify; user-specified audit scope requires that release verify fails closed and creates no release artifacts. Evidence: the test file covers unknown command, test, verify, verify readme, verify docs, update wiki --check, package, process exit code, and timeout. It does not contain a focused test for release verify. The production code has RouteReleaseAsync, but the acceptance evidence does not prove its exit code, diagnostic code, or no-artifact behavior. Fix: add a focused test such as ReleaseVerifyFailsClosedWithoutArtifacts, asserting non-zero exit code, E2D-BUILD-RELEASE-VERIFY-BLOCKED, command=release, step=release verify, and absence of release artifacts/tags/files. Проверка: dotnet test tests\\Electron2D.Tests.Integration\\Electron2D.Tests.Integration.csproj --filter FullyQualifiedName~RepositoryBuildToolTests passes and includes the new release verify case. Blocker: tests/Electron2D.Tests.Integration/RepositoryBuildToolTests.cs / no-artifact guarantee is not tested for package --rid Критерий: package --rid <rid> must intentionally fail closed before packaging tasks and must not create release/package artifacts. Evidence: PackageRequiresRidAndReportsChosenRid checks missing --rid, non-zero exit, E2D-BUILD-PACKAGE-BLOCKED, and runtimeIdentifier=win-x64. It does not assert that no .zip, .tar.gz, release-manifest.json, checksum file, artifacts directory, or release output was created. Static code currently does not write files, but acceptance needs a regression test because this is a release-safety boundary. Fix: run the command from an isolated temp working directory or snapshot the repository output paths before/after, then assert no package/release artifacts are created. Проверка: focused test fails on any accidental artifact creation and passes for the current fail-closed implementation. Blocker: eng/Electron2D.Build/Program.cs / external cancellation can leave child process alive Критерий: domain document requires the process runner to support cancellation and timeout, with portable process execution and structured diagnostics. Evidence: ProcessRunner.RunAsync creates a linked cancellation token for WaitForExitAsync, but kills the process only in the timeout branch: catch (OperationCanceledException) when (timeout.IsCancellationRequested && !cancellationToken.IsCancellationRequested). If the caller’s cancellationToken is cancelled, the exception escapes and TryKillProcessTree(process) is not called. That violates cancellation semantics and risks orphaned child processes. Fix: handle caller cancellation explicitly: kill the process tree, wait for exit, drain stdout/stderr safely, and either return a structured E2D-BUILD-PROCESS-CANCELED diagnostic or rethrow only after cleanup and diagnostic emission. Проверка: add a test with a long-running child process and CancellationTokenSource.CancelAfter(...); assert the child is killed and structured cancellation behavior is stable. Blocker: eng/Electron2D.Build/Program.cs / package argument routing is too permissive Критерий: command routing must be stable and reject invalid CLI shapes. Evidence: RoutePackageAsync calls TryReadRuntimeIdentifier(args), and TryReadRuntimeIdentifier scans all arguments for --rid. This means invalid shapes such as package junk --rid win-x64, package --rid win-x64 --extra, or duplicate --rid can be accepted as E2D-BUILD-PACKAGE-BLOCKED instead of E2D-BUILD-CLI-INVALID-ARGUMENTS. For a future release/package command, permissive parsing is unsafe. Fix: require exact shape: args.Length == 3 && args[1] == "--rid" && !string.IsNullOrWhiteSpace(args[2]); reject all other forms with structured invalid-arguments diagnostics. Проверка: add focused tests for extra args, misplaced args, duplicate --rid, and missing value. Passed / non-blocking observations: The task statement in TASKS.md is scoped correctly for T-0207: internal repository/build tool, not public e2d CLI; command skeleton, process runner, timeout, exit codes, structured diagnostics, and cross-platform constraints are explicitly listed. Domain docs were updated in the right places: docs/release-management/ci-matrix.md, docs/release-management/release-packaging.md, and docs/releases/0.1.0-preview.md. Static code review confirms the current package --rid and release verify implementations only emit diagnostics and return blocked exit code; they do not currently write package/release files. Static code review confirms the process runner uses ProcessStartInfo.ArgumentList, UseShellExecute=false, explicit WorkingDirectory, redirected stdout/stderr, and no shell command-string composition. The archive file list does not show .git, bin, obj, .temp, old ZIPs, or obvious secret files. I did not independently rerun the .NET tests because this sandbox does not have dotnet installed; the rejection does not depend on rerunning them. The blockers are visible from the archive contents and source/evidence structure.
```

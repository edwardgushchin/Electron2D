VERDICT: NEEDS_FIXES

TASK_ASSESSMENT:

* Проверен пакет `T-0982` итерации `r08`. Область пакета одиночная: закрыть sanitizer-блокеры r01-r07, заменять quoted exact repo-root tokens и безопасные дочерние пути с платформенной семантикой регистра, разделителей и token terminator-ов, отклонять placeholder-per-character artifacts и сохранять direct/punctuation/parent-traversal/parent-traversal-root-token/case-variant/POSIX-backslash/quoted-punctuation нерепозиторные absolute paths как отказ `E2D-BUILD-AUDIT-ABSOLUTE-PATH`.
* Пакет содержит полные snapshots изменённых файлов. Предыдущие r01-r07 verdict-файлы доступны в архиве, `metadata.blockerClosureList` связывает каждый прошлый blocker с текущими focused sanitizer tests и `audit-loop-stabilization`. Конкретные прошлые примеры r01-r07 в основном закрыты тестами: direct sibling prefix, punctuation sibling prefix, `repo/../repo backup`, case-variant root, quoted exact root, POSIX backslash-after-root, POSIX quote-punctuation sibling path и `repo/..` перед token delimiter-ом.
* Изменение нельзя принять: sanitizer всё ещё не является полноценной проверкой path-token-а. Он может скрыть нерепозиторный absolute path, если parent traversal появляется после допустимого символа имени файла, который код считает token terminator-ом, а также может скрыть POSIX quoted-punctuation sibling path с comma/semicolon после quote delimiter-а.

Техническая привязка:

* `metadata.taskId`: `T-0982`
* `metadata.iteration`: `r08`
* `metadata.scopeTaskIds`: `["T-0982"]`
* `metadata.scopeSummary`: `Close T-0982 r01-r07 sanitizer blockers by replacing quoted exact repo-root tokens and safe child paths with platform path-case, separator and token-terminator semantics, rejecting broken placeholder-per-character evidence, and preserving direct, punctuation, parent-traversal, parent-traversal-root-token, case-variant, POSIX backslash and quoted-punctuation non-repository absolute paths as E2D-BUILD-AUDIT-ABSOLUTE-PATH blockers.`
* `metadata.previousVerdictChain`: `["docs/verdicts/release-management/t-0982-audit-r01.md", "docs/verdicts/release-management/t-0982-audit-r02.md", "docs/verdicts/release-management/t-0982-audit-r03.md", "docs/verdicts/release-management/t-0982-audit-r04.md", "docs/verdicts/release-management/t-0982-audit-r05.md", "docs/verdicts/release-management/t-0982-audit-r06.md", "docs/verdicts/release-management/t-0982-audit-r07.md"]`
* `metadata.blockerClosureList`: r01 `B1`, r02 `B1`, r03 `B1`, r04 `B1`, r05 `B1`, r06 `B1`/`B2`, r07 `B1` заявлены закрытыми через `t0982-r08-focused-sanitizer-tests` и `audit-loop-stabilization`
* Проверенные файлы: `repo-after/eng/Electron2D.Build/AuditPackageCommand.cs`, `repo-after/tests/Electron2D.Tests.Integration/RepositoryBuildToolTests.cs`, `repo-after/docs/release-management/audit-package.md`, `repo-after/TASKS.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r01.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r02.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r03.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r04.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r05.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r06.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r07.md`, `repo-after/data/documentation/electron2d-local-docs-index.json`, `repo-after/data/dev-diary/2026/07 Июль/07-07-2026.md`
* Проверенные артефакты: `AUDIT-MANIFEST.md`, `metadata/audit-package.input.json`, `metadata/repo-file-snapshots.json`, `repo-file-hashes.json`, `SHA256SUMS.txt`, `T-0982.patch`, `evidence/T-0982-r08/preflight/*`
* `combined scope`: не используется, область одиночная

BLOCKERS:

* B1

  * Что не так: sanitizer может скрыть parent-traversal absolute path, если `..` появляется после сегмента с пробелом, запятой, точкой с запятой, скобкой или другим символом, который код считает token terminator-ом. Например, путь `/home/user/repo/dir with space/../../repo backup/logs.txt` при `repoRoot = /home/user/repo` указывает на sibling-каталог `/home/user/repo backup`, то есть вне репозитория. Текущий `HasParentTraversalSegmentAfterRepoRoot` останавливает сканирование сегмента на пробеле внутри `dir with space`, возвращает `false`, и sanitizer заменяет prefix на `<repo>`. В архив попадёт `<repo>/dir with space/../../repo backup/logs.txt`, а исходный machine-local absolute prefix уже не будет виден последующей проверке.
  * Почему это важно: текущая область r08 требует сохранять parent-traversal и другие нерепозиторные absolute paths как blockers `E2D-BUILD-AUDIT-ABSOLUTE-PATH`. Пробелы, запятые, скобки и точка с запятой являются допустимыми символами имени файла как минимум на POSIX, а пробелы также обычны в Windows paths. Поэтому нельзя считать их безусловным концом пути и после этого fail-open заменять repo-root.
  * Что исправить: перейти от локального сканирования до первого token terminator-а к полноценному path-token parsing или fail-closed нормализации. Минимально нужно не завершать проверку безопасности child path на символе, который может быть частью имени файла, если после него в той же строке продолжается path-like tail с separator-ами и `..`-сегментами. Надёжнее выделять весь path-token, нормализовать его с платформенной семантикой и заменять только если нормализованный путь остаётся внутри `repoRoot`.
  * Как проверить исправление: добавить focused integration test через production `audit package`, где imported preflight `output.txt` содержит `fixture.RepositoryRoot + "/dir with space/../../repo backup/logs.txt"` и native separator variant. Ожидаемый результат — команда завершается отказом `E2D-BUILD-AUDIT-ABSOLUTE-PATH`, а не создаёт ZIP с `<repo>/dir with space/../../repo backup/logs.txt`. Существующие позитивные tests для настоящих child paths и exact repo-root tokens должны продолжать проходить.
  * Проверка опровержения: проверены текущие tests, документация, closure list и evidence. `AuditPackageRejectsPreflightEvidenceTraversalSiblingPathPrefix` покрывает только форму `repo/../repo backup/...`, где `..` идёт сразу после separator-а. `AuditPackageRejectsPreflightEvidenceParentTraversalRootToken` покрывает только `repo/..` перед quote delimiter-ом. Ни один текущий тест не проверяет parent traversal после path segment с пробелом или другим valid filename character, который код считает token terminator-ом. Evidence `9/9` и `audit-loop-stabilization` не опровергают проблему, потому этот случай отсутствует в проверяемом наборе.
  * Техническая привязка:

    * `File/symbol`: `repo-after/eng/Electron2D.Build/AuditPackageCommand.cs`, `IsRepoRootPathCandidateBoundary`, строки 1449-1477; `HasParentTraversalSegmentAfterRepoRoot`, строки 1507-1542; `IsRepoRootPathTokenTerminator`, строки 1559-1562; `ValidateMachineLocalPathText`, строки 4614-4622.
    * `Criterion`: `metadata.scopeSummary`; `repo-after/TASKS.md`, строки 3728-3731, 3759, 3763-3766, 3795; `repo-after/docs/release-management/audit-package.md`, строка 451.
    * `Evidence`: цикл строк 1519-1522 завершает segment на `IsRepoRootPathTokenTerminator`; строка 1536 затем возвращает `false` для terminator-а, не продолжая поиск последующих `..`. Строка 1561 включает в terminator-ы пробел, запятую, точку с запятой и скобки.
    * `Impact`: нерепозиторный parent-traversal absolute path может быть замаскирован как `<repo>/...`, и archive content validation уже не увидит исходный local absolute path.
    * `Fix`: реализовать token-aware/path-normalizing проверку child paths или fail-closed правило для ambiguous path tails после filename-valid terminator characters.
    * `Verification`: focused sanitizer test должен ожидать `E2D-BUILD-AUDIT-ABSOLUTE-PATH` для `repoRoot/dir with space/../../sibling/...`; прежние r01-r07 regression tests и positive sanitization tests должны остаться зелёными.

* B2

  * Что не так: quoted exact-root handling всё ещё принимает comma/semicolon после закрывающей кавычки как безопасный конец token-а, не проверяя, что за comma/semicolon не продолжается POSIX path. Например, строка `"/home/user/repo",backup/logs.txt` содержит POSIX absolute path `/home/user/repo",backup/logs.txt` к sibling-каталогу `repo",backup` вне репозитория. При `repoRoot = /home/user/repo` текущий код видит matching opening quote перед root, closing quote после root и comma после quote, считает это exact-root token-ом и заменяет prefix на `<repo>`. В архив попадёт `"<repo>",backup/logs.txt`, а исходный `/home/user/repo...` будет скрыт.
  * Почему это важно: r08 scope прямо говорит, что quoted-punctuation non-repository absolute paths должны оставаться под `E2D-BUILD-AUDIT-ABSOLUTE-PATH`. Текущий тест закрывает только вариант с `")backup`, но comma и semicolon также являются punctuation и допустимы в POSIX имени файла. Следовательно, r06 `B2` закрыт неполно: sanitizer всё ещё может скрыть quoted-punctuation sibling path.
  * Что исправить: сделать exact-root replacement действительно token-aware. Для `metadata.json` лучше нормализовать JSON string values структурно. Для raw `output.txt`/`result.txt` closing quote/backtick должен приниматься только если доказан конец quoted token-а, а последующий tail не выглядит как продолжение path segment. Ambiguous tails вида `",backup/...` и `";backup/...` должны fail closed и попадать под existing absolute-path guard.
  * Как проверить исправление: добавить focused regression через production `audit package` на non-Windows/POSIX, где imported preflight `output.txt` содержит `fixture.RepositoryRoot.Replace('\\', '/') + "\",backup/logs.txt"` и `... + "\";backup/logs.txt"`. Ожидаемый результат — `E2D-BUILD-AUDIT-ABSOLUTE-PATH`, а не ZIP с `"<repo>",backup/...`. Позитивный test для настоящих exact repo-root values в `metadata.json`, `output.txt` и `result.txt` должен продолжать заменять их на `<repo>`.
  * Проверка опровержения: проверены `AuditPackageRejectsPreflightEvidencePosixSiblingPathTokens` и `AuditPackageSanitizesExactPreflightEvidenceRepositoryRootTokens`. Первый тест покрывает POSIX backslash-after-root и quote-punctuation sibling only для `")backup`, но не для `",backup` или `";backup`. Второй тест доказывает замену настоящих quoted exact roots, но не доказывает, что comma/semicolon после quote безопасны как tail. Evidence `9/9` на текущем пакете не закрывает этот вариант; на Windows POSIX-guarded assertions в тесте возвращаются без активной POSIX проверки.
  * Техническая привязка:

    * `File/symbol`: `repo-after/eng/Electron2D.Build/AuditPackageCommand.cs`, `IsRepoRootExactTokenEndDelimiter`, строки 1480-1505; `IsRepoRootPathCandidateBoundary`, строки 1449-1477; `ValidateMachineLocalPathText`, строки 4614-4622.
    * `Criterion`: `metadata.scopeSummary`; `repo-after/TASKS.md`, строки 3728-3731, 3759, 3763-3766, 3795; `repo-after/docs/release-management/audit-package.md`, строка 451.
    * `Evidence`: строка 1488 требует matching opening delimiter, но строка 1504 без дополнительного tail parsing считает `,` и `;` безопасным окончанием token-а. Проверки, что после comma/semicolon не продолжается path-like suffix, нет.
    * `Impact`: POSIX quoted-punctuation sibling absolute path может быть замаскирован как `<repo>...`, что нарушает fail-closed contract текущей задачи и делает imported preflight evidence небезопасно переносимым.
    * `Fix`: структурная нормализация JSON values и token-aware validation для raw text exact-root replacement; ambiguous quoted punctuation path tails должны оставаться blockers.
    * `Verification`: focused sanitizer tests должны ожидать `E2D-BUILD-AUDIT-ABSOLUTE-PATH` для `",backup/...` и `";backup/...`; existing exact-root positive tests должны продолжать проходить.

EVIDENCE_REVIEW:

* Полнота снимков проверена. `metadata/repo-file-snapshots.json` содержит полные `repo-after/` и `repo-before/` snapshots для всех изменённых файлов; `repo-file-hashes.json` совпадает с `repo-after/`, а `SHA256SUMS.txt` совпадает с файлами архива при чтении ZIP как UTF-8.
* Реализация проверена по полному `repo-after/eng/Electron2D.Build/AuditPackageCommand.cs`. Imported preflight evidence проходит через staging-copy, `PreparePreflightEvidenceSource`, `WriteNormalizedPreflightMetadata`, `SanitizePreflightEvidenceText`, `ReplaceRepoRootPathCandidates`, `ValidateNoBrokenPreflightPlaceholderText` и затем через обычную archive content validation. Конкретные r01-r07 fixes присутствуют, но replacement всё ещё основан на локальных boundary heuristics, а не на проверке полного path-token-а.
* Тесты проверены по полному `repo-after/tests/Electron2D.Tests.Integration/RepositoryBuildToolTests.cs`. Focused suite содержит девять tests: positive child path/readability, broken placeholder rejection, direct sibling rejection, punctuation sibling rejection, traversal sibling rejection, case-variant rejection on case-sensitive platforms, exact quoted root sanitization, POSIX sibling token rejection и parent-traversal root-token rejection. Не покрыты parent traversal после filename-valid terminator внутри child segment и POSIX quoted comma/semicolon sibling paths.
* Документация проверена по `repo-after/docs/release-management/audit-package.md`. Документ описывает atomic `<repo>` replacement только для whole repo-root/safe child paths и обещает, что sibling/non-repository paths, parent traversal, POSIX backslash и quoted-punctuation cases остаются blockers. Реализация расходится с этим обещанием для B1 и B2.
* Проверка прошлых verdict-файлов выполнена. В пакете доступны r01-r07 отчёты из `metadata.previousVerdictChain`. Прошлые blocker-и прочитаны: r01 — direct sibling prefix, r02 — punctuation sibling prefix, r03 — parent traversal sibling path через `repo/../repo backup`, r04 — case-insensitive matching on case-sensitive platforms, r05 — exact quoted repo-root token, r06 — POSIX backslash sibling и quote-punctuation sibling, r07 — `repo/..` перед token delimiter-ом. Текущие tests закрывают эти конкретные examples, но не закрывают B1/B2 выше.
* Preflight evidence проверены. Все заявленные проверки завершились exit code `0`: build tool build, focused sanitizer tests `9/9`, `audit-loop-stabilization-r08` `9/9`, `verify audit-contracts`, `update docs --check`, `verify docs`, `verify audit-followups`, `verify licenses`, `git diff --check`, а также sanitizer fixture. Эти artifacts подтверждают выполненные проверки, но не покрывают два residual cases выше.
* Проверка области не выявила лишних runtime/API изменений. Изменения ограничены audit package build tool, integration tests, release-management documentation, generated docs index, task card, diary и сохранёнными r01-r07 reports для previous verdict chain. Public API, игровой runtime, rendering/input/editor/browser submission path и Godot compatibility surface не менялись.
* Проверка секретов и локальных данных не выявила реальных секретов, приватных ключей, токенов или конфиденциальных локальных путей в current included artifacts. Синтетические Windows/Linux/POSIX paths находятся в saved audit reports, tests, docs или patch-блоках previous verdict context как примеры прошлых blocker-ов; current preflight evidence использует `<repo>` placeholders и не раскрывает локальный repo-root.

Техническая привязка:

* `implementation content review`: `repo-after/eng/Electron2D.Build/AuditPackageCommand.cs`
* `test coverage review`: `repo-after/tests/Electron2D.Tests.Integration/RepositoryBuildToolTests.cs`
* `documentation review`: `repo-after/docs/release-management/audit-package.md`, `repo-after/data/documentation/electron2d-local-docs-index.json`
* `task compliance review`: `repo-after/TASKS.md`, `AUDIT-MANIFEST.md`, `metadata/audit-package.input.json`
* `previous verdict files`: `repo-after/docs/verdicts/release-management/t-0982-audit-r01.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r02.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r03.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r04.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r05.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r06.md`, `repo-after/docs/verdicts/release-management/t-0982-audit-r07.md`
* `previous blockers closure`: r01 `B1`, r02 `B1`, r03 `B1`, r04 `B1`, r05 `B1`, r06 `B1`/`B2`, r07 `B1` проверены; конкретные прошлые examples закрыты, но общий sanitizer contract остаётся не закрыт из-за B1/B2 выше.
* `secret scanning`: `repo-after/`, `repo-before/`, `T-0982.patch`, `evidence/T-0982-r08/preflight/*`
* `scope scanning`: `repo-file-hashes.json`, `metadata/repo-file-snapshots.json`, `AUDIT-MANIFEST.md`
* `evidence gap`: нет по полноте snapshots; есть поведенческий gap в sanitizer regression coverage для path segments with filename-valid token terminators before traversal и POSIX quoted comma/semicolon sibling paths.
* Проверенные evidence paths: `evidence/T-0982-r08/preflight/audit-loop-stabilization/*`, `evidence/T-0982-r08/preflight/t0982-r08-build-tool-build/*`, `evidence/T-0982-r08/preflight/t0982-r08-focused-sanitizer-tests/*`, `evidence/T-0982-r08/preflight/t0982-r08-sanitizer-fixture/*`, `evidence/T-0982-r08/preflight/t0982-r08-verify-audit-contracts/*`, `evidence/T-0982-r08/preflight/t0982-r08-update-docs-check/*`, `evidence/T-0982-r08/preflight/t0982-r08-verify-docs/*`, `evidence/T-0982-r08/preflight/t0982-r08-verify-audit-followups/*`, `evidence/T-0982-r08/preflight/t0982-r08-verify-licenses/*`, `evidence/T-0982-r08/preflight/t0982-r08-git-diff-check/*`

RISKS_AND_NOTES:

* None.

CLOSURE_DECISION:

* Задача остаётся открытой. Пакет r08 закрывает конкретные r01-r07 blocker-и и улучшает sanitizer boundary handling, но реализация всё ещё может маскировать нерепозиторные absolute paths как `<repo>...` из-за неполной проверки полного path-token-а. До исправления B1 и B2 принимать `T-0982` небезопасно: imported preflight evidence может выглядеть переносимым после скрытия machine-local path, который не является фактическим repo-root или безопасным дочерним путём репозитория.

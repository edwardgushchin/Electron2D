# T-0228 audit r01

- Задача: T-0228
- Домен: release-management
- Актуально на: 2026-06-26T22:14:12+03:00
- Область проверки: `T-0228-audit-r01.zip`, deterministic audit package tool, restore verification, evidence, manifest, patch, release workflow.
- Статус вывода: `VERDICT: NEEDS_FIXES`
- Предыдущий аудит: отсутствует
- Следующий аудит: `T-0228 audit r02` после исправления blocker-ов

```text
VERDICT: NEEDS_FIXES

Blocker: evidence/T-0228-r01/checks/*/metadata.json, критерий: каждый evidence item обязан иметь машиночитаемые timeout, expected exit code, actual exit code, duration, stdout/stderr paths и SHA, согласованные с sidecar-файлами; evidence: в focused-repository-build-tool-tests/exit-code.txt указано expected: 0, actual: 0, а timeout-seconds.txt — 600, но metadata.json содержит expectedExitCode: 600, timeoutSeconds: 0; такая же перестановка видна у остальных checks, например git-diff-check даёт expectedExitCode: 120, timeoutSeconds: 0; fix: исправить порядок аргументов CheckEvidenceMetadata в RunConfiguredChecksAsync, записывать поля из config/result без перестановки и добавить regression-test на совпадение metadata.json с exit-code.txt и timeout-seconds.txt; проверка: пересобрать пакет и выполнить jq '.expectedExitCode,.timeoutSeconds,.actualExitCode' evidence/T-0228-r01/checks/*/metadata.json либо C#-проверку, затем audit package verify.

Blocker: evidence/T-0228-r01/checks/focused-repository-build-tool-tests и verify-local-documentation, критерий: checks/evidence должны покрывать заявленные критерии и быть inspectable raw evidence; evidence: test command запускает dotnet test ... --verbosity quiet *> $null; exit $LASTEXITCODE, поэтому stdout.txt и stderr.txt пустые, trxFiles пустой, фактическое количество тестов/названия упавших или пройденных тестов в архиве не проверяемы; для verify-local-documentation вывод также перенаправлен в $null; отдельно в archive evidence нет standalone-команд audit package на минимальном fixture и audit package verify, хотя они прямо перечислены в критериях T-0228 и доменном документе; fix: убрать подавление stdout/stderr, включить TRX для dotnet test через --logger trx --results-directory ..., добавить отдельные evidence entries для audit package fixture run и audit package verify; проверка: новый архив должен содержать непустой stdout/stderr или TRX для focused tests, а manifest должен ссылаться на evidence для package и verify runs.

Blocker: eng/Electron2D.Build/AuditPackageCommand.cs / VerifyGeneratedTaskIds, критерий: инструмент должен поддерживать previousVerdictChain и blockerClosureList, а исторические ссылки на другие task id должны быть разрешены только в явно переданных исторических списках/evidence; evidence: реализация сканирует весь текст metadata/audit-package.input.json, AUDIT-MANIFEST.md, AUDIT-REQUEST.md и repo-file-hashes.json регулярным выражением T-\d{4} и отклоняет любой id, отличный от текущего taskId; это сломает r02/r03-пакеты, где previousVerdictChain содержит, например, verdict path другой задачи или blocker text с историческим T-0213/T-0207; fix: проверять task id структурно: generated paths/root names должны совпадать с текущим task id, но поля previousVerdictChain и blockerClosureList должны иметь явный allowlist для исторических ссылок; проверка: добавить fixture с taskId = T-0001, previousVerdictChain = ["docs/verdicts/release-management/t-0207-audit-r04.md"] и blocker text с T-0207, затем audit package + audit package verify должны проходить.

Blocker: eng/Electron2D.Build/AuditPackageCommand.cs / restore verification, критерий: audit package verify должен быть источником истины восстановления и сравнивать восстановленное состояние с expected restore manifest, включая список файлов; evidence: после git apply код проверяет только SHA файлов, перечисленных в repo-file-hashes.json, и deleted list, но не сравнивает фактический changed-file set patch-а с manifest; архив с patch, который дополнительно меняет неучтённый repo-owned файл, может пройти после обновления SHA256SUMS.txt, потому что extra file не проверяется; fix: после apply выполнить git diff --name-status <baseline> -- или эквивалент и сравнить точный набор changed/deleted paths с repo-file-hashes.json; любые extra/missing paths должны давать E2D-BUILD-AUDIT-RESTORE-MISMATCH; проверка: добавить tampered-archive fixture, где patch меняет дополнительный файл вне restore manifest, обновить checksum и убедиться, что audit package verify падает.

Blocker: ZIP determinism / UTF-8 archive contract, критерий: задача требует детерминированный ZIP с UTF-8 paths/flag и проверкой archive structure; evidence: у записей текущего T-0228-audit-r01.zip central directory flag не содержит UTF-8 bit 0x800 — для inspected entries flag равен 0x2; audit package verify также не проверяет UTF-8 flag, compression method/level или external attributes, хотя task criteria требуют стабильный ZIP contract; fix: либо явно обеспечить UTF-8 flag для всех entries выбранным ZIP writer-ом, либо уточнить контракт и тесты так, чтобы UTF-8 flag требовался минимум для non-ASCII paths, плюс добавить verify-проверку flags/external attributes; проверка: regression-test на archive-only evidence path с кириллицей и проверкой entry.FullName, UTF-8 flag, sorted entries, timestamp, отсутствия platform external attributes.

Blocker: patch/task-owned scope, критерий: task-owned changes должны быть минимальны и без unrelated formatting churn; evidence: T-0228.patch содержит массовую line-ending/formatting churn вместо точечных изменений: docs/releases/0.1.0-preview.md меняется примерно +1372/-1371, Program.cs примерно +416/-408, AGENTS.md примерно +93/-87, при этом git-diff-check/stderr.txt фиксирует предупреждения о LF will be replaced by CRLF для нескольких файлов; такой patch затрудняет review и увеличивает риск чужих изменений; fix: восстановить исходные line endings для существующих файлов, оставить только содержательные hunks T-0228, при необходимости добавить .gitattributes отдельной задачей/коммитом; проверка: git diff --stat и git diff --word-diff должны показывать точечные изменения, git diff --check не должен выдавать line-ending warnings.
```
